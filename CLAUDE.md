# CLAUDE.md — 人与 AI 的协作契约

本文件只保存**长期有效的协作规则、权限边界和判定原则**。当前模型、当前任务与截止日见
[`ROADMAP.md`](ROADMAP.md)；研究上下文与命令见 [`NOTES.md`](NOTES.md)；完整旧文与主题历史见
[`research_history/`](research_history/README.md)。

## 1. 绝对禁止

1. 不删除、不移动、不重命名任何文件；需要清理时列出清单，由用户执行。
2. 不执行 `git commit`、`git push`、`git tag`。
3. 不修改 `docs/`、`examples/`、`timeseries_api/`；它们是主办方原文，只读。
4. 不生成公榜 CSV，不打包私榜 zip；`scripts/make_submission.py` 只能由用户执行。
5. 不直接覆盖 `strategies/v3_hybrid/model/`；候选必须先 staging、验证，再由用户明确授权转正。
6. AI 不得自行向“人工核实事实”增加条目，也不得把实验观察升级为人工事实。

## 2. 开始工作前的阅读顺序

1. 本文件：确认边界和工作规则。
2. [`ROADMAP.md`](ROADMAP.md)：确认当前生产基线、正在做什么和下一步顺序。
3. [`NOTES.md`](NOTES.md)：确认研究口径、常用命令和最近探索。
4. 任务对应的实验报告、manifest、模型 meta 和代码；不要只依据概述文档。
5. 若需要理解结论演变，再进入 [`research_history/`](research_history/README.md) 或原文快照。

发生冲突时，可信度顺序为：

```text
当前代码 / 模型产物 / manifest / ledger
> 对应实验 JSON/Markdown
> ROADMAP 当前状态
> NOTES 研究记录
> research_history 历史叙述
```

历史文件只说明“当时为什么这样判断”，不能覆盖当前产物真值。

## 3. 信息标签

研究记录使用以下标签，避免把不同可信度的信息混在一起：

- `FACT`：由用户亲自核实，只有用户可以新增。
- `RESULT`：在明确数据、切分和指标口径下得到的实验结果。
- `HYPOTHESIS`：尚未验证的机制或方向。
- `REJECTED`：已被实验否决，写清否决口径和证据。
- `SUPERSEDED`：曾经有效，但被更新模型、更新数据或更强证据替代。
- `INCIDENT`：工程事故、根因和防复发门禁。

任何结论都应尽量附日期、模型/数据版本和证据路径。

## 4. 人工核实事实

以下条目由用户本人确认，AI 只能原样保留、引用或指出后续证据带来的解释变化：

- 2026-07-24：市场共同分量占 target 方差 73%（当时只在 p008 测量，后续已做九分区复验）。
- 2026-07-24：分区级加权均值符号为 5 正 4 负，长期漂移方向不稳定。
- 2026-07-24：`train.py` 的特征选择和预处理只在训练段拟合，没有使用验证段。
- 2026-08-08：九个训练分区的市场共同分量占比为 0.626–0.732，均值 68.4%±3.4%；
  p008 的 0.732 是最高值。证据：`outputs/experiments/mt_diagnostics.md`。
- 2026-08-08：分区长期漂移符号不稳定，不等于逐 `time_id` 市场分量不可预测；市场分量样本外
  R² 约 0.0018，8/10 折为正。证据：`outputs/experiments/mt_predictability.md`、
  `outputs/experiments/ab_meandev.md`。
- 2026-08-31：**提交策略的存放路径与开发环境（JupyterHub）是分开的，选手看不到。**
  向主办方问得。⟹ 官方 `submission_and_evaluation.md` 里的 `~/submit`
  （「评测系统会导入 `~/submit/main.py`」、检查清单「`~/submit` 已创建，内容与 ZIP 一致」）
  只是**选手侧自检用的本地目录**，不是评测端实际导入代码的位置；评测端用的是上传的 ZIP。
  ⟹ 「JupyterHub 上残留旧 `~/submit` 被误用」这条假想风险不存在。
  证据：`RUNBOOK_8_23.md` §D6 上传日卡片 2026-08-31 条。

## 5. 研究与代码修改规则

1. **先写变量和判据，再看结果。** 说明改了什么、为何可能有效、通过/否决门槛和停止条件。
2. **一次只回答一个问题。** 若必须同时改多项，要明确是组合臂，不能把收益归给其中一项。
3. **时序因果优先。** 特征、切分、归一化、选列和调参都只能使用当时可见数据；保留 embargo。
4. **训练与推理必须同口径。** 对树模型，数学等价不够；归约顺序、dtype 和列顺序也要一致。
5. **比较模型要比较 peak/IC 或同一 scale 下的原始分。** 不用不同 scale 的单点分数排模型。
6. **本地绝对分不用于排序生产候选。** 主要看配对增量、同号折数、去最好折和机制拆解。
7. **代理量不可跨结构搬用。** A/B、无权 B、迁移折扣等近似换模型族后必须重新验证。
8. **失败也要留证。** `REJECTED` 项记录实验口径、失败幅度和重新开放条件，避免重复探索。
9. 修改公共基础设施或生产推理路径后，至少运行相关单测、完整 unittest 和一致性检查。
10. 数据、模型和报告产物不得静默覆盖；覆盖必须使用已有的显式 `--force` 门禁。

## 6. 模型晋级与交付门禁

候选模型必须经过以下顺序：

```text
候选目录
→ promotion staging
→ meta 结构校验
→ LightGBM / NumPy 后端对拍
→ train / inference 一致性
→ 端到端顺序推理与耗时检查
→ 用户确认
→ 原子转正并保留备份
→ 用户执行私榜打包
```

关键要求：

- `blend_weight`、`prediction_scale`、轮数、种子数、history、市场模型和训练权重都属于模型身份。
- 烟测只证明“能跑”不证明“是榜上模型”；必须核对 meta、文件数量和 hash。
- 测耗时必须测完整 runner，不能只计时 `booster.predict`。
- LightGBM 主路径与 NumPy 兜底都要验证；兜底性能单独列为交付风险。
- `timeseries_api` 内的超时是返回后的耗时判定，不是真正可中断的硬超时；最终验收看外层 wall clock。

## 7. 文档维护规则

- **状态变化**：更新 `ROADMAP.md`，不要写进本文件。
- **新实验过程**：先写 `NOTES.md` 的最近研究日志。
- **实验结案**：在 ROADMAP 留一行结论和证据，把完整过程归入对应主题历史。
- **长期规则**：只有跨多个任务仍有效的规则才进入本文件。
- **生产数值**：优先引用 `hybrid_meta.json`、promotion manifest 和 `experiments/ledger.csv`；
  不在多处手工维护同一个数字。
- **旧结论**：不删除；标为 `SUPERSEDED` 并链接到新结论或原文快照。

## 8. 长期伤疤规则

1. 本地验证曾多次在 alpha、轮数和 history 宽度等“拟合紧密度”轴上与公榜量反；此类参数不得
   只凭本地分数晋级。
2. 公榜 CSV 曾由临时 override 生成，而候选 meta 仍保留训练占位值；发布必须验证完整模型身份。
3. LightGBM 输入相差一个 float32 ULP 也可能翻叶子；训练和推理必须调用同一特征实现。
4. 各策略都有 `main`、`train`、`features` 等同名模块；跨策略加载要按文件路径隔离，不能依赖
   `sys.path` 恰好排序正确。
5. 小批量推理中 pandas 取列和历史状态维护可能比树本身更贵；性能判断必须端到端测量。
6. “低相关”不代表适合集成；弱模型可能只是噪声，必须看组合后的目标指标。
7. 同期 responder 相关性不代表可部署增量；必须先过样本外可预测性和 target 残差增量门禁。
8. 主办方原始发布包曾缺少可核验基线；收到更新包时必须先做文件级数据审计和只读目录 diff。
9. 「`peak = A²/B` 对缩放不变」只对**全局缩放**成立，不是「口径差异不要紧」的通行证。
   `blend_weight`、`long_window`、种子数都不是缩放，换了就是另一个模型。
   2026-08-24 首轮密封期标定因此作废：六个臂里四个用了 `train.py` 的占位
   `blend_weight=0.5 / scale=0.856`，而它们的公榜真值是在 `1.0 / 1.16` 上打的。
10. 预注册文件里的「这些候选都有已知公榜真值」这类**归属断言**必须当场核，不能默认成立。
   核法：拿新算出的逐行预测与留档公榜 CSV 对 `max|pred|`；比值恒等于 scale 之比才是同一模型，
   比值**散开**就说明混进了非缩放差异。
11. **预注册程序写死了「怎么做」，却没有机械手段核对「它量的是不是那个东西」。**
   2026-08-24 一天之内同型四次：① 密封期标定跑的是候选目录的占位 `blend_weight/scale`，
   不是有公榜真值的那个模型；② responder Stage C 读的缓存**建自回补窗口之前**，
   逐位复现旧结果却一行新数据都没碰；③ 重训候选缺 `slow_fast_*` 三键被 `main.py` 静默关掉，
   等于把「扩展数据」与「丢了一块 +2.93% 的结构」混在一起；④ D2b 的折版图让验证段在新数据
   **之前**，因果上根本回答不了「多给一段更近的数据值多少」。
   ⟹ 每个测量都要配一个**能失败的归属检查**，且它必须独立于被测量本身。三种有效形式：
   **对已知真值**（新算的逐行预测 vs 留档公榜 CSV 的 `max|pred|`）、
   **零对照**（构造上必须给 0 的那一格，如 D2b 的 fold 0 实测 Δ 逐位为 0）、
   **口径边界**（缓存/数据根的 `time_id` 范围与它声称回答的窗口对不对得上）。
12. 判据读数要核**分母**：`positive_folds: 2/4` 在 D2b 里是低估 —— fold 0 构造上不可能为正，
   诚实分母是 3。「分母里混进不可能为正的格子」会让真效应看起来更弱。
13. **本机 `/tmp` 是 tmpfs（在内存里）**，session scratchpad 就在它下面。
   往那里写大中间文件 = 占内存，且**进程退出后不释放**。
   2026-08-24：`v3_production_oof --disk-cache` 写进 scratchpad 的 3.9 GB memmap
   在 D2b 结束后一直挂着，先后掐死 D3 与 D4.5 两个任务，而两次的日志都毫无异常
   （SIGKILL 不留痕）。⟹ `--disk-cache` 只有指向**真实块设备**时才省内存；
   长任务开跑前要看 `df -h /tmp` 和 `free -g` 的 `shared` 列，不能只看 `available`。
14. **OOM 的归因不能靠「这一步看起来轻」来排除。** 同上那次，D3 死在 modulo 10 的加载阶段，
   我据此判「不像 OOM」——错的：它死在一台已被偷走 3.9 GB 的机器上。
   `status: killed` 不区分人停与内核杀；先查 `free`、`df -h /tmp`、内核日志，再下结论。
15. **内存峰值只认连续采样。** 手工读两次 `ps` 不是峰值 —— 2026-08-24 据此把 D4.5 的峰值
   记成 14.71 GB，实际峰值在设计矩阵构建阶段、更高。便宜的复现法：把轮数降到 20
   （设计矩阵不变、森林飞快），5 分钟就能量出整条曲线。
16. **长任务的输出不要接 `tail`/`head`。** 被 SIGKILL 时管道缓冲区里的内容全部丢失，
   现场就没了。直接 `> 文件`，要看尾部再对文件 `tail`。
17. **交付件的运行口径必须写在交付件里，环境变量不算数。** 2026-08-29：`main.py` 从不设
   `num_threads`，而历次交付验证的「4 线程」全部来自命令行前缀 `OMP_NUM_THREADS=4` ——
   那个开关**不随提交包走**。评测机 `os.cpu_count() = affinity = 128`、CPU 配额 4 核，
   lightgbm 默认 `-1` 起 128 线程挤 4 核，实测慢 **73.27×**，折算 6.04 h vs 总预算 2.98 h
   ⟹ 约一半 `time_id` 会被填 0。四次全量交付验证**全都没抓到**，因为它们量的正是那个外部开关。
   ⟹ 归属检查要多问一句：**「这个读数依赖的每一个口径，是不是都在交出去的那件东西里？」**
   环境变量、命令行前缀、shell 里的 `export`、开发机的核数，都不在。
   便宜的自检：把提交包解压到干净目录，**不带任何环境变量**跑，再和留档读数比。
18. **本地解释器比评测机新，会替我们兜住语法错。** 2026-08-29：本地 Python **3.13**、
   评测机 **3.11.15**。`verify_delivery_runtime.py` 里一处**跨行 f-string**（PEP 701，3.12+）
   本地跑了四天没事，云端开跑即 `SyntaxError` —— 而它是 08-25 加 `--from-zip` 时进来的，
   ⟹ **那条路径从加进来那天起就没在评测机的 Python 上跑过**，文档里却记着「已在真机验过」。
   ⚠️ `ast.parse(..., feature_version=(3, 11))` **抓不到**这类（3.12 的 f-string tokenizer
   不受它约束）。⟹ 进提交包的模块和要在评测机上跑的脚本，都要过
   `tests/test_eval_python_compat.py` 的 token 级扫描；版本口径取自评测机实测产物，不是记忆。
19. **判据要跟着比较的「轴」走。** 逐位指纹（`predictions_sha256`）只在**同机**比较里成立；
   跨机器有一条已量过的浮点轴（同机换后端 1.4e-15、**同后端换机器 2.3e-8**），指纹必然不同。
   2026-08-29：我预告「云端指纹必须等于本地的 `524e14e0…`」，实测 `21698601…`、
   `max|pred|` 相对差 1.35e-08 —— 落在已知轴上，判据本身写错了。
   ⟹ 跨机跑**同时**变了机器、依赖版本和被测开关，三个变量混在一起，
   本来就回答不了「这一个开关改没改预测」；那个问题只能用**同机 A/B**回答。
   定判据前先问：**这次比较只变了一个东西吗？**没有的话，换一个对该轴敏感的量
   （跨机比 `max|pred|` 的相对差量级，而不是 hash）。
