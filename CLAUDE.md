# CLAUDE.md

## 绝对禁止

1. 不删除、不移动、不重命名任何文件。需要清理时列出清单交给我执行。
2. 不 git commit / push / tag。
3. 不修改 `docs/` `examples/` `timeseries_api/`（主办方原文，只读）。
4. 不生成公榜提交，不打包 zip（打包走 `scripts/make_submission.py`，由我执行）。
5. AI 不得往「已核实的事实」里加条目。

## 已核实的事实

（只写我本人亲手验证过的，注明日期。）

- 2026-07-24 市场共同分量占 target 方差 73%（只在 p008 测过，待在 9 分区上复验）
- 2026-07-24 分区级加权均值符号随机（5 正 4 负），漂移不可预测
- 2026-07-24 train.py 特征选择与预处理均在训练段拟合，无泄漏
- 2026-08-08 share 的 73% 此前只在最后一个分区算过；9 个分区全算了一遍，
范围 0.626~0.732，均值 68.4%±3.4%，p008=0.732 恰是最高的那个
（证据 outputs/experiments/mt_diagnostics.md）
- 2026-08-08 上面「符号随机」那条复现无误，但它被错误解读成「m_t 不可预测」。
符号随机说的是分区级长期漂移没有方向，不等于每个 time_id 的 m_t 不可预测。
实测 m_t 弱可预测：样本外 R²≈0.0018（8/10 折为正），而它占 target 方差 72%，
两者相乘 → 当前 85% 的分数来自这一块
（证据 outputs/experiments/mt_predictability.md、ab_meandev.md）

## 当前状态

- 公榜 **0.0032523499**（8/11 提交，480 轮 LGBM 截面块 **+ 每资产滚动历史特征**
  + phase_balanced 重训的 ridge @ scale 1.16）。较上一版 **+23.52%**。
  **两点法真值（scale 1.16 + 0.90 联立，回代残差 0）**：`A = 0.00272226`、`B = 0.00227652`、
  **峰值 0.00325527**、**最优 scale 1.1958**、**IC 0.05705**。对旧模型峰值 +23.58%、IC +11.17%。
  公榜第一 0.00520002（IC 0.07211）—— 到它的 **62.6%**，IC 到 **79.1%**。
  **交付基线 = `outputs/candidates/v3_hybrid_r480_phasebal_hist/`**（history 40 列，
  一个目录，已接线，三道门禁全过）。⚠️ `..._hist_c80/`（80 列）公榜 0.0032522460
  **完全打平**（−0.0032%）却慢 0.44 分钟 —— **归档留证，不要用**。
- ✅ **交付 scale 已结算，不必再花额度**：scale **1.16 处已达峰值的 99.910%**，
  挪到最优 1.1958 只多 +0.090% ⟹ ROADMAP 第 6 项的预注册判据提前结案。
- ⚠️ **私榜必须带 480 轮，推理 7.95 分钟甩不掉**。完整轮数阶梯（同一批模型文件）：
  160 **−10.71%** / 320 **−3.50%** / 480 基准。而**旧模型（无 history）上 320 只低 0.88%**
  ⟹ **history 让模型对轮数更敏感**；本地却说 160 更好 —— ②类量反第三次。
  「降轮数省时间」与「减 history 列数」两条退路**都已证伪**。
  压超时只剩**减种子数（3→2）**，未测，8/31 打包前实测 `predict_total` 再定。
- 上一版 0.0026330806（峰值 0.00263415 @ 最优 scale 1.1371，IC 0.05132）已被取代。
- ⚠️ **生产 `strategies/v3_hybrid/model/` 还是旧的**（`blend_weight` 0.5 /
  `num_iteration` 160 / `prediction_scale` 0.856 / ridge 是 `periodic`）。
  当前最好模型**只存在于 `outputs/candidates/`**，先测后转正 ——
  **8/31 打包私榜前必须转正，否则交出去的不是榜上那个。**
- 市场模型重建项已于 **08-10 结案**。
- **A1（每资产历史特征）08-11 一否一立**：接**岭回归**❌ 否决 —— 原 +2.5% 是固定
  `prediction_scale=0.5` 下的绝对分，换 peak 后 **−1.97%、2/5 折**（`2ΔA < ΔB`）；
  接 **LGBM 截面块** ✅ **已接线完成**：受限选列口径 **+10.10%、5/5 折**、
  去掉最好一折 +9.10%、`2ΔA > ΔB`（`outputs/experiments/history_peak_lgbm_scoped.md`）。
  ⚠️ 480 轮下的 +17.64% **别用** —— 本地 baseline 在 480 轮上被打坏（②类量反）。
  候选 = `outputs/candidates/v3_hybrid_r480_phasebal_hist/`（设计矩阵 361 列，
  生产 `model/` 未动）。三道门禁全过：一致性 1.903e-08、两后端自检通过、
  端到端 21.4 万次 0 超时 / 0 非有限值。**下一步：交 1 次公榜确认（同 scale 1.16）。**
- ⚠️ **推理耗时已实测**（4 线程）：160 轮无 history **4.93** 分钟 →
  480 轮无 history **6.64** → 480 轮 + history **7.95**。ROADMAP 原来的
  「≈4 分钟」低估 66%。公榜交 CSV 无风险；**私榜 zip 跑在主办方 4 核机上，
  7.95 分钟是要盯的数**，320 轮 / 2 种子是便宜退路。
- **A0（`responder_*`）08-11 彻底结案 ❌**：三种形态全否决 —— 换目标（47 列全扫，
  −2.80%）、线性组合（`multi`，−3.66%）、投影去噪（`projection`，把整体拖到 −6.58%，
  内层选中它那折外层 −17.67%）。⟹ **responder 作为「训练标签的任何线性变换」这条路关闭。**
  ⭐ 但**多任务表示学习没试过**（共享编码器 + 多头同时预测 target 与若干 responder，
  让 responder 当辅助监督塑造表示，不是换标签）—— LGBM 做不到，属 C 段 NN 原型。
- 🚩 **判据一律用 `peak`，绝不用任何固定 scale 下的绝对分** —— 固定低 scale 对
  B 膨胀的惩罚不到 peak 的一半，这已是同一机制**第二次**咬人（市场模型重建、A1）。
- ⚠️ **ROADMAP 的 `alpha_eff = α×var(y)/var(target)` 已作废，别再按目标方差调 alpha。**
  岭回归对目标缩放**严格等变**（`R(y/s,α)=R(y,α)/s`），peak 又对缩放不变
  ⟹ 目标量纲对 peak 零影响；按那条规则调反而引入 1.05% 失真。
  真正要小心的是**中心化**（peak 对平移不是不变的）。
- 当前交付最优 scale 为 **1.1371**。
- ⚠️ 「本地 ΔA 一律打 2.2 折」这条**已加限定**：折多少取决于改动性质 ——
  拧同一个模型的紧密度约 2.2，加一个结构不同的分量只有 1.07（见 NOTES A/B 分解一节）
- 「本地绝对分 ≈ 公榜分」这条**已撤回** —— 那次吻合很可能是巧合（见 NOTES.md）。
  **有效的是逐折配对 Δ，不是绝对分。**
- 调参先分类（ROADMAP「参数怎么调」一节）：**①纯后处理**（scale / blend 权重）
  解析求解、**0 次额度**；**②拟合紧密度**（alpha / 特征数 / 树深 / 轮数 / 学习率）
  本地量反、只能公榜判；**③结构性改动**本地 ΔA 约 1:1 迁移、本地尺子能信。
  **8/23 公榜停止更新 + 标签回补；之后没有任何公榜可打，只剩 10 次私榜文件（到 8/31），
  而且私榜看不到分数。** 所以 8/23 之前只做第③类，第②类全部攒到回补之后本地网格搜。
- ⚠️⚠️ **本地对第③类结构改动系统性低估**：相位 1.6 倍（+1.84%→+2.86%），
  history 2.3 倍（+10.10%→≥+23.47%）。而 ROADMAP 写的③类迁移率是 ~1.07。
  ⟹ **看到正的点估计就值得上公榜确认，别被本地量级吓退**。
  已查（`history_gap`）：种子数**排除**、数据量只解释一部分，换到生产口径
  **只关掉 33% 的对数差距，大头未解** —— 最可能是测试期时段效应，等 8/23 回补才能查。
- ⭐⭐ **读相对增益前先看 baseline 是不是被削弱了**：实测 `Δpeak` 与 baseline 强度
  **强烈反相关（r = −0.986）**。今天被它误导过三次（固定 scale 0.5 压扁的基准、
  480 轮打坏的基准、砍半时间跨度打坏的基准）。**大比值 ≠ 改动强，也可能是基准弱。**
- 🚩 **8/23 标签回补之前，不许碰任何②类旋钮**（列数 / 轮数 / alpha / 树深 / 学习率）。
  8/11 一天两个实证：**轮数**（160 轮公榜 −10.71%，本地却说 160 更好）、
  **history 列数**（本地梯子预测 +2.96%，公榜实测 **0.00%**）。②类攒到回补后本地网格搜。
- ⚠️ **内部效度 ≠ 迁移效度**：本地梯子有「倒 U 内部极值」只说明**这次本地测量本身干净**
  （共用基准、不吃基准弱的坑），**不构成它能迁移到公榜的理由**。c80 就是这么栽的。
- ⚠️ **改动同时动了两个臂时，`verdict()` 会给出误导性的 PASS**：它只比「臂内」
  （history vs 无 history）。换预处理/换口径这类改动会**同时改变基准臂**，
  这时必须另外拿**会上线的那个臂**做横向绝对比较。A2 的 rank 就是这么假 PASS 的
  （报 +11.58% > raw 的 +9.79%，实际 history 臂绝对 peak 低 0.73%，因为基准被削弱 2.32%）。
- **A2（截面 rank）、A3′（滞后截面偏差）、A4（条件性组合器）08-11 全部结案 ❌**。
  A1′ 的 raw 口径是对的，别改。**A 段③类已全部测完** ——
  8/23 前公榜额度只剩「冻结基线」用；新的余量要么等②类网格搜（8/23 后），
  要么来自 C 段的 NN（含 responder 多任务这条唯一没试过的用法）。
- **A5（`responder_*` 多任务）08-12 立项**，四阶段门禁：结构分析 → 样本外可预测性 →
  残差增量 → 多任务 NN（见 ROADMAP A5）。A/B/C 本地免费、8/12–8/22 做；
  **D 等 8/23 之后**，且**上传数据到 Modal 前必须由本人先确认比赛规则允许第三方云**。
  ⚠️ 先验被 A0 压低，做 A/B/C 是为了钉死这条线，不是指望它直接提分。
- ⭐ **第③类改动看本地点估计的符号与量级，不要求本地显著**（8/10 相位那次：
  本地 5 折 +1.84%、t=0.44、判据算「测不出来」，公榜实测 +2.86%）。分区只有 9 个
  → 本地检出下限卡在 +8%，等显著只会漏掉真的。⚠️ **只对第③类成立**，
  且**不豁免「去掉最好一折」** —— 点估计不稳和点估计不显著是两回事。
- ⚠️ **无权 B 代理只在结构相近的「完整预测」之间用。** 拿去估分量 `m̂` 的 B 偏了 14.2%，
  据此算出的配比 CSV 交上去比榜上还低，白花一次额度（见 NOTES）。
- `experiments/variant_submission.py` 出变体 CSV（改临时副本的 meta，不碰生产 model/）；
  `experiments/public_csv_fingerprints.py` 存历史 CSV 的指纹 + Gram 矩阵（删 CSV 前必跑）
- 提交台账见 `experiments/ledger.csv`；实验产物见 `outputs/experiments/`

## 评分

Score = 1 − Σw(y−ŷ)² / Σwy²。全零预测得 0 分。
唯一实现在 `src/metric.py`，不得另写。

## 工作方式

- 每次改动前，先说明要验证的假设是什么，等我确认。
- 改完跑本地验证，把改动前后的数字都报给我。
- 改动涉及预处理 / 推理口径时，必须跑 `scripts/check_consistency.py`。
- 不确定的事情问我，不要自己假设，不要自己补全。

## 伤疤清单

- 2026-07-23 AI 把自己的策略写进 examples/（主办方目录），导致后续误判文件归属。
- 2026-07-23 walk_forward 报告写 Accepted: True，但特征从未接入 train.py，
  报告结论与实际代码无机械联系。
- 2026-07-26 发现主办方 `README.md` 与 `examples/linear_window_strategy/train.py`
  曾被 AI 写入过（mtime 07-22，非发布包解压时间 07-01），原始发布包已丢失，
  改动内容无法核验。README 已尽量还原；linear_window train.py 保持现状，
  待重新下载发布包或 8/23 重发包后 diff 核验。
- 2026-08-10 把「无权 B 代理」从完整预测搬到分量 `m̂` 上（它只在完整预测上标定过），
  `B_m` 估低 14.2%，据此算出的配比 CSV 交上去比榜上还低 —— **白花一次公榜额度**。
  教训：在 A 场景标定过的近似，搬到 B 场景之前必须重新验证。
- 2026-08-10 `public_csv_fingerprints.py` 原本是覆盖式写档，而它存在的理由正是
  「CSV 删了还能算」—— 历史 CSV 删完之后再跑一次，14 份存档被压成 4 份，靠 git 捞回。
  已改成累积式合并（已删 CSV 的记录原样搬运，从未同时在场的两份之间那格是 `null` 不是 0）。
