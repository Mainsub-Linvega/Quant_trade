# NOTES.md — 会过期的工作笔记

（规则类内容在 CLAUDE.md；优先级在 ROADMAP.md。本文件随时改写，不保证常新。）

## 仓库结构（2026-07-26 重构后）

```
data/                    # 主办方数据，20G，只读，gitignore
docs/                    # 主办方赛题/数据说明，只读
examples/                # 主办方示例，只读
timeseries_api/          # 主办方本地 runner，只读（vendoring：入库以便 8/23 更新时 git diff）

strategies/lightgbm_baseline/   # ★ 主办方增补包（约 8/06 发布），只读原文，勿改
                                #   要重构就新开目录，原件留着 8/23 重发时 diff

src/                     # 离线专用公共库（提交包不含）
  metric.py              #   加权零均值 R² —— 全项目唯一实现
  io.py                  #   FEATURE_COLUMNS / train_files / load_time_sample
  validation.py          #   partition_folds / rolling_time_folds（含 offset）/ rolling_fold_chunk_size

strategies/v1_ridge/     # 自包含：整个目录 = 提交包内容
  features.py            #   ★ 预处理+推理唯一实现，train.py 和 main.py 都 import 它
  train.py               #   训练（可 import src/；main.py 不可以）
  main.py                #   交付推理件，只依赖 numpy + 同目录 features.py
  model/baseline_model.json

strategies/v3_hybrid/    # ★ 现在上线的：冻结的生产岭回归 + LGBM 截面分量 blend50
  features.py            #   与 v1_ridge 逐字节相同的拷贝（提交包不能跨目录 import）
  train.py               #   只训 LGBM 部分；predict_array() 是离线全量口径的参照物
  main.py                #   交付推理件；lightgbm 主路径 + numpy 兜底 + 开机对拍自检
  lgbm_numpy.py          #   ★ 纯 numpy 树遍历，只依赖 numpy，随包提交
  model/                 #   baseline_model.json + hybrid_meta.json + 3 个 lgbm_seed*.txt

⚠️ 三个策略目录里的模块都叫 main / train / features，跨目录 import 必然拿错，
   见工程坑第 11 条。

experiments/             # 离线实验脚本 + 台账
  walk_forward.py        #   训练窗口对比（分区级 3 折，绝对分口径，已被下面那个取代）
  walk_forward_rolling.py#   ★ P0 主力：time_id 滚动多折 + 配对 A/B + 噪声地板
  mt_diagnostics.py      #   m_t 共同分量占比 + 自相关衰减形状
  mt_predictability.py   #   ★ 分数拆解（择时 vs 截面）+ 直接回归 m_t 测可预测性
  mt_lagged.py           #   ★ 全分辨率流式聚合 + 滞后特征对 m_t 的增量诊断（缓存 outputs/cache/）
  phase_diagnostic.py    #   ★ 拆开训练相位与评估相位，量 sample_modulo 的混叠效应
  walk_forward_history.py#   因果历史特征验证（依赖先跑 walk_forward.py）
  history_features.py    #   因果 lag/rolling 特征构造（未接入主模型）
  ledger.csv             #   ★ 提交台账：本地分 vs 公榜分校准

scripts/
  check_consistency.py   #   ★ 断言 train 与 main 预测逐元素一致（改口径必跑）
  make_submission.py     #   只读打包 main.py+features.py+model/ → zip

outputs/                 # 生成产物，csv/zip gitignore；experiments/ 小结入库
```

## 数据事实（从 parquet/manifest 核实）

| 字段 | 训练 | 测试 | 说明 |
|---|---|---|---|
| row_id, time_id, asset_id | ✅ | ✅ | 索引；15 个 asset；分区顺序 = 时间顺序 |
| feature_000..322（323 个） | ✅ | ✅ | 唯一可用输入，匿名 |
| weight | ✅ | ❌ | 主办方给定，仅训练/验证用 |
| responder_00..46（47 个） | ✅ | ❌ | 未来构造，绝不可当输入（=泄露），只能当辅助目标 |
| target | ✅ | ❌ | 预测目标 |

- 训练 1322 万行 / 9 分区；测试 322 万行 / 3 分区。float32 + zstd。
- E_w[y²] = 1.1757，sd(y) = 1.0784。
- 推理端约束：4 核 / 12GB / 无 GPU / 无网络；超时该 time_id 置 0。

## 常用命令

```bash
# 训练（生成 strategies/v1_ridge/model/baseline_model.json）
OPENBLAS_NUM_THREADS=4 OMP_NUM_THREADS=4 .venv/bin/python strategies/v1_ridge/train.py

# 训练/推理口径一致性（改预处理后必跑）
.venv/bin/python scripts/check_consistency.py

# 时序验证
OPENBLAS_NUM_THREADS=4 OMP_NUM_THREADS=4 .venv/bin/python experiments/walk_forward.py
OPENBLAS_NUM_THREADS=4 OMP_NUM_THREADS=4 .venv/bin/python experiments/walk_forward_history.py

# 滚动配对 A/B（P0-2）—— 结论一律看每折的 Δ，不看绝对分
#   配置名在 experiments/walk_forward_rolling.py 的 CONFIGS 里，第一个是 baseline 臂
WF="OPENBLAS_NUM_THREADS=4 OMP_NUM_THREADS=4 .venv/bin/python experiments/walk_forward_rolling.py"
$WF                                                              # 单配置，复现 walk_forward_rolling.{json,md}
$WF --configs baseline,baseline --disable-fit-cache --label selfcheck   # 自检：Δ 必须恒为 0
$WF --configs baseline,feat323 --label feat323                   # 一次 A/B
$WF --configs baseline,feat323 --label feat323_offhalf --fold-offset half  # 噪声地板（P0-3）
#   噪声地板 = |mean(Δ)_off0 − mean(Δ)_offhalf|；效应没明显超过它就等于没测出来
#   同名报告已存在会直接报错退出，要覆盖得显式 --force
#   scale 是后处理旋钮 → 同一 (feature_count, alpha) 的多个 scale 臂共用一次拟合，很便宜

# 过度收缩联合网格（18 臂只要 6 次拟合，配置名由 CONFIGS 里的循环生成）
ARMS=$(.venv/bin/python -c "import sys;sys.path[:0]=['experiments','strategies/v1_ridge','.'];\
import walk_forward_rolling as w;print('baseline,'+','.join(n for n in w.CONFIGS if n.startswith('g')))")
$WF --configs "$ARMS" --label shrinkgrid

# 分数拆解 + m_t 可预测性（P2-2）
OPENBLAS_NUM_THREADS=4 OMP_NUM_THREADS=4 .venv/bin/python experiments/mt_predictability.py

# 相位诊断：训练相位 vs 评估相位拆开报分
OPENBLAS_NUM_THREADS=4 OMP_NUM_THREADS=4 .venv/bin/python experiments/phase_diagnostic.py

# 本地全量顺序推理（21.4 万次 predict 调用）→ 公榜交这个 CSV
#   v3_hybrid 实测 4.93 分钟（lightgbm 主路径）/ 5.44 分钟（numpy 兜底）/ 0 超时
.venv/bin/python timeseries_api/run_timeseries_api.py \
  --data-root data --strategy-dir strategies/v3_hybrid --output outputs/submission.csv

# 私榜提交包（zip，不是公榜用的）。包内 = 策略根目录所有 *.py（除 train.py）+ model/
.venv/bin/python scripts/make_submission.py --strategy v3_hybrid

# 训练/推理一致性（改预处理后必跑）。v3_hybrid 可以指定树后端分别验
.venv/bin/python scripts/check_consistency.py --strategy v3_hybrid --n-time-ids 500
.venv/bin/python scripts/check_consistency.py --strategy v3_hybrid --backend numpy --n-time-ids 500

# 两个树后端逐位对拍 + 防呆用例
.venv/bin/python -m unittest tests.test_lgbm_numpy -v

# 公榜两点法的折扣系数 / 交付 scale 的账
.venv/bin/python experiments/ab_decomposition.py
.venv/bin/python experiments/scale_transfer.py
```

## 当前模型参数（2026-08-09 核对过代码）

**上线的是 `v3_hybrid`** = 冻结的生产岭回归（下表）+ LightGBM 截面分量 blend50。

```
岭回归            加权 Ridge（lsqr, tol 1e-8 / max_iter 2000），400 设计列 = 200 原始 + 200 截面去均值
train_partitions  4        ← walk_forward 推荐 4，已统一
sample_modulo     5        ← 训练抽样；先别改
prediction_clip   0.5
intercept         ~-0.004  ← 置 0 在单折和滚动配对两套框架下都测不出差异
NaN 预处理        nanquantile ← 修复了 NaN→0 污染统计量的 bug
LGBM              num_leaves 63 / lr 0.03 / feature_fraction 0.7 / lambda_l2 1.0 /
                  min_data_in_leaf 9070，160 轮 × 3 种子，目标 e = y − 无权截面均值(y)
```

| | `v1_ridge/train.py` 默认值 | `baseline_model.json`（生产） |
|---|---|---|
| feature_count | 200 | 200 |
| ridge_alpha | **2e6** | **2e6** |
| prediction_scale | **1.13** | **1.13** |

⚠️ **08-08 一度把默认值改成 alpha 5e5 / scale 0.8（本地网格推荐），公榜实测峰值反而低
19.2%，当天就回滚了。** 下面那张「滚动 10 折」的对比表说的是被回滚掉的那个配置 ——
它是「本地与公榜量反」那条主线的**证据**，不是现在的配置：

| 滚动尺子 | alpha 2e6 / scale 0.6424 | alpha 5e5 / scale 0.8（已回滚） |
|---|---|---|
| 10 折 @modulo 10（仅相位 0） | 0.00152539 | 0.00175314（+14.9%） |
| 10 折 @modulo 5（相位 0+5） | 0.00083224 | 0.00102767（+23.5%） |

两个本地采样口径都单调指向 alpha=5e5（各 8/10 折同号），公榜说反话。详见「本地与公榜在
alpha 上量反了」一节。**v3_hybrid 的 `prediction_scale` 另算，见「交付 scale」一节。**

⚠️「323 特征略优」那条旧结论**已被联合网格否定**，见下面「过度收缩」一节。

⚠️ **`train.py` 打印的 `validation_score` 不能用来选配置**：它训练用
`validation_sample_modulo=10`（只有相位 0）、评估用 `sample_modulo=5`（相位 0+5），
正是「相位」一节里测出有害的那种训练/评估错配，而生产模型没有这个错配。
新配置在这个数上是 0.00067794（旧版 0.00089825），方向与两把滚动尺子相反 —— **以滚动配对 Δ 为准**。

## P1 拆分测试结果（2026-08-07 单折口径，⚠️ 已被下一节取代，保留作对照）

| 配置 | validation_score | vs 基线 | 当时的结论 |
|---|---|---|---|
| 基线（200特征, scale=0.5, 真截距） | 0.00089825 | — | — |
| 只改截距=0 | 0.00089825 | ±0 | 无影响 |
| 只改 323 特征 | 0.00089307 | -5.2e-6 | 拖后腿 |
| 只改 auto-scale | 0.00094467 | +4.6e-5 | 有效，保留 |

## 滚动配对 A/B 结果（2026-08-07/08，P0-2/P0-3 框架，10 折）

产物：`outputs/experiments/ab_*.{json,md}` —— selfcheck、scale_auto(+offhalf)、p1_stable(+offhalf)、
featsweep、alphasweep、scalesweep、meandev、f323_scale、shrinkgrid。
每个 md 自带完整配置与每折数字，结论一律以产物为准。复跑命令见上面「常用命令」。
baseline = window4 + nanquantile + 200 特征 + alpha 2e6 + scale 0.5 + 真截距。

自检（`ab_selfcheck`）：同配置两臂关缓存，Δ 逐位为 0；且各次运行的 baseline 臂都
逐位复现 `walk_forward_rolling.json`（max|Δ| = 0.000e+00）。

**尺子本身的分辨率。** 噪声地板 = 同一个 A/B 把 fold 边界平移半段后 mean(Δ) 的漂移量。
关键点：**地板不是框架的常数，它取决于你在测什么改动 —— 两臂共享的计算越多，Δ 越安静。**

| 两臂共享什么 | SE(Δ) | 噪声地板 |
|---|---:|---:|
| 一切（同配置自检） | 0（严格逐位相等） | — |
| 同一次拟合，只换后处理旋钮（intercept） | 1.24e-05 | **9.9e-06** |
| 只共享 fold 切分，各自拟合（feature_count） | 3.54e-05 | **2.16e-05** |
| 还多一个自己在抖的派生参数（a*） | 5.11e-04 | **2.2e-04** |

对照：绝对分的 SE = 2.11e-04，且均值在两套边界间漂移 1.44e-04 —— 绝对值确实没法比这个量级。
ROADMAP 想要的 6e-6 仍未达到（最好 ~1e-5）；各自拟合那类实验按 2.16e-5 的地板算。

**各改动的结论**（baseline = 200 特征 / alpha 2e6 / scale 0.5；地板按共享程度取）：

| 改动 | 证据文件 | mean(Δ) | 同号 | vs 地板 | 判定 |
|---|---|---:|---:|---:|---|
| scale 0.75 | `ab_scalesweep` | +2.73e-04 | 8/10 | +27× | ✅ 有效 |
| scale 1.00 | `ab_scalesweep` | +2.81e-04 | 8/10 | +28× | ✅ 有效 |
| scale 1.50 | `ab_scalesweep` | −4.94e-04 | 6/10 | −49× | ❌ 过头了 |
| alpha 5e5 | `ab_alphasweep` | +2.16e-04 | 8/10 | +9.8× | ✅ 有效 |
| alpha 4e6 | `ab_alphasweep` | −1.29e-04 | 8/10 | −5.9× | ❌ 更差 |
| feat323 @alpha 2e6 | `ab_featsweep` | +5.9e-05 | 7/10 | +2.7× | ⚠️ 假象，见下 |
| feat100 @alpha 2e6 | `ab_featsweep` | −1.74e-04 | 8/10 | −7.9× | ❌ |
| meandev r=10 | `ab_meandev` | −3.93e-04 | **10/10** | −17.9× | ❌ **P2-1 被否决** |
| intercept=0 | `ab_p1_stable` | +1.81e-05 | 5/10 | +1.8× | ⚪ 测不出 |
| auto_inner scale | `ab_scale_auto` | −7.6e-05 | 6/10 | −0.35× | ⚪ 测不出 |

### 过度收缩：三个旋钮修的是同一个毛病

alpha 太大、预选太狠、scale 太小 —— 这三个「发现」都是**模型被压得太扁**的不同表现，
**收益不可加，必须放在同一个网格里比**。联合网格 `ab_shrinkgrid`（18 臂 6 次拟合）：

因为分数关于 scale 精确二次（残差 ~1e-13），每个配置的最优 scale 是**解出来**的不是扫出来的：

| 特征 | alpha | 最优 scale | 该处分数 |
|---:|---:|---:|---:|
| **200** | **500,000** | **0.759** | **0.00175839** |
| 200 | 250,000 | 0.696 | 0.00175515 |
| 323 | 500,000 | 0.697 | 0.00173015 |
| 323 | 2,000,000 | 0.849 | 0.00168135 |
| 200 | 2,000,000 | 0.883 | 0.00164787 |

**教训：`feat323` 的 +5.9e-5 是 alpha=2e6 过度正则的假象。** alpha 调到 5e5 后，
323 特征反而略差于 200。单独扫某一个旋钮会得出错误结论。

### 内层估 a\* 为什么失败

每折估出来 0.34~1.78（sd=0.41），`corr(a*, Δ) = −0.50` —— 估得越大结果越差，这是估计噪声的
特征而非真实的时变最优。fold 5 估出 a\*=1.78，那折分数从 +0.00043 掉到 **−0.00387**。

但要分清两件事：**「每折重估 scale」这个流程没用 ≠「scale 该取多少」这个问题不重要**。
我当初把两者混为一谈，得出了「auto-scale 没有证据支持」的错误框架性判断。
固定 scale 扫描（共享拟合、地板 1e-5）证明 scale 影响极大。

**为什么单折那张表被取代**：单折框架连 5.1e-5 都分辨不了，表里的 −5.2e-6 / ±0 都在它自己的
分辨率以下，从来就没有测量支持；而 +4.6e-5 是「a\* 在打分的同一批数据上估」的样本内产物
（a\* 按定义就是让那批数据分数最大的值，所以那个差必然 ≥ 0，是上界不是估计）。

**限制**：本框架用 sample_modulo=10 + 4/9 窗口，生产用 modulo=5 + 4 分区。
n=10 的符号检验功效很低（10/10 才 p=0.002，7/10 只有 p=0.34），别只看 p 值。

## 本地 ↔ 公榜校准（2026-08-08）

同一个模型（200 特征 / alpha 2e6），只改 scale，两次公榜提交：

| scale | 本地滚动 10 折 | 公榜实测 | 公榜/本地 |
|---:|---:|---:|---:|
| 0.5000 | 0.00133767 | 0.00128602 | 0.961 |
| 0.6424 | 0.00152539 | 0.00151886 | **0.996** |

### ⚠️ 这个吻合很可能是巧合，别当地基（2026-08-08 降级）

我当初据此写下「本地绝对分就近似等于公榜分，后面可以只在本地定夺」——**这条撤回**。

理由：`phase_diagnostic` 显示，同一个模型在 modulo 10（只有相位 0）上得 0.00133767，
在 modulo 5（相位 0+5）上只有 0.00071851。公榜是**全相位**的，按理该更接近后者，
实际却贴着前者。更可能的解释是**公榜测试期本身更好预测**：公榜从 time_id 888,480 起，
紧邻本地最后一折（839,105–888,475），而那折恰是十折里最好的（modulo 5 下 0.00111，
十折均值只有 0.00072）。可预测性在往后变高，本地十折均值被前面那些难的时期拖低了。

**仍然有效的**：配对 Δ（那本来就是这个框架的用途）。
**不再有效的**：拿本地绝对分当公榜分的代理。

### 抛物线解法本身是对的

`Score(a) = 2aA − a²B`，不触限幅时精确成立（实测残差 ~1e-13）。
旧模型两点定出：`A = 0.00165067, B = 0.00145858` → 公榜顶点 **a\* = 1.1317，峰值 0.00186804**。

⚠️ **私榜交付用本地最优，不用公榜最优** —— 本地是 10 个时期的平均，
公榜是单一测试集的精确值，后者等于对公榜过拟合。

## 公榜两点法：分数关于 scale 是精确二次式（2026-08-08 已验证）

`Score(a) = 2aA − a²B`，其中 `A = Σw·y·raw/Σw·y²`、`B = Σw·raw²/Σw·y²` 是测试集上的常数。
不触限幅时精确成立 —— **同一模型交两个不同 scale，就能解出它的公榜最优 scale 与峰值**。
峰值 `= A²/B = IC²`，与 scale 无关，是**唯一能公平比较不同模型的量**。

**外推验证**：用 scale 0.5 与 0.6424 两点拟合，外推到区间外的 1.13，
预测 0.00186804 / 实测 **0.00186805**，差 9.3e-09。三点最小二乘残差 ~6e-10。

| 模型 | 公榜最优 scale | 峰值 | IC |
|---|---:|---:|---:|
| 200 特征 / alpha=2e6（legacy 求解器） | 1.1317 | 0.00186805 | 0.04322 |
| 200 特征 / alpha=5e5（legacy 求解器） | 0.8139 | 0.00150896 | 0.03885 |
| 严格求解器 `c23a8cfb`（200/2e6） | 1.1350 | 0.00187236 | 0.04327 |
| **v3_hybrid（严格岭回归 + LGBM 截面 blend50）** | **1.2196** | **0.00229575** | **0.04791** |

限幅检查：测试集 raw 幅度 0.354（alpha 2e6）/ 0.425（alpha 5e5），
clip(0.5) 分别自 scale 1.411 / 1.178 起生效，超过就不再是二次式。
hybrid 的 clip 自 **1.435** 起生效，两个点（0.90 / 1.30）都安全。

### ⭐「一个点 + B 的无权近似」外推 → 已被证实可用（2026-08-09）

08-08 只有严格模型一个公榜点时，用「B 按**无权**近似重算」外推它的峰值。
那条建立在「测试集权重近似均匀」上，而**测试集没有 `weight` 列，当时无从验证**。
08-09 交了第二点 scale 0.92，两点解出真值：

| | 预测（一个点 + 无权近似） | 实测（两点） | 误差 |
|---|---:|---:|---:|
| 最优 scale | 1.1359 | 1.1350 | 0.08% |
| 峰值 | 0.00187237 | 0.00187236 | **0.00%** |

**结论：这个近似可用。** 以后只有一个点时，可以靠它外推出相当准的峰值估计 ——
但它只省一次额度，**要精确比较两个模型仍然老老实实交两点**。

## 🏁 公榜裁决：v3_hybrid 峰值 0.00229575，较严格岭回归 +22.6%（2026-08-09）

两条抛物线各交两点解出来的，峰值 `A²/B` 与 scale 无关，**这是唯一能公平比较两个模型的量**。

| 模型 | A | B | 最优 scale | 峰值 | IC | 花了几次 |
|---|---:|---:|---:|---:|---:|---:|
| 严格岭回归 `c23a8cfb`（= `blend_weight` 0） | 0.00164960 | 0.00145335 | 1.1350 | 0.00187236 | 0.04327 | 2 |
| v3_hybrid（`blend_weight` 0.5） | 0.00188239 | 0.00154345 | 1.2196 | 0.00229575 | 0.04791 | 2 |
| v3_hybrid replace（`blend_weight` 1.0，160 轮） | 0.00211518 | 0.00179844 | 1.1761 | 0.00248770 | 0.04988 | **1** |
| ↑ 同上只改 `num_iteration` 160→80 | 0.00193723 | 0.00158002 | 1.2261 | 0.00237519 | 0.04874 | 1 |
| ↑ 同上 `num_iteration` 320 | 0.00226538 | 0.00199955 | 1.1329 | 0.00256656 | 0.05066 | 1 |
| ↑ 同上 `num_iteration` 480 | 0.00234971 | 0.00213228 | 1.1020 | 0.00258931 | 0.05089 | 1 |
| **↑ 再把 ridge 换成 `phase_balanced`** | 0.00231661 | 0.00203735 | 1.1371 | **0.00263415** | **0.05132** | 1 |

blend50 vs 岭回归：ΔA **+14.11%** / ΔB **+6.20%**，判据 `2·ΔA > ΔB` 通过 → 峰值 1.2261 倍。
replace vs 岭回归：**+32.87%**；replace vs blend50：**+8.36%**。
**LightGBM 截面分量这条线是真的，而且该全量替换、不是五五开。**

已交的最好成绩 **0.0026330806**（480 轮 + `phase_balanced` ridge @ scale 1.16）。
公榜第一 0.00520002（IC 0.07211）—— 我们到了它的 **50.7%**（IC 到了 71.2%）。

### 当前最好模型是怎么拼出来的（2026-08-10）

它**不是一个现成的目录**，是两个候选产物的组合 —— 生产 `strategies/*/model/` 一个字节没动
（仍是 160 轮 / `blend_weight` 0.5 / `periodic`，**先测后转正**）：

```bash
mkdir -p outputs/candidates/v3_hybrid_r480_phasebal
cp outputs/candidates/v3_hybrid_r480/{hybrid_meta.json,lgbm_seed*.txt} \
   outputs/candidates/v3_hybrid_r480_phasebal/
cp outputs/candidates/v1_ridge_phasebal/baseline_model.json \
   outputs/candidates/v3_hybrid_r480_phasebal/
```

出 CSV（**同 scale 1.16**，这样原始分能和历史点直接比大小、不依赖 B 近似）：

```bash
.venv/bin/python experiments/variant_submission.py \
  --model-dir outputs/candidates/v3_hybrid_r480_phasebal \
  --blend-weight 1.0 --num-iteration 480 --scale 1.16 \
  --output outputs/submission_phasebal_r480_s116.csv
```

`v3_hybrid_r480_phasebal/` 本身**不入库**（9.1 MB 全是重复），`.gitignore` 挡掉了；
两个源目录入库，拼装靠上面这两条 `cp`。

⚠️ 换 ridge 之所以是**干净的单变量改动**：`main.py` 只从 `baseline_model.json` 取
系数与预处理统计量，`num_iteration` / `blend_weight` / `prediction_scale`
**一律取自 `hybrid_meta.json`**（`strategies/v3_hybrid/main.py:95-97`）。

⭐ **后两行各只花了一次额度**，因为 `A` 关于 `blend_weight` 精确线性，
前两行已经把整条 `A(w)` 钉死了 —— 见下一节。

用掉的四次提交（ledger 2026-08-08/09）：strict @1.13 / strict @0.92 /
hybrid @0.90 / hybrid @1.30。四份 CSV 都 0 行触 clip → 二次式精确成立。

**期望值仍然要压住**：截面块这条线基本吃完了（它总共只占 28% 方差 / 15% 的分）。
到 0.0052 还差 2.3 倍，再往上需要新的信号来源。

## ⭐⭐ 整条 `blend_weight` 曲线是**免费**解出来的（2026-08-09）

这是本项目到目前为止杠杆最高的一个观察。**一次公榜额度都没花。**

### 推导

预测关于 `blend_weight` 是**精确线性**的：

```
f(w) = m̂ + (1−w)·ê_ridge + w·ê_lgbm = f(0) + 2w·(f(0.5) − f(0))
```

而 `A = Σw·y·f / Σw·y²` 是 f 的**线性泛函** ⟹ **`A(w)` 是 w 的一次函数**。
我们手上恰好有两个公榜精确解：`w=0` 就是严格岭回归（`m̂ + ê_ridge = ridge_raw`，
逐位相同不是近似），`w=0.5` 就是 v3_hybrid。两点定一条直线：

```
A(w) = 0.00164960 + w × 0.00046558          A(1) = 0.00211518   (ΔA = +28.22%)
```

`B = Σw·f²/Σw·y²` 是 f 的二次型 ⟹ `B(w)` 二次，三个系数用**两份现成 CSV 的
二阶矩与交叉矩**算（无权近似）。用 `w=0` 的真值标定，实测误差 **+1.60%**。

| `blend_weight` | A | B | 最优 scale | 预测峰值 | vs blend50 |
|---:|---:|---:|---:|---:|---:|
| 0.50（当时上线的） | 0.00188239 | 0.00154345 | 1.2196 | 0.00229576 | — |
| 0.75 | 0.00199879 | 0.00165647 | 1.2067 | 0.00241184 | +5.1% |
| **1.00（`replace`）** | **0.00211518** | 0.00182255 | **1.1606** | **0.00245480** | **+6.9%** |
| 1.047（解析最优） | 0.00213706 | 0.00185970 | 1.1491 | 0.00245579 | +7.0% |
| 1.25 | 0.00223158 | 0.00204169 | 1.0930 | 0.00243912 | +6.2% |

**取 w = 1.0，不取 1.047**：顶部极平（差 0.04%），而 1.047 是拿公榜量出来的东西
去拟合一个标量 —— 正是 ROADMAP §5 禁的那种；1.0 是结构性先验。
把 B 整体拨 ±1.6%（近似的误差上限），最优 w 稳定在 1.05、增益 +5.3%~+8.7%，**结论不翻**。

### 机械验证（不是纸上推导）

在 3,750 行真实测试数据上跑 `blend_weight=1.0` 的模型，对 `2·f(0.5) − f(0)`：
**`max|Δ| = 3.9e-08`** —— 而 strict 那份 CSV 是 8 位小数，舍入底噪就有 ~4e-09。
恒等式成立。

### ✅ 预测 vs 实测（2026-08-09 交了 replace，验完了）

| | 预测 | 实测 | |
|---|---:|---:|---|
| `replace` 峰值 | 0.00245479 | **0.00248770** | 预测**偏低 1.34%** |
| `B(1)` | 0.00182255 | 0.00179844 | 无权近似**高估 B 1.34%** |
| vs blend50 | +6.9% | **+8.36%** | 方向与量级都对 |

**误差全部来自无权 B 近似，而且它的偏差是稳定的**：w=0 时高估 **1.600%**、
w=1 时高估 **1.340%**。→ **以后按 −1.5% 修正，精度从 1.6% 提到约 0.3%。**
（A 那一半是精确的，不贡献误差。）

### 两条推论

1. **`replace` 只要交一次就能拿到精确峰值。** 因为 `A(1)` 是精确值，
   一个点 `(a, Score)` 直接反解 `B(1) = (2aA − Score)/a²`，不需要第二个 scale。
   **这把「每个模型 2 次额度」的规矩，在同一个线性族里降到了 1 次。** 已经用掉两次
   （replace、replace@80 轮），都只花一个点。
2. **纯后处理旋钮（scale / blend 权重 / 集成权重 / 分量配比）以后一律先算再说，别花额度。**
   见 ROADMAP「参数怎么调」的第①类。

## 轮数：公榜说**加**不说减（2026-08-09）

`num_iteration` 160 → 80（免训练，模型文件本来就有 160 棵树，只用前 80 棵）：

| | A | B | 最优 scale | 峰值 | IC |
|---|---:|---:|---:|---:|---:|
| 160 轮 | 0.00211518 | 0.00179844 | 1.1761 | 0.00248770 | 0.04988 |
| 80 轮 | 0.00193723 | 0.00158002 | 1.2261 | 0.00237519 | 0.04874 |

**80 轮比 160 轮低 4.5%**（ΔA −8.4% / ΔB −12.1%）。把 B 拨 ±1%，结论在 −4.47%~−4.57%
之间，不翻。`corr(pred80, pred160) = 0.9878`。

**→ 方向：加轮数。** 逐折 `best_iteration` 是 44~302、折均才 160，
说明那个折均是偏保守的；下次重训往 **320** 走。

### 2026-08-10：480 轮已训好，且**嵌套性已机械验证** —— 一次训练白送四个轮数

`outputs/candidates/v3_hybrid_r480/`（3 种子 × 480 轮，约 8 分钟；生产 `model/` 未动）。
`experiments/lgbm_nested_check.py` 验了两件事：

1. meta 对拍：选列、`lower/upper/center/scale`、`min_data_in_leaf`、`train_rows`、
   采样口径 —— **全部逐位一致**（有一项不同，两组树就不在同一个特征空间里，后面免谈）
2. 前 160 棵树的预测对拍：`max|Δ| = 1.7e-18` —— **纯 float64 求和顺序**
   （480 棵的模型与 160 棵的模型，predict 内部分块不同）

⚠️ 门限取 **1e-10**，和 `main.py` 开机对拍同一个常数同一个理由：翻一个分裂会跳一个
叶子值（~1e-3），浮点求和顺序是 ~1e-18，中间隔八个数量级。
**第一版判据卡「恰好 0」是错的** —— 那会把求和顺序当成模型不同。

**后果**：`num_iteration` ∈ {160, 240, 320, 480} 是同一族的嵌套模型，
`A(160)` 的公榜精确值继续有效，每个轮数只花 **1 次**额度（一点 + 无权 B 近似，修正 −1.5%）。
出 CSV：`variant_submission.py --model-dir outputs/candidates/v3_hybrid_r480`
（08-10 给它加了 `--model-dir`，推理代码仍取自 `strategies/v3_hybrid/*.py`，只换 `model/`）。

⚠️ 注意这和 alpha 那条**不矛盾也不同向**：ridge 上公榜偏好**更强**的收缩（2e6 > 5e5），
LGBM 上公榜偏好**更多**的容量（160 > 80）。**「公榜总是偏好更强收缩」不成立**，
每个旋钮都得单独量。

### 2026-08-10 结算：整条轮数曲线（4 个点，共花 3 次额度）

r320 / r480 各交一次，**都钉在 scale 1.16**。同 scale 的原始分可以直接比大小，
这个排序不依赖无权 B 近似准不准 —— B 只用来把峰值解出来。

| 轮数 | 公榜@1.16 | A | B | 最优 scale | **峰值** | IC | vs 160 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 80 | 0.00236829 | 0.00193723 | 0.00158002 | 1.2261 | 0.00237519 | 0.04874 | −4.52% |
| 160（生产） | 0.00248723 | 0.00211518 | 0.00179844 | 1.1761 | 0.00248770 | 0.04988 | — |
| 320 | 0.00256510 | 0.00226538 | 0.00199955 | 1.1329 | 0.00256656 | 0.05066 | +3.17% |
| **480** | **0.00258213** | 0.00234971 | 0.00213228 | 1.1020 | **0.00258931** | 0.05089 | **+4.08%** |

`B(k)` 一律用「无权代理比值 × `B(160)` 真值标定」：实测四份 CSV 的
`Σf_k²/Σf_160²` = 0.87855 / 1 / 1.11182 / 1.18563。
⭐ **方法在这批数上又自检了一次**：r80 那一格给出 `B(80)=0.00158002`，
与 08-09 独立算出来的值对到 **5e-6 相对误差**。
B 拨 ±1%，480 的结论落在 +4.03%~+4.14%，不翻。4 份 CSV 全部 0 行触限
（max|pred| 0.4058 < clip 0.5）→ 二次式精确成立。

**三条读数：**

1. **轮数轴基本吃完了。** 按 log2(轮数) 折算，每翻一倍的增益是
   **+4.74% → +3.17% → +1.52%**，逐次减半。外推 960 轮约 +0.9%、1920 轮再 +0.45%，
   **剩余总余量约 +1.8%**，代价是推理树数再翻倍。**不再为轮数花额度。**
2. **每一步都是 ΔB > ΔA**（+13.8/+9.2、+11.2/+7.1、+6.6/+3.7），
   最优 scale 单调下滑 1.2261 → 1.1761 → 1.1329 → 1.1020。
   这是「加容量买到的信号赶不上买到的方差」的标准形态，也是曲线为什么会饱和。
3. ⭐ **副产物：`m̂` 与 `ê` 的最优 scale 分家分得更开了。**
   `⟨m̂,ê⟩ ≡ 0` ⟹ `B_el(480) = B(480) − B_m = 0.00100408`，
   比 `B_el(160) = 0.00067024` **涨了 49.8%**，而同期总 `A` 只涨 11.1%。
   生产模型强制两块用同一个 scale，**放开这个约束是第①类、0 额度的改动**，
   值 +0.1%~+9%，完全取决于唯一未知量 `A_m` —— 见下面「`A_m` 的价值随轮数变大」。

### ❌ 已排除：把 boosting 路径重新配权（2026-08-10，0 额度）

四个轮数的预测是嵌套的 ⟹ `{f80, f160, f320, f480}` 与
`{f80, Δ1, Δ2, Δ3}` 张成同一个空间，所以「线性组合四个轮数点」= **给 boosting
的各段树重新配权**。全部二阶矩已知（无权 Gram）、`A` 也已知 ⟹ 闭式解、**0 额度**。

| 组合 | 理论峰值 | vs 单用 r480 |
|---|---:|---:|
| r320+r480 | 0.00258947 | +0.01% |
| r160+r480 | 0.00259244 | +0.12% |
| r80+r160+r480 | 0.00261037 | +0.81% |
| 四个全上 | 0.00261132 | +0.85% |

**但这是纸面数。** Gram 条件数 **3038**，系数在 ±1.5 之间反复横跳
（四分量解 `[0.847, −0.755, −0.465, 1.541]`）。把 `A` 按它的真实精度 **±0.3%**
扰动 2000 次、再拿扰动出来的系数去评真实分数：

    实际拿到 0.00259400 ± 0.00002136（5% 分位 0.00255076）
    对照单用 r480   0.00258931

**期望增益只有 +0.18%，且有相当概率比什么都不做更差。放弃。**
教训与 ROADMAP 0b 同源：**近共线的分量 + 有噪声的对齐度 = 病态最优化**，
闭式解的「理论峰值」是上界不是可实现值。

## ⭐⭐ `replace` 之后，岭回归 400 列里有 200 列完全不起作用（2026-08-09）

`replace` 是 `f = m̂ + ê_lgbm`，而 `m̂` 是岭回归预测的**截面均值**。
截面去均值那 200 列**按构造**在每个 time_id 内零均值：

```
Σᵢ devᵢⱼ = 0  ∀j  ⟹  Σᵢ (devᵢ · coef_dev) = (Σᵢ devᵢ) · coef_dev = 0
⟹ m̂ = intercept + mean(raw) · coef_raw     ——  dev 那 200 列的贡献恒为 0
```

300 个 time_id 上实测：完整 400 列算出的 `m̂` 与只用 200 原始列算出的
`max|Δ| = 4.5e-08`（相对 1.9e-07）—— **纯浮点误差**。

**后果（这是现在最该动的地方）**：

1. 岭回归**唯一的作用**就是产 `m̂`，而 `m̂` 占预测方差 **63%**
2. 但它是照「预测 y」训的 —— 特征选择、alpha、设计矩阵全是为 400 列预测 y 定的，
   不是为「预测 m」定的
3. ~~`mt_predictability` 早就量过：**专门回归 m_t 的样本外 R²_m = +0.00177，
   比现在模型隐含的 +0.00157 高 13%**。那时这是个可选优化，现在它是主线~~
   → ❌ **2026-08-10 推翻，见下一节。那 13% 是不公平比较的产物。**
4. 顺带：推理时那 200 列可以整个省掉（岭回归那半的耗时能减半）—— 这条仍然成立

## ❌ 「重建市场模型」结案：那个 13% 余量不存在（`market_model`，2026-08-10）

**结论：专门回归 m_t 打不过现在这个「顺带产出的」`m̂`，而且数据越多输得越多。
ROADMAP 0b 那个「最高优先级」的前提是假的。**

### 那 13% 是怎么来的 —— 两个臂不在同一个尺度上

`mt_predictability` 的比较是：

| | 值 | 问题 |
|---|---:|---|
| 候选（直接回归 m_t @α=1e6） | +0.001766 | 最小二乘，**自带最优缩放**（实测 a\*=0.877，本来就近最优） |
| 基准（模型隐含的 R²_m） | +0.001565 | 在 **`prediction_scale=0.5`** 下算的 —— 预测被砍半 |

**预测被人为砍半，R² 当然低。**这个比较量的是「谁的幅度碰巧对」，不是「谁更准」。

修法：改用尺度无关的 `peak = A²/B`（`A=ΣW·m·m̂/ΣW·m²`、`B=ΣW·m̂²/ΣW·m²`），
即**各臂在自己最优 scale 下**的 R²_m —— 与公榜两点法同一套 A/B 框架。

**先验证机制没错**：同一份预测按固定 scale 报出来是 **+0.001766**，
与 `mt_predictability` 报的**逐位相同**。所以差异只来自换指标，不来自换实现。

### 公平比较下的结果（10 折，sample_modulo 10，与 `mt_predictability` 同一套折）

| 臂 | peak_m | vs 基准 |
|---|---:|---:|
| **基准 `implied_u`**（生产 `m̂`，**无权**截面均值） | **+0.002474** | — |
| `implied_w`（加权截面均值，历史口径） | +0.002483 | +0.4% |
| `direct_w`（加权 X̄ 直接回归，= 原来那个候选） | +0.002416 | **−2.4%** |
| `direct_u`（无权 X̄，**可交付**） | +0.002524 | +2.0% |
| `direct_u_sel200` / `sel50`（先按 \|corr\| 预选） | +0.002407 / +0.001806 | −2.7% / −27.0% |

**基准的真实水平比原来报的高 58%**（0.001565 → 0.002474）—— 那 13% 全被这一项吃掉了。

唯一为正的 `direct_u` 也过不了判据：6/10 折为正（符号 p=0.75）、
**去掉最好的一折就翻负**、六档 α 里**只有 α=1e6 一档为正**（那还正是当初被挑出来的那一档）。

### 数据量这个杠杆是**反的**（决定性的一条）

「直接回归只用了 39,480 个 time_id，而它本可以用全部 888,315 个」—— 这条听着很硬，实测反了：

| 训练窗 | `implied_u` | `direct_u` | Δ |
|---|---:|---:|---:|
| 39,480 个 time_id | +0.002474 | +0.002524 | **+2.0%** |
| 60,000 个 time_id（+52%） | **+0.002655** | +0.002523 | **−4.9%** |

**多给 52% 的数据，直接回归纹丝不动**（0.002524 → 0.002523），Δ 从 +2.0% 翻成 −4.9%。
行级岭回归能从 15 倍的行里榨出市场信号（截面变异也在识别那 200 个 raw 列的系数），
而「先取截面均值再回归」把那部分信息**在进模型之前就扔掉了** —— 它只有 N 个 time_id
的自由度，早就饱和。**外推到全分辨率只会更差，不必再花那 20 分钟建缓存。**

⚠️ **上表里「基准涨 7.3%」那个数当天下午被推翻，别再引用**（`ridge_data_ladder`，见下节）。
两次运行的**验证段根本不同**（394,960–888,470 vs 600,200–888,470），
而 `peak_m` 逐折在 0.000189~0.007327 之间摆 —— 换一批折就能造出 ±25% 的差异。
配对之后（同一批验证段、只换训练窗）39,480 → 60,000 实测是 **−3.7%**。

**这一节的结论不受影响**：`direct_u` vs `implied_u` 的 Δ 是**同一次运行内**的配对比较，
两个臂共用验证段，翻号是真的。被推翻的只是那个跨运行的绝对值。

### 三条副产品

1. ⭐ **无权口径不吃亏**：`implied_u` +0.002474 vs `implied_w` +0.002483（差 0.4%），
   而 `direct_u` 反而比 `direct_w` **好 4.5%**。「推理端拿不到 weight」这个担心
   **在市场块上不成立**，以后不必为它设计绕路
2. **对 m_t 做特征预选是有害的**：`sel50` −27%、`sel200` −2.7%。323 列全上最好
3. **`peak` 应该成为以后比模型的默认指标**。固定 scale 下的 R²/分数会把
   「幅度碰巧对」算进模型质量里 —— 这次它造出了一个 13% 的幻觉，
   而且是写进 ROADMAP 当最高优先级的那种

### 还没测的（别当成已排除）

- **非线性市场模型**：这次只否定了「换一个**线性**目标重训」。`lgbm_mt` 两轮已经
  否定过树做 m_t，所以这条也快满了
- 全时间分辨率 + 全相位的联合效应：数据量那条反向得很干脆，先验已经很低

## ❌ 「岭回归缺数据」结案：加数据买不到东西（`ridge_data_ladder`，2026-08-10）

### 假设与它的来路

生产岭回归 `--train-partitions 4 × --sample-modulo 5` ⟹ `train_rows = 1,146,659`，
**只用了 13.2M 行的 8.7%**；而 `replace` 之后它唯一的作用就是产 `m̂`。
上一节那个「多给 52% 数据、基准涨 7.3%」看起来是个现成的杠杆。

### 怎么做成配对的（这一步做错整个实验就废了）

`rolling_time_folds` 的**验证段位置由 `train_window` 决定** —— 直接换窗口跑两遍，
两边评的不是同一批行。所以：**折按阶梯里最大的那档划定一次，小档取同一训练区间的后缀**
（`train_ids[-W:]`），各档共用**逐位相同**的验证段。
`fold_alpha = 2e6 × 窗口 / 80,000`，每行的正则强度跨档不变
（不然「加数据」会混进第②类的松紧变化）。6 折，embargo 6，sample_modulo 10。

### 结果：曲线在生产窗口那里就已经平了

| 训练窗（原始 time_id） | 训练行数 | `peak_m` | vs 生产档 | 正折 | 去掉最好一折 |
|---:|---:|---:|---:|---:|---:|
| 300,000 | 443,359 | +0.002083 | **−20.1%** | 2/6 | −0.000639 |
| **394,800（生产等效）** | 585,463 | **+0.002607** | — | — | — |
| 500,000 | 743,127 | +0.002618 | +0.4% | 4/6 | −0.000029 |
| 600,000 | 893,100 | +0.002512 | −3.7% | 2/6 | −0.000172 |
| 700,000 | 1,043,053 | +0.002557 | −1.9% | 2/6 | −0.000213 |

四条判据全部未通过。

### ⭐ 为什么这次能说「没效果」而不是「测不出来」

**因为负向档响了。** 30,000 那一档是**故意放的阴性对照**：数据砍掉 24%，
`peak_m` 掉 **20.1%**。⟹ 这把尺子对数据量是**有分辨力的**，
它只是在生产窗口以上再也读不到东西。这正是 `mt_lagged` 那次缺的东西 ——
没有阳性/阴性对照，「判据没过」就分不清是没效果还是没功效。

### 顺带推翻了上一节那个 +7.3%

`market_model`（窗 39,480）验证 394,960–888,470，`market_model_w60k`（窗 60,000）
验证 600,200–888,470 —— **两次的验证段根本不是一回事**。
而 `peak_m` 逐折在 **+0.000189 ~ +0.007327** 之间摆（同一次运行内相差 39 倍），
光换折的划分就能造出 ±25% 的差异，是那个 +7.3% 的三倍多。

**规矩**：⚠️ **跨运行比 `peak` 的绝对值是无效比较**，除非验证段逐位相同。
和「本地绝对分 ≈ 公榜分」被撤回是同一个病 —— **有效的永远是配对 Δ**。

### 剩下的路

数据量这根轴关掉了（两个方向都验过）。岭回归那边还没排除的只剩
**相位错配**（ROADMAP 第 2 项）—— 那是「换哪些行」不是「加多少行」，另一根轴。

## ⭐⭐ 三分量拆解：一次额度能解锁整个最优配比（2026-08-09）

把预测拆成三块 `f = c_m·m̂ + c_r·ê_ridge + c_l·ê_lgbm`。
关键性质：**`⟨m̂, ê⟩ ≡ 0`**（`ê` 逐 time_id 零均值，实测 1e-21）→ 二阶矩可加。

三个分量的二阶矩**已经全部已知**（`m̂` 从 replace 的 CSV 直接算，
另两个从 `public_csv_fingerprints.json` 的 Gram 矩阵反解）：

| | 值 | 说明 |
|---|---:|---|
| `B_m` | 0.00112820 | 市场分量占预测方差 **63%** |
| `B_er` | 0.00032887 | 岭回归的截面分量 |
| `B_el` | 0.00067024 | LGBM 的截面分量，幅度约是前者的两倍 |
| `corr(ê_r, ê_l)` | **0.618** | 只有六成相关 → **两个一起用应该比只用一个好** |

对账（必须成立，否则拆解是错的）：`B_m + B_er = 0.00145707` vs 公榜真值 `0.00145335`，
差 **+0.26%**（`B_m + B_el` 那条是标定用的，差 0.00%）。

**唯一缺的是 `A_m`。** 交一次「只有 `m̂`」的 CSV 即可精确解出
（`B_m` 是精确值 ⟹ `A_m = (Score + s²·B_m)/(2s)`），于是
`A_er = A(0) − A_m`、`A_el = A(1) − A_m` 全出来，三分量最优配比**有闭式解**：

```
peak_opt = aᵀ M⁻¹ a        a = (A_m, A_er, A_el)ᵀ，M = 3×3 二阶矩阵
```

⚠️ **现在的模型不在这个最优点上**：`scale·(m̂ + (1−w)ê_r + w·ê_l)` 只有 2 个自由度，
隐含约束 `c_r + c_l = c_m`。放开它属于**纯后处理（第①类）→ 免费**。
而且现在 `replace` 把 `ê_ridge` 整个丢了，但它与 `ê_lgbm` 只有 0.618 相关 ——
**丢掉的那部分里有独立信息**。

`m̂` 那份 CSV **不用跑模型**：它就是 replace CSV 的逐 time_id 截面均值。
`max|m̂| = 0.3013` → clip 从 scale 1.66 才生效。

## 🏁 ⭐⭐ 三个公榜点把最后一个近似量钉死了（2026-08-10 傍晚）

**结论先说：分量配比这条线值 +0.02%，ROADMAP 0c 结案。** 而这次花掉的两次额度
换回来一个更值钱的东西 —— **`A_m`、`B_m` 的精确值，以及无权 B 代理的失效边界。**

### 方程组怎么定死的

`replace` 系列里，`⟨m̂, ê⟩ ≡ 0` ⟹ 二阶矩可加；`A` 关于 `blend_weight` 精确线性 ⟹
**`A_m + A_el = A(480) = 0.00234971` 是精确关系**。于是两个未知量 `A_m`、`B_m`
配上两个公榜点就正好定死（`A_el`、`B_el` 由那两条恒等式跟出来）：

| 点 | `(c_m, c_l)` | 公榜分 |
|---|---|---:|
| 只有 m̂ @1.3 | (1.3, 0) | 0.0014991067 |
| 配比 mix2 | (1.1611, 1.0356) | 0.0025767425 |

| | 存档 Gram 估的 | **真值** | |
|---|---:|---:|---:|
| `B_m` | 0.00112820 | **0.00131423** | 估低 **14.2%** |
| `A_m` | 0.00130991 | **0.00143083** | 估低 8.5% |
| `A_el` | — | 0.00091888 | |
| `B_el` | — | 0.00081805 | |

前提成立：mix2 那份 `max|pred| = 0.400075`、**0 行触限** ⟹ 二次式精确。

### ⚠️⚠️ 无权 B 代理在「分量」上失效 —— 这是今天最该记住的一条

08-09 验过「一个点 + B 的无权近似」误差约 1.6%（修正 −1.5% 后到 0.3%），
我就直接把它搬到了 `m̂` 上 —— **错了，实际偏 14.2%。**

机制：`B = Σw·f²/Σw·y²`，无权代理默认「加权与无权二阶矩成比例，且比例常数对各向量相同」。
**`m̂` 在每个 time_id 内是常数**，它的加权/无权之比取决于 weight 与 time_id 级结构的相关性；
完整预测则由截面部分主导。**两者根本不是同一族向量。**

> **规矩**：无权 B 代理**只在完整预测之间**用（而且要是结构相近的完整预测）。
> 拿它去估某个**分量**的 B，误差是一位数百分比起步。
> 这与「跨运行比 `peak` 绝对值无效」「本地绝对分 ≈ 公榜分已撤回」是同一类错误：
> **把一个在 A 场景标定过的近似，搬到 B 场景。**

### 用真值重算：配比这条线是零

| | 峰值 | vs 单一 scale |
|---|---:|---:|
| 单分量 `scale·(m̂+ê)` @ scale 1.1020 | 0.00258931 | — |
| **2 分量真最优** @ `c_m`=1.0887 `c_l`=1.1233 | **0.00258991** | **+0.02%** |

市场块占分 **60.1%**（不是先验说的 85%）。

⟹ 一个 **OOF 线性 stacker 学出来的就是这组系数**，所以那条路的上限也是 +0.02%。
还没试过的只剩**条件性/非线性**的组合器（按截面离散度或 regime 调两块权重）。

### `A_m` 的价值随轮数变大（2026-08-10 上午，⚠️ **已作废**）

> 下面这一节是 08-10 上午写的，整套数**建立在估低 14.2% 的 `B_m` 上**。
> `A_m` 真值 0.00143083 落在表外（表里最大才 0.00160，且那一档的 +9.25% 是假的）。
> 保留原文只为记住「近似量搬错场景会造出多大的幻觉」。

`B_m` 只跟岭回归有关、**不随轮数变**；而 `B_el` 随轮数猛涨：
`B_el(160) = 0.00067024 → B_el(480) = 0.00100408`（**+49.8%**），
同期总 `A` 只从 0.00211518 涨到 0.00234971（+11.1%）。
两块分量的最优 scale `A_m/B_m` 与 `A_el/B_el` 因此**分家分得更开**，
而生产模型强制它们相等 —— 于是「放开单一 scale」这个免费改动值多少，
随轮数单调变大。480 轮上（`B_el = 0.00100408`，`A(480) = 0.00234971`）：

| `A_m` 假设 | 市场块占分 | `A_el` | 2 分量解耦峰值 | **免费增益** | 最优 `c_m` | 最优 `c_l` |
|---:|---:|---:|---:|---:|---:|---:|
| 0.00120 | 49.2% | 0.00115 | 0.00259283 | +0.14% | 1.064 | 1.066 |
| 0.00130 | 57.7% | 0.00105 | 0.00259537 | +0.23% | 1.152 | 1.080 |
| 0.00140 | 65.9% | 0.00095 | 0.00263556 | **+1.79%** | 1.241 | 1.094 |
| 0.00150 | 73.5% | 0.00085 | 0.00271340 | **+4.79%** | 1.330 | 1.107 |
| 0.00160 | 80.2% | 0.00075 | 0.00282888 | **+9.25%** | 1.418 | 1.121 |

（曲线在 `A_m ≈ 0.00125` 处触底 —— 那正是现在的单一 scale 恰好最优的点。
往两边走，强行同 scale 的损失都在变大。）

⚠️ **2 分量（`m̂`, `ê_lgbm`）稳，3 分量（再加 `ê_ridge`）不稳。**
2 分量只用到 `B_m` 与 `B_el = B(480) − B_m`，系数在整个先验区间里都很温和
（`c_m` 1.06~1.42、`c_l` 1.07~1.12）。3 分量则有两处麻烦：

1. `B_er` 两个来源对不上：`B(0) − B_m = 0.00032515` vs Gram 反解的 0.00032887，差 **1.13%**
2. 最优 `c_r` 在先验区间里从 **+0.225 一路走到 −1.065、中途变号**
   （因为 `A_er = A(0) − A_m` 随 `A_m` 变小甚至变负，而 `corr(ê_r,ê_l)=0.618`
   让求解器改用「减掉 `ê_r`」来净化 `ê_l`）

⟹ 3 分量的结论只有在 `A_m` 落定、且 `c_r` 对 `B_er` ±1.5% 不变号时才可信。
这与「boosting 路径重配权」被否掉是同一个病：**近共线分量 + 有噪声的 `A`**。

## 交付 scale：一处口径错误，纠正后结论翻了（`scale_transfer`，2026-08-09）

### 前提（别搬到不成立的地方）

- `w=0` 必须**恰好**是另一个已测模型。这里成立是因为 hybrid 冻结复用了同一个岭回归；
  换一个 ridge 就不成立
- 无权近似的 1.6% 误差只在 B 上；A 是精确的。所以峰值误差也是 ~1.6%
- 触限幅之后二次式失效 —— 上表所有 w 在各自最优 scale 下 `max|pred|` 都 ≤ 0.42 < 0.5

## 交付 scale：一处口径错误，纠正后结论翻了（`scale_transfer`，2026-08-09）

比较「本地最优 scale」和「公榜最优 scale」时，**两边必须是同一个模型**。
08-09 我一度拿岭回归的本地最优 **0.759** 对公榜的 1.1350，算出比值 1.50 ——
但 0.759 是 **alpha=5e5** 那个配置的最优 scale，生产模型是 alpha=2e6。
**小 alpha 抬高 B、压低 `a*=A/B`**，那个比值里混进了「换模型」的成分。

生产 alpha 下五把尺子给 0.865~0.973（均值 **0.912**），比值是 **1.244**。
hybrid 走同一次实验里的臂间比换算（1.120，公榜对应比 1.075，差 4% 互相印证）
得本地最优 ≈ **1.022**，公榜 1.2196 —— **只差 1.19 倍**。

结论因此从「差 1.42 倍，要好好想」变成：

| 取值 | 最坏能拿到峰值的 |
|---|---:|
| 1.022（本地最优）/ 1.112（调和平均）/ 1.220（公榜最优） | 96~99% |
| **0.856（`hybrid_meta.json` 现值）** | **91.1%** |

**真正该动的是那个 0.856**（训练脚本里的本地占位值），不是「本地还是公榜」之争。
判据与保留项见 ROADMAP 第 6 项与 `outputs/experiments/scale_transfer.md`。

**教训**：`a* = A/B` 同时被「模型有多准」和「预测幅度多大」决定，
拿两个不同正则强度的模型比 a*，比的是后者不是前者。

## ⚠️ 本地与公榜在 alpha 上量反了（未解决）

上表说 alpha 2e6 的峰值比 5e5 高 **23.8%**。而三把本地尺子全说反话：

| 尺子 | 2e6 / 5e5 峰值比 | 产物 |
|---|---:|---|
| 公榜 | **1.238** | `experiments/ledger.csv` |
| 相位隔离（训练相位 0+5，验证相位 1+6） | 0.935 | `ab_phase_gen_alpha` |
| modulo 10（只评训练相位） | 0.870 | `ab_shrinkgrid` |

### 两个假设都试过，都不成立

**假设 1：本地只在模型训练过的相位上评估。** ❌
造了 `--holdout-phase`，训练相位 0+5、验证相位 1+6（模型完全没见过），
方向没变（`ab_phase_gen_alpha`，2e6/5e5 = 0.935）。

**假设 2：本地外推距离太短。** ❌
公榜要往训练期后外推 214,538 个 time_id，而本地每折验证段只有 49,345 个。
把验证段拉长 3.3 倍重测：

| 验证段跨度 | 占公榜 | 最优 alpha | 2e6/5e5 峰值比 | 产物 |
|---:|---:|---:|---:|---|
| 49,345 | 23% | 500,000 | 0.908 | `ab_shrinkgrid_m5` |
| 164,490 | 77% | 500,000 | 0.914 | `ab_horizon_f3` |
| **246,740** | **115%** | 500,000 | 0.929 | `ab_horizon_f2` |
| 214,538（公榜） | 100% | ≥ 2e6 | **1.238** | `ledger.csv` |

**决定性的是第三行**：验证段拉到比公榜还长 15%，本地依然说小 alpha 赢。
所以差异与「预测多远」无关。本地三点的斜率是每增 1 万 time_id 动 +0.0010，
要够到 1.238 还需再加 296 万个 time_id —— 现有全部训练数据的 3.3 倍，不可能。

**剩下的候选**：公榜测试期本身的分布/regime 与全部训练期数据不同，
而更强的收缩对分布漂移更稳健。**这个在本地测不了** —— 没有那段时期的标签。

### 什么时候能解决：2026-08-23

主办方文档：「**公榜截止 & 标签回补 8月23日**：公榜停止更新，
发布扩展训练数据供最终模型训练与本地验证使用」。
拿到公榜测试期的标签后，就能在本地直接复现公榜评分，这个矛盾会被一次性诊断清楚。

### 在那之前的工作规矩

**凡是影响「拟合紧密度」的参数 —— alpha、特征数、模型复杂度、树深、轮数、学习率 ——
本地结论一律不可信，改用公榜两点法比峰值。**

预算够：公榜到 8/23 还有约 75 次（5/天），每个模型 2 次拿到精确峰值 → 约 35 组配置。

**不受影响、仍可在本地定夺的**：纯后处理旋钮（scale、clip）、
结构性诊断（分数拆解、相位效应、m_t 可预测性）。

⚠️ 分析脚本里判「趋势上移」用 `<=` 会把「没动」判成「上移」，已在结论里纠正。
自动判据要用严格不等号。

## 相位：`time_id % 10`（`phase_diagnostic`，2026-08-08）

`sample_modulo` 不是中性抽样。modulo 10 只取 `time_id % 10 == 0`（相位 0），
modulo 5 取相位 0 和 5。把训练相位和评估相位拆开：

| 场景 | 训练相位 | 评估相位 | 分数 |
|---|---|---|---:|
| A | 0 | 0 | 0.00133767 |
| C | 0+5 | 只看相位 0 | **0.00097096** |
| B | 0+5 | 0+5 | 0.00071851 |

**两个效应都成立**：

1. **相位 5 的行难预测 2.1 倍**（同一模型：相位 0 得 0.00097096，相位 5 只有 0.00045755）
2. **训练里混进相位 5，模型在相位 0 上也掉 27%**（A→C）。多给一倍数据反而变差，
   说明不同相位的「特征→target」关系不一样，混在一起拟合会互相稀释

旁证：p008 上按相位统计，`mean_w(y)` 是个平滑的周期-10 循环
（相位 0 = −0.028 → 相位 4 = +0.005 → 相位 9 = −0.030），而 `E_w[y²]` 只差 ±2%。
`time_id` 大概率编码了某种周期为 10 的结构。

**由此暴露的口径错配**：测试集是连续 time_id（**全部 10 个相位**），
而生产模型 `sample_modulo=5` **只在 2 个相位上训练**。

### 2026-08-10：`phase_balanced` 加到 5 折 → 仍然测不出来，而且**本地永远测不出来**

`ridge_phase_sampling.py --validation-partitions 4 5 6 7 8`（同预算、验证段全相位、
5 折，最多就这么多 —— `--train-partitions 4` 顶着分区 0）：

| | periodic | phase_balanced | Δ |
|---|---:|---:|---:|
| peak 均值 | 0.00095686 | 0.00097451 | **+1.84%** |
| 正折 | | | 4/5（符号检验 p = 0.188） |
| 去掉最好一折 | | | **−0.39%，翻负** |
| 配对 t | | | **0.44** |

**⭐ 关键不是「没过判据」，是这个设计的检出下限**：逐折 Δ 的标准差是 9.06e-05
（≈ 均值 peak 的 9.5%），5 折下要 t≥2 得有 **+8.5%** 的效应才看得见。
点估计只有 +1.8% ⟹ **要把它测出来需要约 105 折**。分区就 9 个，做不到。

旁证：折数从 3 加到 5，点估计从 **−0.01% 跳到 +1.84%** —— 估计量本身在这个 n 下不稳。

⟹ **这是个公榜问题，不是本地问题。** 相位采样是第③类改动、要重训 ridge，
一次额度 + 无权 B 代理就能定到 0.3% 精度，比再堆本地折强得多。

（前一次 3 折的结论「没测出来」保持不变，只是现在知道了**为什么**测不出来。）

### ✅ 公榜裁决：`phase_balanced` 赢了，+1.97%（2026-08-10 当晚，新最好成绩）

候选 = **只换 ridge**：`--sampling phase_balanced` 重训，其余与生产逐项相同
（最后 4 个分区 / `sample_modulo` 5 / alpha 2e6 / 200 特征 / `ridge_n_iter` 都是 35 /
`train_rows` 1,146,653 vs 1,146,659）；LGBM 截面块三个文件与 `v3_hybrid_r480` **逐位相同**（sha 对拍）。

| | A | B | 最优 scale | 峰值 | IC |
|---|---:|---:|---:|---:|---:|
| r480（生产 ridge） | 0.00234971 | 0.00213228 | 1.1020 | 0.00258931 | 0.05089 |
| **r480 + phase_balanced** | 0.00231661 | 0.00203735 | 1.1371 | **0.00263415** | **0.05132** |

**同 scale 1.16 原始分 0.0025821304 → 0.0026330806（+1.97%）—— 这个比较不依赖任何近似。**
峰值 +1.73%，B 拨 ±1% 落在 +1.71%~+1.75%，不翻。0 行触限（max|pred| 0.360777）。
`corr(f480, f_pb) = 0.985`。

因为 `ê_lgbm` **逐位相同**，市场块能直接剥出来（`A_el`、`B_el` 不变）：

    peak_m  0.00155777 → 0.00160227   +2.86%   ← 公榜实测的相位效应

### ⭐⭐ 由此得到一条新的工作规则：③类改动**看点估计，不等显著**

本地 5 折说 **+1.84%**，但 `t=0.44`、去掉最好一折翻负、符号检验 p=0.188、检出下限 +8.5% ——
**按四条判据这是「测不出来」，如果照判据办事就不会去交这一次。**
公榜实测 **+2.86%**：**方向对、量级对（1.6 倍）。**

这与 `ab_decomposition` 测出的「结构性改动本地 ΔA 约 1:1（比值 1.07）」一致，
是第二个独立证据。

> **规矩**：第③类（结构性）改动，看**本地配对 Δ 的点估计的符号与量级**就可以上公榜确认，
> **不要求本地显著**。理由是硬的：本地折数有上限（分区只有 9 个），
> 检出下限就卡在 +8% 量级，而真实效应普遍在 +2~3% —— **等显著等不到，只会漏掉真的。**
> ⚠️ 这条**只对第③类成立**。第②类（拟合紧密度）本地连符号都是反的，见 alpha 那节。

## P2-2：分数是从哪来的（`mt_predictability`）

`Score = share_m·R²_m + share_e·R²_e`（m = 逐 time_id 加权截面均值，e = y − m）。
恒等式残差 **5.89e-16**，说明拆解是精确的。

| | 均值 | 占总分 |
|---|---:|---:|
| share_market（择时块占方差） | 0.721 | — |
| R²_择时 | 0.001565 | — |
| R²_截面 | 0.000713 | — |
| **择时贡献** | **0.001138** | **85%** |
| **截面贡献** | **0.000200** | 15% |

三条结论：

1. **那 72% 的共同分量不但吃得到，它就是主菜** —— 85% 的分来自择时。
   `ab_meandev` 从反面独立佐证：压制择时分量，10/10 折掉分。
2. **这个可预测性是结构性的**：单变量 IC 在训练段与验证段之间的相关性 **+0.720**。
3. **但线性择时已近饱和**：专门回归 m_t 的样本外 R²=+0.00177（最优 alpha=1e6，8/10 折为正），
   比模型现在隐含的 0.00157 只高 13%，换算总分 0.001338 → 0.001473。
   要继续吃这块，只剩「加滞后输入」和「上非线性」两条路。
4. **被浪费的是截面块**：占 28% 方差却只出 15% 的分，R²_截面 只有 R²_择时 的一半。

## 滞后特征对 m_t 无增量预测力（`mt_lagged`，2026-08-08）

在**全时间分辨率**（888,315 个 time_id）上，用特征截面均值的滞后去预测 m_t：

| 臂 | 列数 | 峰值 R²_m | ΔR²_m | 逐折 Δ 为正 | 去掉最好的一折 |
|---|---:|---:|---:|---:|---:|
| `now`（仅当期） | 323 | +0.00085 | — | — | — |
| `lag1` | 646 | +0.00091 | +0.00005 | 4/10 | −0.000069 |
| `lag3` | 1292 | +0.00090 | +0.00004 | 3/10 | −0.000056 |
| `roll6` | 646 | +0.00088 | +0.00002 | 5/10 | −0.000002 |

七档 alpha（1e5~1e11）整条曲线都没上移，四个臂在 α=1e8 同时见顶且几乎重合。
表面增量全部在去掉单个最好折后翻负 → **假设证伪**。

**为什么必须全分辨率**：`mt_predictability` 用 sample_modulo=10，那里的「lag 1」
是真实的 lag 10，而 m_t 自相关到 lag 5~6 就归零 —— 在那个口径下做滞后诊断必然假阴性。

**教训**：m_t 的自相关（ac1=0.836）≠ 滞后特征能预测它。衰减形状贴合 MA(6) 窗口重叠理论
而非指数长尾，说明它主要来自 **target 的构造方式**（前瞻窗口重叠），是标签的性质，
不是可利用的信息。**择时块（85% 的分）的线性路到此为止。**

## A/B 分解：本地与公榜的分歧在哪一项（`ab_decomposition`，2026-08-08）

`Score(a) = 2aA − a²B`，`A = Σw·y·f/Σw·y²`（信号对齐）、`B = Σw·f²/Σw·y²`（预测方差）。
由 `最优scale = A/B`、`峰值 = A²/B` 反解：**`A = 峰值/a*`、`B = 峰值/a*²`** ——
所以任何已有结果都能拆出 A、B，不用重跑。

alpha 从 2e6 降到 5e5 时：

| 尺子 | ΔA | ΔB | 峰值比 |
|---|---:|---:|---:|
| **公榜** | **+12.3%** | +56.2% | 1.238 |
| modulo10 | +24.2% | +44.6% | 0.937 |
| modulo5 | +29.7% | +53.9% | 0.915 |
| 相位隔离 | +28.1% | +53.4% | 0.935 |
| 横跨 f3 | +28.5% | +50.8% | 0.914 |
| 横跨 f2 | +26.5% | +48.6% | 0.929 |

**ΔB 五把本地尺子与公榜几乎一致 —— 分歧完全在 ΔA。** 本地 ΔA 均值 / 公榜 ΔA = **2.22**。

### ⭐⭐ 2.2 折不是普适常数 —— 它取决于改动的性质（2026-08-09 修订）

08-09 拿到了第二条轴：把 LGBM 的截面分量 blend50 进来（严格岭回归 → v3_hybrid），
公榜两组两点法给出的 ΔA **与本地几乎一样，没有 2.2 倍的高估**。

| 改动性质 | 公榜 ΔA | 本地 ΔA | **ΔA 比值** | 公榜 ΔB | 本地 ΔB | ΔB 比值 |
|---|---:|---:|---:|---:|---:|---:|
| 调正则强度（alpha 2e6→5e5） | +12.3% | +27.4% | **2.22** | +56.2% | +50.3% | 0.89 |
| 加一个结构不同的模型分量（LGBM 截面块 blend50） | +14.1% | +15.1% | **1.07** | +6.2% | +2.8% | 0.45 |

**为什么会这样（一个说得通的解释，未验证）**：拧 alpha 是在**同一个假设空间里**
挪紧密度，本地那点样本外优势主要是拟合噪声，换个时期就没了；
而加一个结构不同的分量是**换了假设空间**，学到的是别的东西，迁移性天然更好。

⚠️ **两条保留项，别把这张表当定论**：

1. **换模型轴的两个基准不是同一个**。本地基准是逐折拟合的 ridge（内层选 alpha /
   phase_balanced），公榜基准是生产 ridge（alpha 2e6 / modulo 5 periodic），是近似对读。
   但 2 倍以上是量级差异，不是口径噪声能解释的。
2. **ΔB 那一列反过来了**：换模型轴上公榜 ΔB（+6.2%）比本地（+2.8%）**大**一倍多，
   而正则轴上两者接近（0.89）。「ΔB 1:1 迁移」这条只由正则轴支撑，
   在换模型轴上**只有一个点、方向还相反** —— 见下面 `replace` 那条预测为什么分叉。

### 由此得到的工作规则（2026-08-09 修订）

> 靠**增加信号对齐（A↑）**起作用的改动，本地收益要打折再信 —— **折多少看改动性质**：
> 「拧紧拧松同一个模型」（alpha / 特征数 / 树深 / 轮数）打约 **2.2 折**；
> 「加一个结构不同的分量」约 **1.1 折**（几乎不打）。
> 靠**降低预测方差（B↓）**的改动，在正则轴上大致 1:1 迁移；换模型轴上未定。

判据算术：`峰值 = A²/B`，所以峰值涨 ⟺ **`2·ΔA > ΔB`**。折扣时只折 ΔA。

⚠️ 每条轴目前都只有**一个**公榜观测点，是量级参考不是精确系数。每多一次公榜两点测量
都应回填 `experiments/ab_decomposition.py` 的 `PUBLIC_POINTS` / `PUBLIC_MODELS` 重算。

### 这条修订立刻翻了一个案：`replace` 比 `blend50` 好

`lgbm_blend` 当初按 2.2 折判 `blend50`（折后 +11.1%）优于 `replace`（+9.8%）。
但 blend50 的公榜实测是 **+22.6%**（原始 +28.9%、折后 +11.1%）—— **折扣过严**，
那个排序的唯一依据没了。而本地原始分一直说 `replace` 赢：
`replace_xs_loose` 本地峰值 **+42.2%**、t=5.12、**5/5 折为正**、去掉最好一折 t=4.57。

**不用猜 ΔB 怎么迁移，也不用花额度 —— 整条 `blend_weight` 曲线能直接解出来，见下一节。**

## 🏁 A1 / A0 结算（`history_peak` / `responder_targets`，2026-08-11）

### 🚩 A1：同一批拟合，两把尺子给出**相反符号** —— 本节最值钱的东西

`walk_forward_history`（07-23）报 baseline 0.00080097 → history 0.00082130
（**+2.54%**、3/3 折、结论栏 `Accepted: True`），躺了 18 天没接线。
**它是在固定 `prediction_scale=0.5` 下算的绝对分**（`walk_forward_history.py:37,124`），
不是尺度无关的 peak。在当前口径下重测（5 折滚动配对、严格求解器、同一批选中列）：

| 口径 | 配对 Δ 均值 | 相对 | 正折 | 去掉最好一折 |
|---|---:|---:|---:|---:|
| 固定 scale 0.5（07-23 那把尺子） | +9.254e-06 | **+0.71%** | 3/5 | −0.89% |
| **peak（尺度无关）** | −3.250e-05 | **−1.97%** | 2/5 | −4.37% |

**同一批拟合、同一批折、同一批系数，只换尺子就换符号。**
机制：`ΔA +2.21%` vs `ΔB +6.65%` ⟹ `2ΔA < ΔB` ⟹ peak 必跌。
而在 `Score(a)=2aA−a²B` 上固定 `a=0.5` 时，分数对 B 的相对惩罚只有
`a²B/(2aA−a²B) ≈ 0.22`，peak 的对应量是 `0.5` —— **不到一半**。
history 臂恰恰把设计矩阵从 400 列加到 560 列，典型的 B 膨胀，于是被系统性高估。

⚠️ **这与「重建市场模型 13% 余量」是同一个机制，第二次咬人**（那次是「基准被
`prediction_scale=0.5` 压扁」）。已在 ROADMAP「用尺子的规矩」里立成硬规矩：
**判据一律用 peak，绝不用任何固定 scale 下的绝对分。**

**A1 在岭回归上结案否决。** 那条 alpha 阶梯还顺带把②类纠缠照了出来：

| alpha | 1e4 | 1e5 | 1e6 | 1e7 | 1e8 | 1e9 |
|---|---:|---:|---:|---:|---:|---:|
| Δpeak | −9.49% | −4.71% | −1.92% | −1.06% | +1.29% | +68.59% |

单调上升 —— history 块只在**极端收缩**下才「有用」，那是它替代了正则，不是它带来了信号。

### ✅ 但 LGBM 截面块过了判据（原实验从没测过这一块）

| 配置 | Δpeak | 正折 | 去掉最好一折 | ΔA | ΔB | `2ΔA>ΔB` |
|---|---:|---:|---:|---:|---:|---|
| 160 轮，选列按 target | +7.43% | 5/5 | +6.05% | +5.36% | +3.50% | ✅ |
| **160 轮，选列按 `e`（生产口径）** | **+8.38%** | **5/5** | **+7.73%** | +6.17% | +3.74% | ✅ |
| 480 轮，选列按 `e` | +17.64% | 5/5 | +14.35% | +12.43% | +7.32% | ✅ |

`2ΔA > ΔB` 成立 ⟹ **是加信号不是加方差**，与 ridge 臂正好相反
（树能在 160 列里挑，岭回归必须给每列一个系数、为全部 160 列付方差）。

⚠️ **480 轮那个 +17.64% 不能当点估计**：本地 baseline 从 160 轮的 0.001775
掉到 480 轮的 0.001625，即**本地认为 480 轮更差**，而公榜说 480 比 160 好（+4.1%）——
正是②类旋钮本地量反。那 +17.64% 有一半是「history 救了一个本地过拟合的 baseline」。
**点估计取 +8.38%。** 另一个副产物：history 臂的绝对 peak 在 160/480 轮几乎不动
（0.001924 / 0.001912），**history 让模型对轮数这个旋钮变得不敏感**。

### ❌ A0 结案：47 列 responder 换训练目标，不支持

阶段 1（47 列全扫、单折、冻结选列 + 共用 Gram，只剪枝）：
`responder_04` **+15.80%**、`responder_28` +11.34%、`responder_05` +7.68%、
`responder_29` +5.57%；而**与 target 相关性最高的 `responder_03`（0.82）是 −15.47%**。
⭐「不看相关性」这条被硬证实。

阶段 2（短名单 5 个 + target，外层 5 折，候选只在内层选）：**四条判据全不过**。
`inner_selected` **−2.80%、1/5 折**、去掉最好一折 −6.30%、整条阶梯 7 档全负；
`multi` −3.66%。⭐ **两层折确实拦住了选择偏置**：`responder_04` 筛选折 +15.80%，
外层被选中那折 **−7.55%**；5 折里 2 折内层自己选回了 `target`。

### ⚠️⚠️ 更正：ROADMAP 的 `alpha_eff` 规则是错的

原文（ROADMAP A0 专条 / 8-11 计划书 §4.4）：「目标缩放 c 倍、等效正则要按 c² 缩放才不变
⟹ 给 std 0.29 的 `responder_04` 的正则强了约 14 倍」。**不成立。**
正确的等变律是 `R(y/s, α) = R(y, α)/s`（**不是** `R(y, α·s²)/s`）——
`J̃(b)=Σw(y/s−Xb)²+α‖b‖²` 令 `b=u/s` 得 `(1/s²)·[Σw(y−Xu)²+α‖u‖²]`，
**数据项与惩罚项同步缩放 `s²`**，α 原封不动。
而 peak 对预测缩放不变 ⟹ **目标的量纲对 peak 零影响**。

实测：合成数据 **1.4e-15**、真实数据 **4.19e-15**（机器精度）；
按 `alpha_eff` 调 α 反而**引入** 1.05% 的 peak 失真、系数偏 **65%**。
⟹ **不许再按目标方差调 alpha**；8/10 粗筛的「缺陷 1」根本不存在。
真正要小心的是**中心化**（peak 对平移不是不变的，[0,1] responder 的 0.5 偏移会炸 B）。
验证在 `experiments/responder_targets.py:check_solver`（`--check-solver`）。

### 🏁🏁 A1′ 公榜裁决：0.0032523499（08-11，项目至今最大一次跳变）

同 scale 1.16，对上一版 0.0026330806 = **+23.52%**（原始分直接比大小，不依赖任何近似）。

⭐ **一个点就能拿到的严格下界**：`Score(a) ≤ peak` 恒成立，而本点是在 **scale 1.16** 处测的
⟹ **peak ≥ 0.0032523499**、**峰值增幅 ≥ +23.47%**、**IC ≥ 0.05703（+11.1%）**。
且旧模型在 1.16 处已达自身峰值的 **99.959%**（最优 1.1371）
⟹ **这次不是「scale 碰巧对齐」捡来的，是真涨**。

### 🏁 两点法真值（补交 scale 0.90 那点后，08-11 当天解出）

第二点 `scale 0.90 → 0.0030560810`（同一模型，只改 `prediction_scale`）。
联立 `S = 2aA − a²B`：

| | 值 |
|---|---:|
| `A` | **0.00272226** |
| `B` | **0.00227652** |
| **峰值** | **0.00325527** |
| **最优 scale** | **1.1958** |
| **IC** | **0.05705** |

回代残差 **0 / −4.3e-19**，自洽。对旧模型峰值 **+23.58%**、IC **+11.17%**
（下界 +23.47% 收得很紧，说明 1.16 几乎就在峰上）。

⭐⭐ **交付 scale 就此结算**：scale 1.16 处已达峰值的 **99.910%**，
挪到最优 1.1958 只多 **+0.090%** ⟹ **不必再为 scale 花任何额度**，
ROADMAP 第 6 项那个预注册判据可以提前结案。

⚠️ **顺带重新标定「无权 B 代理」**：交之前用它预测过一次，事后对账 ——

| | 代理预测 | 真值 | 偏差 |
|---|---:|---:|---:|
| `B` | 0.00209845 | 0.00227652 | **−7.82%** |
| `A` | 0.00261898 | 0.00272226 | −3.79% |
| 峰值 | 0.00326862 | 0.00325527 | +0.41% |
| 最优 scale | 1.2481 | 1.1958 | **+4.37%** |

旧族上它号称 0.3%，在 history 这个新结构上 `B` 偏了 7.8%。
**峰值只偏 0.41% 是因为 A、B 的误差同向抵消，别把这当成它准。**
⟹ CLAUDE.md 那条「在 A 场景标定过的近似，搬到 B 场景之前必须重新验证」再次成立。

### ⚠️ 160 轮：②类本地量反的又一个硬证据

同一批模型文件（480 轮模型的前 160 棵树 == 160 轮模型，`lgbm_nested_check` 验过 1.7e-18），
同 scale 1.16 → **0.0029039065**，比 480 轮**低 10.71%**（480 比它高 +12.00%）。

而**本地说反话**：history 臂的 peak 在 160 轮是 0.00192421、480 轮是 0.00191184，
本地认为 160 轮**更好**（还更省推理）。公榜说 480 轮明显更好。

⟹ **原先「history 让模型对轮数不敏感 ⟹ 降到 160 轮省 1.7 分钟推理」这条退路被证伪。**

**320 轮也交了，同样不行。** 完整的轮数阶梯（同一批模型文件，只改 `num_iteration`）：

| 轮数 | 公榜 @1.16 | 对 480 轮 | 推理估算 |
|--:|---:|---:|---:|
| 160 | 0.0029039065 | **−10.71%** | ≈6.24 分钟 |
| 320 | 0.0031385676 | **−3.50%** | ≈7.09 分钟 |
| **480** | **0.0032523499** | — | **7.95 分钟**（实测） |

⚠️⚠️ **关键对比：旧模型（无 history）上 320 只比 480 低 0.88%，现在是 3.50%**
⟹ **history 让模型对轮数更敏感，不是更不敏感** —— 与本地的说法（160 轮 peak 0.00192421
≥ 480 轮 0.00191184，本地认为 160 更好）**方向相反**。②类本地量反的第三个实证。

⟹ **私榜必须带 480 轮，7.95 分钟的推理包袱甩不掉。**
ROADMAP 里「320 只比 480 低 0.9%，是便宜的退路」那句**对现模型已作废**（那是无 history 时的数）。
压超时只剩一个**未测**杠杆：**减种子数 3→2**（线性省 1/3 树遍历，但会掉分），8/31 打包前要实测。

| | 旧（480 轮 + phasebal ridge） | 新（+ history） |
|---|---:|---:|
| 公榜原始分 @1.16 | 0.0026330806 | **0.0032523499** |
| 峰值 | 0.00263415 | **≥ 0.0032523499** |
| IC | 0.05132 | **≥ 0.05703** |
| 占公榜第一（0.00520002） | 50.7% | **62.5%** |
| IC 占第一（0.07211） | 71.2% | **79.1%** |
| 到 0.0045 还需 IC | +30.8% | **+17.6%** |

⚠️ **最优 scale 未知** —— 只有一个点，解不出 A、B。8/31 私榜交付要用它，得再交一点。

### ⚠️⚠️ 本地把这次低估了 2.3 倍 —— 未解，且很重要

本地 5 折 peak 点估计 **+10.10%**，公榜至少 **+23.47%**。
ROADMAP 记的③类迁移率是 **~1.07（约 1:1）**，这次差了一个数量级级别的倍数。
相位那次也低估（本地 +1.84% → 公榜 +2.86%，**1.6 倍**）。

⟹ **「本地③类 ≈ 公榜」这条在结构性改动上似乎系统性偏保守。**

**已查（`history_gap`，四个口径臂 × 3 折）—— 三条假设都没解释掉大头：**

| 臂 | m | 采样 | 种子 | 训练窗 | baseline peak | Δpeak |
|---|--:|---|--:|--:|--:|--:|
| A 基准口径 | 10 | periodic | 1 | 39,480 | 0.00171327 | **+9.79%** |
| B +3 种子 | 10 | periodic | 3 | 39,480 | 0.00174926 | **+8.15%** |
| C m5/phase（⚠️ 跨度砍半） | 5 | phase_balanced | 1 | 39,480 | 0.00078002 | **+20.72%** |
| D 生产口径全套 | 5 | phase_balanced | 3 | 120,000 | 0.00127133 | **+13.08%** |

- **H3 种子：排除。** 3 种子反而把相对增益压到 +8.15% —— 种子同时也抬了 baseline。
- **H2 采样：没测成。** ⚠️ `train_window` 的单位是**采样后**的 time_id，
  m10→m5 时保持 39,480 不变 = **时间跨度砍半**（394,800 → 197,400 个原始 time_id），
  C 的 baseline 塌到 0.00078 —— 它测的是「一个被削弱的 baseline」，不是采样口径。
- **H1 数据量：只解释一部分。** D（1.8M 行 + 生产采样 + 3 种子）给 +13.08%。
- **剩余大头未解**：公榜/本地倍数从 A 的 2.40× 只降到 D 的 1.79×，
  **换到生产口径只关掉 33% 的对数差距**。最可能是**公榜测试期的时段/分布效应**，
  本地无标签测不了 —— 8/23 回补后才能查。

### ⭐⭐ 但捡到一条更普适的规律：Δpeak 与 baseline 强度强烈反相关（r = **−0.986**）

baseline 越弱，history 的相对增益看起来越大。**今天已经被它误导过三次**：

- 07-23 的 +2.5% —— baseline 被固定 `prediction_scale=0.5` 压扁
- 480 轮那次 +17.64% —— baseline 被过多轮数打坏（②类本地量反）
- C 臂 +20.72% —— baseline 被我自己砍半的时间跨度打坏

⟹ **读任何相对增益之前，先看 baseline 是不是被削弱了。**
一个大比值既可能是「改动强」，也可能是「基准弱」，这两件事必须分开看。

**操作结论**：在查清之前，对③类改动一律按「本地会低估」处理 ——
看到正的点估计就值得上公榜确认，别被本地量级吓退。

### history 窗长 × 列数联合梯子（08-11，本地 3 折）

`window=5` / `40 列`都是从 07-23 那个老实验继承来的，**项目从没搜过**。
⭐ 这里**无 history 的 baseline 与窗长/列数无关**，五个格子共用同一个基准
（peak 折均 0.00171327）⟹ 比较是干净的，**不吃「基准弱」那个坑**。

| 窗长 | 列数 | history peak | Δpeak | 去最好折 | ΔA | ΔB | `2ΔA>ΔB` |
|--:|--:|--:|--:|--:|--:|--:|:--:|
| 5 | 40 | 0.00188096 | +9.79% | +9.13% | +5.86% | +1.98% | ✅ ←现产 |
| 10 | 40 | 0.00188194 | +9.85% | +7.38% | +5.97% | +2.20% | ✅ |
| 20 | 40 | 0.00182513 | +6.53% | +5.43% | +4.39% | +2.14% | ✅ |
| **5** | **80** | **0.00193672** | **+13.04%** | **+10.72%** | +7.65% | +2.96% | ✅ |
| 10 | 80 | 0.00189323 | +10.50% | +8.83% | +6.69% | +3.28% | ✅ |

列数继续往上探（窗长锁 5）：

| 列数 | 设计列数 | history peak | Δpeak | 去最好折 |
|--:|--:|--:|--:|--:|
| 40 | 361 | 0.00188096 | +9.79% | +9.13% |
| **80** | **521** | **0.00193672** | **+13.04%** | **+10.72%** ← 顶 |
| 120 | 681 | 0.00190212 | +11.02% | +8.43% |
| 160 | 841 | 0.00187535 | +9.46% | +7.84% |

**两条清楚的结论**：

- ⭐ **列数有一个干净的内部极值：80。** 40→80 上升、80→120→160 单调回落，
  是个倒 U 而不是单调趋势 —— **比单调更可信，不用外推**。
- ❌ **加窗长没用，甚至有害**：5→10 几乎不动（+9.79% → +9.85%），5→20 掉到 +6.53%。
  ⟹ **有用的记忆很短（≈5 期）；广度（多少列带历史）比深度（回看多少期）值钱。**

八个格子 `2ΔA > ΔB` 全部成立，机制健康。⚠️ 列数是②类容量方向、本地量级不可信，
但**基准相同 + 有内部极值 + 机制一致** ⟹ 拿 `(5,80)` 重训上榜。

**`(5,80)` 候选已就位**：`outputs/candidates/v3_hybrid_r480_phasebal_hist_c80/`，
设计矩阵 2,645,530 × **521**，`train_rows` 与生产逐位相同。三道门禁全过：

| 门禁 | c40 | **c80** |
|---|---|---|
| `check_consistency`（两后端） | 1.903e-08 | **1.903e-08** |
| lightgbm ↔ numpy 开机自检 | 通过 | **通过** |
| 端到端 21.4 万次 | 0 超时 / 0 非有限 | **0 超时 / 0 非有限** |
| `predict_total` | 7.95 分钟 | **8.39 分钟** |

⭐ **列数翻倍只多 0.44 分钟** —— 树遍历随棵数走、几乎与列数无关，
涨的只是每次调用里算 history 那部分。⟹ 列数不是推理耗时的主要矛盾，**轮数才是**。

### ❌ 但 c80 交上去完全打平 —— 本地这条梯子一点没迁移

| | c40 | c80 |
|---|---:|---:|
| 公榜 @ scale 1.16 | **0.0032523499** | 0.0032522460 |
| 相对 | — | **−0.0032%**（绝对差 −1.04e-07） |
| 本地 history peak | 0.00188096 | 0.00193672（**本地预测 +2.96%**） |
| `predict_total` | 7.95 分钟 | 8.39 分钟 |

**本地预测 +2.96%，公榜给 0。** 多花 0.44 分钟推理，换来零。

⚠️⚠️ **更正一条我写过的推理**：当时论证「倒 U 内部极值比单调趋势更可信，不用外推」。
那个论证**只保证本地测量自身的内部效度**（八个格子共用同一基准 ⟹ 不吃「基准弱」那个坑），
它**管不了本地↔公榜的迁移** —— 内部效度与迁移效度是两件独立的事。
列数是**②类容量旋钮**，ROADMAP 早就写明②类只能公榜判；
拿「内部有极值」当理由去赌迁移，是把两种效度混为一谈。

⟹ **交付基线定 `c40`**（`outputs/candidates/v3_hybrid_r480_phasebal_hist/`）：
同分、少 160 列、快 0.44 分钟。`c80` 归档留证。

⟹ ⭐ **8/23 标签回补之前不再碰任何②类旋钮**。同一天两个实证：
**轮数**（160 轮 −10.71%，而本地说 160 更好）、**列数**（本地 +2.96%，公榜 0.00%）。
②类全部攒到回补之后 —— 那时才第一次能在**真实测试期**上建尺子。

⚠️ **注意区分**：这不影响 A1′ 本身。「**加不加** history」是③类（+23.6%，已确认）；
「history **加多少列**」是②类（0%）。两件事，别混。

### ❌ A3′（滞后**截面偏差**）与 A2（截面 **rank**）双双结案（08-11，3 折同口径）

| 臂 | baseline peak | **history 臂 peak** | 脚本报的 Δpeak | 正折 |
|---|---:|---:|---:|---:|
| **A1′ raw（现口径）** | 0.00171327 | **0.00188096** | +9.79% | 3/3 |
| A3′ 滞后截面偏差 | 0.00171327 | 0.00164937 | **−3.73%** | 0/3 |
| A2 截面 rank | 0.00167346 | 0.00186730 | +11.58% | 3/3 |

**A3′ ❌**：滞后 `cross_sectional_deviation` 明显不如滞后**原始特征**（同折 −3.73%，0/3）。
解释：`difference = x_t − x_{t−1}` 在原始特征上含**该资产自身的绝对变化**（含市场共同移动），
换成 dev 之后只剩相对变化，那部分信息被丢掉了。⟹ **A1′ 的 raw 口径是对的，别改。**

**A2 ⚠️ 看着 PASS，其实不改善** —— 这是「基准弱 ⟹ 比值虚高」的又一个现场：

- 脚本的 `verdict()` 比的是**同一口径内** history 臂 vs 无 history 臂。
  rank 同时作用在两个臂上，所以它测的是「rank 口径下 history 有没有用」，
  **不是「rank 比 deviation 好不好」** —— 后者才是要决策的问题。
- 真正该看的是**会上线的那个模型（history 臂）的绝对 peak**：
  deviation **0.00188096** vs rank **0.00186730**，**rank 低 0.73%**，逐折 +4.00 / −4.28 / +0.04。
- rank 的 +11.58% 之所以大于 raw 的 +9.79%，是因为 **rank 把基准削弱了 2.32%**。

⟹ **A2 结案，不采用**（与 ROADMAP 记的「弱负面先验」一致）。
⚠️ 15 个资产 ⟹ rank 只有 ≤15 个取值，分辨率太粗，多半是这个原因。

⭐ **方法论教训（今天第二次）**：`verdict()` 只保证「臂内比较」正确。
**当一个改动同时动了两个臂（换预处理、换口径），必须另外拿会上线的那个臂做横向绝对比较**，
否则就会读成「基准弱造出来的虚高比值」。

### A1′ 已接线（08-11 当天完成，候选已就位）

受限选列的确认跑（history 列**只从按 `e` 选出的 200 列里取**，保证是 `lgbm_features`
的子集 ⟹ 推理端直接复用那 200 列的统计量、不必扩输入契约）：
peak **+10.10%**、**5/5 折**、去掉最好一折 **+9.10%**、`ΔA +6.67%` / `ΔB +2.87%`
（`outputs/experiments/history_peak_lgbm_scoped.md`）。**比不受限那版还高**
（+8.38%）—— 按 `e` 选 history 列比按 `target` 选更对路。

接线后的产物：`outputs/candidates/v3_hybrid_r480_phasebal_hist/`
（生产 `strategies/v3_hybrid/model/` **一个字节没动**）。
设计矩阵 **2,645,530 × 361** = `[xs_dev(200) ‖ previous(40) ‖ difference(40) ‖
rolling_mean(40) ‖ rolling_deviation(40) ‖ asset_id]`，`train_rows` 与生产逐位相同。

**三道门禁全过**：

| 门禁 | 结果 |
|---|---|
| `check_consistency`（离线整块 vs 在线逐 time_id，两个后端） | `max|Δ| = 1.903e-08`（阈 1e-6） |
| lightgbm ↔ numpy 兜底森林开机自检 | 通过（阈 1e-10，未回落兜底） |
| 端到端 21.4 万次 predict | **0 超时、0 个非有限值 / 3,217,458 行**、首个 time_id 无 NaN |

⭐ **推理耗时实测**（4 线程，这条同时结算了风险清单里「1440 棵树未实测」）：

| 配置 | `predict_total` | 增量 |
|---|---:|---:|
| 160 轮 / 无 history（现生产） | 4.93 分钟 | — |
| 480 轮 / 无 history | **6.64 分钟** | +1.71（轮数） |
| 480 轮 + history | **7.95 分钟** | +1.31（history） |

⚠️ ROADMAP 原来「线性外推 ≈ 4 分钟」**低估 66%**。公榜交 CSV 无风险（本地生成），
**私榜 zip 跑在主办方 4 核机上，这 7.95 分钟才是要担心的数**。

**冷启动**：首个 time_id 无历史 ⟹ `previous` / `rolling_mean` 置 0，
与训练集最前面那些行同口径（模型见过这种输入），实测不出 NaN。
`as_payload`/`from_payload` 仍然没人调用 —— **刻意不做 warm start**，少一个活动部件。
状态量实测 15→30→45→60→75 后封顶（15 资产 × 窗 5），符合预期。

### 工程记录

- ⚠️ **工作方式的坑（08-11 一天犯了三次）**：给长任务写「等它跑完」的轮询壳时，
  等待条件匹配的字样必须是 **stdout 真正会打印的**，不能是最终 `.md` 报告里的词。
  三次都写成 `grep -q "判据"`，而带 `--skip-verify` 的跑法根本不往 stdout 打这两个字
  ⟹ 壳永远轮询下去（无害但要手动 kill）。**沉默不等于还在跑，也不等于成功。**
- ⭐ **`strategies/v3_hybrid/history.py` 刻意没有照抄 `experiments/history_features.py`**。
  那份用「整段 float64 cumsum 再取差」算 `rolling_mean`，离线整块（~10 万行）与
  在线逐 time_id（≤6 行）**不是逐位相同**的。改成**对 ≤window 个滞后量定序直接求和**后，
  两条路径 4 个块全部 `max|Δ| = 0`，且与实验里量出 +10.10% 的那条路径也逐位相同。
  这是工程坑第 7 条（1-ulp 被树放大到 2.85e-03）的直接预防。
- **滞后缓存 ≡ `AssetHistory`**：为了让 5 折 × 2 臂共用一次流式扫描，改成
  「只缓存被采样行的**原始**滞后值，逐折再套该折的变换统计量」。
  `apply_robust_transform` 是逐列逐元素的 ⟹ 与取滞后可交换，四块**逐位相同**（max|Δ| = 0）。
  缓存的是原始值、不含任何拟合量 ⟹ 无泄漏。
- ⚠️ **`rolling_mean` 必须在 float64 里累加、最后一次性 round 回 float32**。
  `AssetHistory` 走的是 `np.cumsum(..., dtype=np.float64)` 再 `.astype(np.float32)`；
  在 float32 里累加会与它差**固定 1.907e-06 = 2⁻¹⁹**，且**与 batch 大小无关**
  （一开始误判成 cumsum 的累积误差，按 batch 大小扫了一遍才排除）。
  接线时在线/离线两侧都要按这个口径写 —— 工程坑第 7 条记着 1-ulp 会被树放大到 2.85e-03。
- ⚠️ **`multi` 臂第一版是实现缺陷不是结论**：γ 在内层模型的预测尺度上标定，
  却拿去乘全训练段重训模型的预测 ⟹ 尺度失配，5 折从 −41% 摆到 +41%。
  改成「用同一批内层模型预测外层」后 fold 0 由 −41.07% 变 −0.37%。
  （按外层验证段统计量归一化 = 用到验证信息 = 泄漏，不能那样修。）
- **阶段 1 的 47 臂共用一个 Gram**：X 和 y 都加权中心化 ⟹ 截距恒 0，
  `G = X̃ᵀWX̃` 只算一次，48 个右端项几乎免费 —— 整个扫描 4 秒。
  Cholesky 与 `sklearn.Ridge(lsqr, tol=1e-8)` 的 peak 相对差 5.6e-06，
  比噪声地板低约 5000 倍（**1e-8 那种门槛是对迭代求解器的误用**）。

## 截面标准差归一化 → ❌ 不采用（`ab_xsstd`，2026-08-08）

`[raw ‖ (raw−截面均值)/截面sd]` 替代 `[raw ‖ raw−截面均值]`。
选它是因为它**固定容量**（列数不变），按上面的规则本该属于迁移性好的那一类。

| alpha | ΔA | ΔB | 本地峰值变化 | 折扣后预期公榜 |
|---:|---:|---:|---:|---:|
| 500,000 | +1.4% | +6.8% | −3.8% | — |
| 2,000,000（生产） | +6.5% | +11.6% | +1.6% | **−5.6%** |
| 4,000,000 | +8.8% | +13.5% | +4.4% | **−5.5%** |

**机制不对**：B 涨得比 A 还多，本地峰值转正只是靠 A 在平方项上占便宜。
折扣后预期公榜为负 → 不上线、不花公榜额度验证。

副产品：峰值变化随 alpha 单调上升（−3.8%→+1.6%→+4.4%），
说明**截面标准化与正则部分互相替代**，它自身带一点收缩作用。

代码保留（`--cross-sectional-scaling std`，默认 `none`），两条路径一致性已验证
（max|train−infer| = 4.47e-08）。

## LightGBM 打择时块 m_t → ❌ 结案，测不出效果（2026-08-08）

> **两轮的最终结论写在这里，下面两小节是过程。**
> 第二轮（`lgbm_mt_v2`）把尺子修严之后，**6 组候选对两个基准全部 INCONCLUSIVE，
> 最大 |t| 只有 0.71，14 个配对比较里「去掉最好一折」后的 t 无一为正。**
> 第一轮那个看着有希望的 `ultra_shrunk`（t=1.11）掉到 t=0.21。
> **择时块的 LightGBM 这条线到此为止，不再纠缠。**

### 第二轮：修尺子 + 收缩轴走到底（`lgbm_mt_v2`）

相对第一轮改了四处：**3 种子平均**（各自内层早停后平均外层预测）、
**pooled Δ**（验证段不重叠，逐折平方和直接相加）、**判据写进代码**（`verdict_of()`，
结论由函数算出来写进 json，不靠人在 md 里下判断）、
**收缩轴外推两格**（`hyper_shrunk` / `extreme_shrunk`，lr 0.005、min_data 5万/10万，轮数 3000）。

还临时加了**第三个基准 `ridge_fixed`**（alpha 预注册死在 1e8，先验来自 mt_lagged，
完全不做选择）。起因是 3 折试跑里 `ridge_fair` 内层选 alpha 选砸、比 `ridge_fixed` 低
0.00084 —— 比任何 LGBM 效应都大。alpha 网格 7 个点跨十倍数量级，选错一档代价巨大；
而 LGBM 的轮数是细粒度的、早停也平滑，**两者选择过程的方差不对等**。
所以判据改成**两个基准都 PASS 才算 PASS**。
（10 折下两个基准其实一致：mean(Δ)=−0.00001、t=−0.12 —— 选择噪声在折数够时会平掉，
但 3 折时能造出 0.00084 的假象，这个教训留着。）

| 臂 | 平均 R²_m | pooled | vs `ridge_fixed` t | vs `ridge_fair` t | 去掉最好一折 t | 总判定 |
|---|---:|---:|---:|---:|---:|:--:|
| `ridge_fixed`（α=1e8 预注册） | +0.00076 | +0.00074 | — | +0.12 | −0.60 | 基准 |
| `ridge_fair`（内层选 α） | +0.00074 | +0.00073 | −0.12 | — | −0.73 | 基准 |
| `shrunk` | **+0.00093** | +0.00088 | +0.61 | +0.70 | −0.28 / −0.23 | INCONCLUSIVE |
| `hyper_shrunk` | +0.00090 | +0.00086 | +0.67 | +0.71 | −0.14 / −0.03 | INCONCLUSIVE |
| `ultra_shrunk` | +0.00080 | +0.00076 | +0.15 | +0.21 | −0.64 / −0.70 | INCONCLUSIVE |
| `loose` | +0.00077 | +0.00071 | +0.05 | +0.10 | −0.87 / −0.94 | INCONCLUSIVE |
| `moderate` | +0.00075 | +0.00069 | −0.01 | +0.02 | −0.96 / −0.95 | INCONCLUSIVE |
| `extreme_shrunk` | +0.00071 | +0.00068 | −0.44 | −0.21 | −1.01 / −0.63 | INCONCLUSIVE |

**怎么读这张表**：最好的 `shrunk` +0.00093 相对基准 +0.00076 名义上是 +22%，
但 t 只有 0.61/0.70，**低于这个设计的检出下限（Δscore ≥ 0.00017，即公榜的 +9%）**。
而且它**对 `ridge_fixed` 的机制判据是 ❌**（ΔB 涨得比 ΔA 快），
去掉最好一折后 t 也翻负。六组无一例外。

**第一轮的信号是噪声**：

| 臂 | 第一轮 | 第二轮 |
|---|---|---|
| `ultra_shrunk` | mean +0.00025，t=**+1.11** | mean +0.00006，t=**+0.21**，去最好一折 t=−0.70 |
| `moderate` | mean +0.00029，t=+0.98 | mean +0.00001，t=**+0.02**，去最好一折 t=−0.95 |

⚠️ **不能说「种子平均削了噪声」** —— 第二轮同时改了种子数、轮数上限和早停耐心，
SE 没有明显下降（部分臂反而略升），**这三个变量在本次实验里无法分离**。
能说的只是：把尺子修严之后，第一轮的表面信号消失了。

**更强收缩没有救回来**：`hyper_shrunk` t=+0.67、`extreme_shrunk` t=−0.44。
`extreme_shrunk` 有 3/10 折撞到 3000 轮上限（训练不足），但它同时也是最差的一组，
所以「再多给轮数」不是显然的解法。

**保留的一条限制**：检出下限是公榜的 +9%。真有 +9% 以内的提升，这个设计看不见。
INCONCLUSIVE 的意思是**测不出来**，不是**没效果** —— 要分辨这个量级只能靠公榜两点法。

### 第一轮的过程与踩坑（`lgbm_mt`，结论已被第二轮取代）

全时间分辨率 888,315 个 time_id（**全部 10 个相位**，所以这块不受相位错配影响），
10 折滚动 + embargo 6。预注册 4 组超参，不做网格搜索。

**无泄漏口径（这次比以往严）**：容量旋钮一律在**内层**验证段上选 ——
训练段尾部 10% 留作内层验证并与内层训练段隔开 embargo 6；ridge 的 alpha 在它上面选，
LGBM 的轮数由它早停决定；**外层验证段全程不参与任何选择**。
`ridge_fair` 与 LGBM 吃完全相同的行（gram 分三片累积再相加，顺带白送吃满训练段的 `ridge_full`）。
`ridge_full` 吃过内层验证段 → 没资格做无泄漏选择，只留曲线对照。

| 臂 | 平均 R²_m | 为正折数 | mean(Δ) | 去掉最好一折 | ΔA | ΔB | 折扣后 |
|---|---:|---:|---:|---:|---:|---:|:--:|
| `ridge_fair`（基准） | +0.00074 | — | — | — | — | — | — |
| `ultra_shrunk` | +0.00099 | 4/10 | +0.00025 | **+0.00005** | +13.3% | **+2.7%** | ✅ |
| `shrunk` | +0.00086 | 4/10 | +0.00012 | −0.00008 | +36.9% | +48.2% | ❌ |
| `moderate` | +0.00104 | 6/10 | +0.00029 | +0.00007 | +50.1% | +56.0% | ❌ |
| `loose` | +0.00073 | 4/10 | −0.00001 | −0.00022 | +29.6% | +46.3% | ❌ |

**为什么判未过关**：三条判据没有一组全过。
逐折符号一致性 4/10、4/10、6/10、4/10 —— **全部不合格**（真信号该到 8/10 以上）。
`ultra_shrunk` 过了折扣判据，但去掉最好一折只剩 +0.00005（Δscore +3.6e-05，
仅 1.7 倍噪声地板）。**fold 3（time 542997–592346）一折就占了 mean(Δ) 的 79~83%** ——
和 `mt_lagged` 当初翻车的形状一模一样。

**但有两条证据指向同一个方向：预注册的网格起点偏松了。**

1. **A/B 的机制随收缩强度单调变好**：ΔB 从 `moderate` 的 +56.0% 一路降到
   `ultra_shrunk` 的 **+2.7%**。收缩越强，涨的越是「信号对齐」而不是「预测方差」——
   按 `ab_decomposition` 的规则，这正是迁移性好的那一类。
2. **最强收缩那组的轮数预算不够用**：`ultra_shrunk` 的轮数曲线到 800 轮**仍在单调上升**
   （+0.00004 → +0.00099，没有见顶），而**内层**早停独立选出的 best_iter 有 709/792/706 三折
   —— 不是外层的乐观假象，内层也在说「还要更多轮」。

当时的结论是「**该往更强收缩 + 更低学习率 + 更多轮再走一格**，而不是判 LightGBM 没戏」。
~~✅~~ **第二轮照这个方向走了，没走通**（见上一节）：`hyper_shrunk` t=+0.67、
`extreme_shrunk` t=−0.44，而 ΔB 随收缩单调变好那条规律在 3 种子 + `ridge_fixed` 基准下
也不再成立（六组机制判据对 `ridge_fixed` 全 ❌）。
**「ΔB 单调」当时是从 4 个单种子点上读出来的，样本量不足以支撑那个推断。**

### 副产品：本地选参能偷多少分（这条对「本地尺子量反」那条主线有用）

同一个臂，无泄漏选参 vs 逐折在外层 oracle 选参：

| 臂 | 无泄漏 | oracle | 偷到 |
|---|---:|---:|---:|
| `ridge_fair` | +0.00074 | +0.00109 | **+47%** |
| `loose` | +0.00073 | +0.00106 | +44% |
| `shrunk` | +0.00086 | +0.00105 | +22% |
| `moderate` | +0.00104 | +0.00123 | +19% |
| `ultra_shrunk` | +0.00099 | +0.00104 | **+5%** |

**在验证段上选超参，能凭空多出 5%~47% 的分。** 越是容量大/旋钮敏感的臂偷得越多。
这是「本地绝对分不能信」的又一个独立量化，且与公榜那次 ΔA 高估 2.2 倍的方向一致。

### 两个踩过的坑（都写进脚本注释了）

1. **`lgb.Dataset` 不能跨不同 `min_data_in_leaf` 的候选复用。** 特征预筛选按构造时的
   `min_data_in_leaf` 定死，后面的候选想调低会撞 `[Fatal] feature_pre_filter`，
   **等于在被第一组阈值筛过的特征集上训练**。第一次 10 折就是这么被污染的，数已作废。
   修法：`feature_pre_filter=False` + 每个候选重建 Dataset。
2. **各折早停轮数不同 → 轮数路径的键跨折取交集可能为空。**
   改成 checkpoint 一律取 `min(k, trained)`，各折键完全一致。

## strategies/v3_hybrid 已建成并验收（2026-08-08）

生产岭回归**冻结复用**（`baseline_model.json` 是 v1_ridge 的原样拷贝，sha `c23a8cfb`）
+ LightGBM 截面分量 **blend50**。唯一的变量是截面分量，与公榜 0.00187232 对比时没有混淆。

推理路径：`ridge_raw`（不乘 scale 不 clip）→ 无权截面均值拆出 `m̂` →
LGBM 预测 `ê` 并投影成无权零均值 → `m̂ + 0.5·ê_ridge + 0.5·ê_lgbm` → **最后**才 scale/clip。
限幅只做一次，中间全程不截断（否则拆解被破坏）。

超参全部预注册自 `lgbm_blend_unweighted`：`xs_loose`、轮数取逐折 best_iteration 折均 **160**
（逐折 44~302）、3 种子、blend 0.5。**没有在全量上再搜一次参。**
训练 2,645,530 行（modulo 5 / phase_balanced），3 个模型共 **3.1 MB**。

**端到端实测（官方 runner，全量测试集）**：

```
rows 3,217,458 | predict_calls 214,538 | 0 timeouts
predict_total 277.2 s = 4.62 分钟（岭回归 87 s）| mean 1.292 ms/次 | max 0.766 s
```

⚠️ **这个数比 `lgbm_inference_cost` 的外推高约 6 倍** —— 那个脚本只计时了
`booster.predict`，**没算外面那一圈**（两次 pandas `.loc` 取列、两次
`apply_robust_transform`、截面去均值、岭回归矩阵乘）。15 行小批上这些开销才是大头
（岭回归单独就 0.406 ms/次）。**结论不变，但那个数测的不是端到端。**

**训练/推理一致性 4.42e-08**（门限 1e-6），证据 `consistency_v3_hybrid.json`。
第一次是 1.138e-03 不通过，原因见工程坑第 7 条。

⚠️ 08-08 那份 json 是**用一段没入库的临时代码跑出来的**（`check_consistency.py` 当时写死
只认 v1_ridge）—— 又是伤疤清单第 2 条那种「报告与代码无机械联系」。
08-09 补上了：`check_consistency.py --strategy v3_hybrid` 与
`strategies/v3_hybrid/train.py:predict_array`（离线全量口径的参照物）。
重跑复现到 **`4.421869946591439e-08` 逐位相同**，确认补的这条路和当初那段临时代码是同一个计算。

四个公榜 CSV（全部 3,217,458 行 / row_id 逐位对齐 / 无 NaN·inf / 0 行触 clip）：

| 文件 | scale | max\|pred\| | 公榜 |
|---|---:|---:|---:|
| `submission_strict_scale113.csv` | 1.13 | 0.399 | 0.00187232 |
| `submission_strict_scale092.csv` | 0.92 | 0.325 | 0.0018051540 |
| `submission_hybrid_scale090.csv` | 0.90 | 0.314 | 0.00213810 |
| `submission_hybrid_scale130.csv` | 1.30 | 0.453 | **0.0022857726** |

hybrid 的 clip(0.5) 自 scale **1.435** 起生效，四个点都安全 → 二次式精确成立。
四条一起解出上面「公榜裁决」那节的两条抛物线。

⚠️ `submission_hybrid_base0856.csv` 是 scale 0.856 的**原始产物**（未缩放、全精度），
另外两个 hybrid CSV 都是从它按比例缩放来的。它是端到端回归测试的黄金基准，别删。

### 私榜依赖：已解除（2026-08-09）

**主办方确认评测环境有 lightgbm**，`make_submission.py` 打出来的包可以直接用。

残余风险只剩一条：模型文本是 **`version=v4`**（LightGBM 4.x 格式），
评测端「有 lightgbm」不等于「有 4.x」，3.3.x 读 v4 文件会报错或误读。
这**不是 `import lightgbm` 失败**，`try: import` 兜不住。
所以 `main.py` 仍然带了一条纯 numpy 的树遍历兜底 + 开机对拍自检，见下一节。

## 纯 numpy 树遍历 `lgbm_numpy.py`（2026-08-09）

### 后端怎么选

`Model.__init__` **无条件**建一份 numpy 森林（0.09 s / 2.9 MB），然后：

```
try:  import lightgbm → 构造 3 个 Booster → 拿固定种子的 15 行合成输入对拍
      一致            → backend = "lightgbm"
      任何一步失败 / 对拍不过 → backend = "numpy"，打一行日志
显式 backend="lightgbm" 则不许降级，对拍不过直接抛（离线校验用）
```

**自检包住的是「import + Booster 构造 + 一次预测」整段**，不是只包 import ——
`version=v4` 被旧版读错属于「import 成功但结果不对」，只有对拍拦得住。
合成输入而不是真数据：提交包里没有数据，这个自检必须在评测机上也能跑。

### 三条让它不慢的做法（都实测过）

1. **叶子自环**：叶子的左右孩子都指向自己、阈值设 `+inf` → 「跑满最大深度」就等于
   「所有行都到了叶子」，遍历里不需要任何 mask 或分支
2. **分类分裂编成合成列**：唯一的 categorical 是 `asset_id`，每个不同的位集合编成一列
   （2424 个分裂只有 1901 个不同位集合），列值 `0.0`=命中 / `1.0`=不命中、阈值 `0.5`
   —— `x <= 0.5` 恰好等价于「命中走左」。整条遍历只剩一种比较。
   合成列只由 asset 决定，所以设计矩阵按 **asset 槽位**布局，`__init__` 里一次填好
3. **按深度排序 + 逐层收窄**：第 k 层只需处理「深度 > k」的树，排序后那是一个**连续后缀**
   → 每层的活动视图预先切好，运行期零切片开销（0.885 → 0.588 ms/次）

### 实测（官方 runner 全量测试集，两条路径各跑一遍）

| 项 | 主路径 lightgbm | 兜底 纯 numpy |
|---|---:|---:|
| `predict_total` | 295.6 s = **4.93 分钟** | 326.2 s = **5.44 分钟** |
| `mean_predict` | 1.378 ms/次 | 1.520 ms/次 |
| `max_predict` | 0.796 s | **0.686 s** |
| `model_init` | 0.475 s（含建 numpy 森林 + 自检） | 0.099 s |
| 超时 | 0 | 0 |
| 只算树那一段（15 行一批） | 0.15~0.21 ms/次 | 0.588 ms/次 |

**兜底路径只贵 10%**（+0.51 分钟），远低于我按树的耗时外推的 +2 分钟 ——
因为端到端的大头是特征管线不是模型（工程坑第 8 条），树那段慢 3 倍摊到全程只剩 10%。

**逐位复现已交 CSV**（3,217,458 行，与 08-08 的 `submission_hybrid_base0856.csv` 对比）：

| 路径 | 结果 |
|---|---|
| lightgbm | **逐字节相同** —— 这次重构没动过生产路径 |
| 纯 numpy | `max\|Δ\| = 2.08e-16`、`max\|Δ\|/std = 6.4e-15`（判据 1e-9），5.9% 的行差最后一两个 bit |

差异纯粹来自 480 个 double 的求和顺序，**没有一个分裂翻枝**。
其它证据：`tests/test_lgbm_numpy.py`（2.1 万行随机 + 真实 test 分区 + 防呆用例，6 个全过）；
`check_consistency.py --strategy v3_hybrid --backend {lightgbm,numpy}` 两条都是 4.422e-08。

## 🚨 阻塞项已清：LGBM 推理成本（`lgbm_inference_cost`，2026-08-08）

评测环境 **4 核 / 12GB / 无 GPU**，超时该 time_id 置 0。这条不过，
`lgbm_mt` / `lgbm_xs` / `lgbm_blend` 三轮实验价值为零。**结论：余量很大。**

按真实形态计时（一个 time_id 一批、实测每批 15.0 行，500 次调用外推到 214,538 次），
`OMP_NUM_THREADS=4` 对齐评测端：

| 树的数量（×3 种子） | ms/次 | 21.4 万次总耗时 | 相对岭回归 |
|---:|---:|---:|---:|
| 100 | 0.164 | 0.6 分钟 | 0.4× |
| 200 | 0.252 | 0.9 分钟 | 0.6× |
| 300 | 0.394 | 1.4 分钟 | 1.0× |
| 450 | 0.673 | 2.4 分钟 | 1.7× |
| 600 | 0.839 | 3.0 分钟 | 2.1× |
| **900** | 1.285 | **4.6 分钟** | 3.2× |

对照：岭回归实测 87 秒（1.45 分钟）跑完且从未超时 → **预算至少不止 87 秒**。
主办方没公布超时阈值，所以这里只给绝对耗时，不自行判定通过/不通过。

- 3 个种子的模型纯文本共 **18.5 MiB**，私榜 zip 装得下
- 成本随树数近似线性 → **可以用轮数换时间**；种子数是线性因子（3→1 直接省 3 倍）
- 模型在 modulo 40（146,615 行）上训的，不是全量。**迁移假设可核验**：
  实测平均树深 18.8（最大 32）、**平均叶子数 63.0 —— 恰好顶到 `num_leaves=63` 的上限**，
  说明树形状是**参数封顶的、不是数据量决定的**，所以换全量训练树数与叶子数不变，
  计时可迁移（深度可能略增，是这条的保留项）

## ⚠️ 权重在推理端拿不到 —— 装车验证已按无权口径重做（2026-08-08）

**发现**：`lgbm_blend` 首轮的市场分量拆解与 ê 投影**都用了 `weight`**，而：

- `data/test/*.parquet` 只有 `row_id, time_id, asset_id` + 323 特征，**没有 `weight`**
- `timeseries_api/runner.py:75` 的 `forbidden = {"weight", "target", "timestamp", "symbol"}`
  会在交给 `predict` 之前剥掉它 —— **即使跑训练集也拿不到**
- **权重重建不出来**：实测每个 asset 约 10 万个不同权重值（几乎每 time_id 一个）、
  组内 CV 0.15~0.60，且各资产均值跨分区大幅漂移（asset 0：2.18 → 0.88）。不是 asset 的常数。

✅ 没问题的：**用权重算分是对的**（那就是评分公式，训练/验证有标签也有权重）；
生产的 `cross_sectional_deviation` 一直用**无权**均值，本来就推理安全。

**重测结论：PASS 活下来了，而且那些拿不到的信息几乎不值钱。**

| 臂 | 加权 Δ% | **无权 Δ%** | 加权 t | **无权 t** | 加权折后 | **无权折后** |
|---|---:|---:|---:|---:|---:|---:|
| `replace_xs_loose` | +43.0% | +42.2% | +7.36 | +5.12 | +11.4% | +9.8% |
| `blend50_xs_loose` | +28.9% | +28.9% | +6.06 | +4.93 | +11.7% | **+11.1%** |
| `replace_xs_shrunk` | +36.6% | +36.0% | +6.27 | +5.55 | +10.5% | +7.6% |
| `blend50_xs_shrunk` | +23.9% | +24.6% | +5.52 | +5.06 | +10.0% | +9.0% |

原始 Δ 几乎没动（+43.0→+42.2），t 从 7.4 降到 5.1 但仍远超门槛，折后只掉 1~3 个百分点。
证据：`lgbm_blend_unweighted.{json,md}`（可落地）与 `lgbm_blend.{json,md}`（加权，仅对读）。

### 该带走的是 `blend50_xs_loose`

| 臂 | ΔA | **ΔB** | 原始 | **折后** |
|---|---:|---:|---:|---:|
| `blend50_xs_loose` | +15.1% | **+2.8%** | +28.9% | **+11.1%** |
| `replace_xs_loose` | +30.3% | +17.9% | +42.2% | +9.8% |
| `blend50_xs_shrunk` | +13.3% | +3.2% | +24.6% | +9.0% |
| `replace_xs_shrunk` | +26.6% | +16.8% | +36.0% | +7.6% |

**五五混合把 LGBM 的方差贡献砍一半、却保住大部分对齐** —— 所以原始分低 13 个百分点、
折后反而最高。这是 ROADMAP §5「0.5/0.5 是先验，先验不会过拟合」那条规矩的实证。

投影诊断（无权口径）：ê_mean RMS 占 ê 的 19.7%（`xs_shrunk`）/ 20.8%（`xs_loose`），
比加权口径的 25.4%/26.2% 小一些。检出下限 9.7%。

**建最终模型要用的参数**：`xs_loose`（`num_leaves 63 / min_data_frac 12000/3.5M /
lr 0.03 / feature_fraction 0.7 / lambda_l2 1.0`），轮数取折均 **160**（逐折 44~302），
3 种子，混合方式 **blend50**。

## 装车验证：LGBM 截面分量接进完整预测 → ✅ **PASS**（`lgbm_blend`，2026-08-08，⚠️ 加权口径，仅对读）

`lgbm_xs` 比的是「只做截面块的 ridge」，而生产模型是一个 ridge 同时做两块 ——
**赢前者不等于赢后者**。这一轮把 LGBM 的截面分量装进完整预测，
用**比赛真正的评分口径**整体量。264 万行（modulo 5 / phase_balanced）、5 折。

做法：拿生产口径 ridge（`[raw(200) ‖ dev(200)]` → y，alpha 内层选）的完整预测 `ŷ`，
拆成 `m̂`（逐 time_id 加权截面均值）与 `ê_ridge = ŷ − m̂`，
再把 `ê` 换成（或混入）LGBM 的。**任何预测都能这么拆，不需要模型配合。**

比**峰值 `A²/B`** 不比原始分：scale 是纯后处理旋钮，各臂最优 scale 本就不同
（baseline 0.768 vs replace 约 0.84），比原始分等于比谁的 scale 碰得准。

| 臂 | 峰值 | 相对 | t | 正折 | 去掉最好一折 t | ΔA | ΔB | **折后** |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `baseline`（生产口径） | 0.000726 | — | — | — | — | — | — | — |
| `replace_xs_loose` | 0.001039 | +43.0% | **+7.36** | 5/5 | +6.25 | — | — | +11.4% |
| `replace_xs_shrunk` | 0.000992 | +36.6% | +6.27 | 5/5 | +5.78 | — | — | +10.5% |
| `blend50_xs_loose` | 0.000936 | +28.9% | +6.06 | 5/5 | +5.28 | — | — | **+11.7%** |
| `blend50_xs_shrunk` | 0.000900 | +23.9% | +5.52 | 5/5 | +5.07 | — | — | +10.0% |

检出下限 8.7%，效应是它的 2.7~4.9 倍。

**最值得记的一点**：本地原始收益从 +23.9% 到 +43.0% 差得很开，
但**打完 2.2 折之后四个臂全部收敛到 +10.0%~+11.7%**。
折扣规则把「容量更大所以本地拟合更好」那部分剥掉了，剩下的量级高度一致。

⚠️ **baseline 的绝对峰值 0.000726 远低于公榜 0.00187232，这是正常的**：
本地是 5 折滚动、覆盖整段训练史，而公榜测试期本来就更好预测
（见「这个吻合很可能是巧合」那节）。**只有配对 Δ 有意义，绝对分没有。**

### 投影确认是必需的（`lgbm_xs` 里唯一没测的量，现在测了）

| 候选 | ê_mean RMS 占 ê | R²_e 投影前 → 后 | ridge 的 R²_e |
|---|---:|---:|---:|
| `xs_shrunk` | **25.4%** | +0.00170 → +0.00182 | +0.00073 |
| `xs_loose` | **26.2%** | +0.00187 → +0.00201 | +0.00073 |

三次独立测量（烟测 26.2%、正式两个候选 25.4%/26.2%）高度一致 ——
**约四分之一的 LGBM 输出幅度是虚假的市场级分量**，不投影掉会污染 `m̂`
（而 `m̂` 那块岭回归已经比树好）。投影买回约 7% 的 R²_e，与解析证明的「只赚不亏」一致。

LightGBM 在截面块上是岭回归的 **2.5~2.7 倍**（+0.00201 vs +0.00073）。

### ❌ 一条被推翻的判断

`lgbm_xs` 那轮我写过「折扣后该带走的是 `xs_shrunk` 而不是 `xs_loose`」。
**装车后不成立** —— `xs_loose` 在原始和折后都赢。
那条只在 `lgbm_xs` 的口径下（孤立测截面块、modulo 3、`min_data_in_leaf` 用绝对值）成立，
**换成完整预测口径就没了，别当结论**。

`replace` vs `blend50`：原始分 `replace` 明显更高（+43.0% vs +28.9%），
但**折后基本打平**（+11.4% vs +11.7%）。按 ROADMAP §5「先验不会过拟合」的规矩，
`blend50` 是更稳的选择。

### 还没验的（按阻塞程度排）

1. **🚨 推理成本没测过。** 评测环境 4 核 / 12GB / 无 GPU，超时该 time_id 置 0。
   岭回归是 0.42ms/次 × 21.4 万次 = 87s，余量很大；
   但 LGBM 是 3 种子 × 数百到近千棵树，**必须实测**，否则前面全白做。
2. **仍然是本地数。** 公榜才是裁判（8/23 到期）。
3. baseline 是「生产口径」不是生产模型本身（采样是 phase_balanced 而非 periodic，
   alpha 逐折内层选而非固定 2e6）。配对 Δ 仍然公平，但绝对值不可与线上直接对读。

## LightGBM 打截面块 e = y − m → ✅ **PASS**（`lgbm_xs`，2026-08-08，装车前的孤立测量）

**项目里第一次有改动通过预注册判据。** 440 万行（`sample_modulo=3, phase_balanced`，
10 个相位精确各 10%）、5 折 × 3 候选 × 3 种子。目标 `e = y − m_t`，
设计矩阵是**截面去均值后的 200 列** + `asset_id`。

**双基准都过**：`ridge_fair`（内层选 alpha，无泄漏）与 `ridge_oracle_alpha`
（alpha 在**外层折均值**上取最大，**故意偏强**）。用「故意偏强的基准」代替「拍一个魔数」——
不需要预注册常数，方向保守。

| 臂 | 平均 R²_e | pooled | vs `ridge_oracle_alpha` t | 正折 | 去掉最好一折 t | 机制 |
|---|---:|---:|---:|---:|---:|:--:|
| `ridge_fair` | +0.00035 | +0.00037 | −1.46 | 0/5 | −1.52 | ❌ |
| `ridge_oracle_alpha` | +0.00113 | +0.00113 | — | — | — | — |
| `xs_loose` | **+0.00175** | +0.00174 | **+6.38** | **5/5** | +5.35 | ✅ |
| `xs_moderate` | +0.00171 | +0.00170 | +6.73 | 5/5 | +5.96 | ✅ |
| `xs_shrunk` | +0.00159 | +0.00158 | +4.04 | 5/5 | +3.17 | ✅ |

逐折 Δ（vs `ridge_oracle_alpha`，`xs_loose`）：`+0.00026 +0.00075 +0.00062 +0.00082 +0.00063`
—— **五折全正、量级一致，不是单折撑起来的**（对比择时块：最大 |t| 只有 0.71，
去掉最好一折全翻负）。alpha 网格最优值 1e5 落在**内部**、曲线干净单峰，基准没被压弱。
无一折撞 1500 轮上限。

### ⚠️ 前提：ê 必须投影成截面加权零均值

`e = y − m_t` 用的是验证段的**真实** m_t。而 `Δscore = 0.28 × ΔR²_e` 依赖恒等式
`Score = 0.72·R²_m + 0.28·R²_e`，它要求预测能干净拆成「市场分量 + 截面分量」。
**LGBM 输出的 ê 在每个 time_id 内的加权均值不一定是 0**，不为 0 的部分会污染市场分量。

好在投影只赚不亏。设 ê = ê_mean + ê_dev（time_id 内加权分解），
因 `Σw·e = 0`（实测 1.91e-14）且 `Σw·ê_dev = 0`：

```
Σw(e−ê)² = Σw(e−ê_dev)² + ê_mean²·Σw  ≥  Σw(e−ê_dev)²
```

**误差严格不增** → 现在测到的 R²_e 是投影版的**下界**，且投影后恒等式才干净。
runner 每次 predict 恰好喂一个 time_id 的全部行，投影是因果的、可实现的。
**这是必须做的实现步骤，不是可选项。** ê_mean 实际多大**尚未实测**（它决定下界有多松）。

### 折扣后排序翻转 —— 该带走的不是本地分最高那个

| 臂 | ΔA | ΔB | 本地峰值 | **折后（ΔA 打 2.2 折）** |
|---|---:|---:|---:|---:|
| `xs_shrunk` | +21.7% | **+8.6%** | +36.4% | **+11.1%** |
| `xs_moderate` | +33.0% | +20.2% | +47.2% | +10.0% |
| `xs_loose` | +39.0% | +28.1% | +50.8% | +8.2% |

**本地最好的 `xs_loose` 折扣后变成最差。** 它的 ΔB 涨 28.1%（靠容量硬拟合），
而 `xs_shrunk` 只涨 8.6%。按「增加容量的改动一律重罚」这条规矩，
**要带走的是 `xs_shrunk`**。

本地换算总分：`xs_shrunk` +0.000127（公榜的 +6.8%）/ `xs_loose` +0.000173（+9.2%）。
检出下限 Δscore ≥ 0.00005（公榜的 +3%）—— 效应是下限的 2.5~3.5 倍。

### 三条还没验的（别当成已经赢了）

1. **这是本地数。** 容量改动的本地结论在本项目已被证明不可信。机制判据带折扣过了、
   5/5 折 t=6.4 也远比择时块强，但**公榜才是裁判**。
2. **基准不是生产模型。** 这里的 ridge 是「截面去均值特征 → e」，
   生产模型是「[raw‖dev] → y」。「赢了截面块的 ridge」**≠**「赢了生产模型」——
   真要下结论得把 LGBM 的截面分量接进完整预测再整体量一次。
3. **只用了 1/3 训练数据**（modulo 3），且 6.3% 的 time_id 资产数不足 15
   （2 资产的截面上 e₁ 与 e₂ 完全共线，本质不可预测；未做过滤，与生产口径一致）。

## 严格求解器已上线（2026-08-08，ROADMAP #3 结案）

`LSQR tol 1e-4 / max_iter 100` → **`1e-8 / 2000`**。正式模型换成
`outputs/candidates/v1_ridge_strict/` 那份（sha `e2bec9a9…` → **`c23a8cfb…`**），
`ridge_n_iter=35`，远未触顶。`train.py` 的 CLI 默认与 `fit_model()` 签名默认现在都是严格档
（此前签名还留着 1e-4/100，绕过 CLI 直接调用会悄悄退回松求解器）。

验收（`ridge_strict_acceptance.json`，7 道门禁全过）：配对 Δ +1.87e-06（base grid）/
+1.43e-06（half-offset），均在 2.16e-05 噪声地板内 —— **样本外没有可测的损失**。
训练/推理一致性 4.66e-08。

### 线程数复现性：实测三档（p008 modulo 10，128,850 行）

| 训练线程数 | sha 与正式模型 | ridge_n_iter | 选中特征 | 加权 R² | ΔR² | max\|Δpred\| |
|---|---|---:|---|---:|---:|---:|
| 4（正式模型口径） | **逐位相同** | 35 | 同 | 0.00372818 | — | — |
| 1 | 不同 | 35 | 同 | 0.00372818 | +4.8e-10 | 4.8e-07 |
| 不钉（本机 32 核） | 不同 | 35 | 同 | 0.00372817 | −7.4e-09 | 1.3e-05 |

**两个结论要分开说，别混：**

- **分数复现：无条件成立。** 相对漂移 1.97e-06，任何线程数下都复现到 6 位有效数字。
  10 月答辩「复现 0.00119」那条**已经安全**（松求解器时预测漂移 1.6e-04，现在最差 1.3e-05，好 12 倍）。
- **逐位复现（sha 相同）：只在钉住线程数时成立。** 要 bit-exact 就必须
  `OPENBLAS_NUM_THREADS=4 OMP_NUM_THREADS=4`。这是 LSQR 迭代解法的固有性质，收紧 tol 只能压小、压不到 0。

### 公榜已验证：0.00187232（2026-08-08，同 scale 1.13）

| 模型 | sha | scale | 公榜 |
|---|---|---:|---:|
| legacy | `e2bec9a9…` | 1.13 | 0.00186805 |
| **strict（现正式模型）** | `c23a8cfb…` | 1.13 | **0.00187232** |

**同 scale、同配置，只换 LSQR 停止条件 → +4.27e-06（+0.229%）。**
两模型在测试集上 `max|Δpred|=0.0053`、`corr 0.99993` —— 不是同一组预测。

值得记一笔：**本地配对 Δ 与公榜同号、同量级**（本地 +1.87e-06 / 公榜 +4.27e-06）。
这是极少数本地与公榜方向一致的情形 —— 因为它**不是「拟合紧密度」维度的改动**
（求解器收敛精度不改变模型容量），所以不受 alpha 那个量反问题影响。

提交 CSV 已验：3,217,458 行、`row_id` 与 `sample_submission.csv` 逐位一致、无 NaN/inf、
`|pred|` 最大 0.399 < clip 0.5（**未触限 → 二次式 `Score(a)=2aA−a²B` 精确成立**）。
预测 std 0.041636 与验收报告的 `candidate_std=0.04163607` 逐位吻合，确认出自严格模型。

**待办（明天）**：交第二点 scale 0.92，解出严格模型自己的 `A`、`B` 与精确峰值 `A²/B`。
用「一个点 + B 的**无权**近似」外推的预测是 **最优 scale 1.1359 / 峰值 0.00187237 / IC 0.04327**
（legacy 是 1.1317 / 0.00186804 / 0.04322）—— ⚠️ 这条建立在「测试集权重近似均匀」上，
**测试集没有 weight 列，无法验证**，明天第二点就是用来检验它的。

## v2_lgbm 的加载路径在本机必然 OOM（2026-08-08）

`strategies/v2_lgbm/data_utils.py:28` 的 `load_parquet_files` 用
`pd.concat((pd.read_parquet(p) for p in paths))` —— **先把 9 个分区全读进来再 concat**。

| 项 | 实测 |
|---|---:|
| 单分区（1,499,352 行 × 328 列） | 1.84 GiB |
| 9 分区合计 | 16.6 GiB |
| `pd.concat` 峰值（输入与输出并存） | **≈33.1 GiB** |
| 本机总内存 | 30 GiB |

**`--max-train-rows` 救不了**：它作用在 `train.py:225` 的 `sample_by_time`，在 CV 循环**内部**，
也就是全量加载**之后**。那是「CV 时的行下采样」，不是「载入上限」，
README 把它当烟测旋钮是有误导性的。烟测进程 exit 137 = SIGKILL，就是这么来的。

**要跑主办方基线（将来做截面块时）必须先加一个载入期的分区/行上限。**
择时块那条线不受影响 —— `lgbm_mt.py` 读的是 `mt_aggregates.npz`（1.17 GB）。

工具链本身没问题：`lightgbm 4.3.0` 在 py3.13 / numpy 2.5.1 / pandas 3.0.3 上
`Dataset`（带样本权重）/ `train`（带早停）/ `predict(num_iteration=k)` / `model_to_string`
全部正常，**不需要单开 `.venv-lgbm`**。`model_to_string` 是纯文本 → 私榜纯 numpy 写树遍历兜底成立。

## 工程坑（都踩过）

1. ~~**train.py 在不同 BLAS 线程数下训出不同模型。**~~ **已修（2026-08-08），见下一节。**
   历史症状：`Ridge(solver="lsqr", tol=1e-4)` 是迭代解法且停得太早，线程数一变求和顺序就变，
   coef 相对差 4.75e-04、预测差到 1.6e-04。
2. **公榜交 CSV（`row_id,target`），私榜才交 zip。** 公榜每天最多 5 次成功评分，私榜共 10 次。
   `scripts/make_submission.py` 是私榜用的，公榜要跑 runner 出 CSV。
3. **提交 CSV 的精度**：主办方示例是 8 位小数、64.1 MB；直接 `to_csv` 写 float64 是 97.6 MB。
   float32 根本没那么多有效位，多出来的全是垃圾位。
4. **配对 A/B 的 fit 缓存键必须由 `FIT_KEYS` 生成。** 曾硬编码成 `(feature_count, ridge_alpha)`，
   导致 meandev 四个臂全部命中 baseline 的缓存、Δ 恒为 0。
   **Δ 严格等于 0 是这类 bug 的显式信号**，看绝对分就发现不了。
5. **`np.ascontiguousarray()` 作用在已经连续的切片上会返回视图、不是副本 ——
   视图通过 `.base` 钉住整个父数组。** `lgbm_inference_cost` 首跑 OOM 就是这么来的：

   ```python
   # ❌ x[va_rows] 在推导里每轮重算 → 每个 batch 钉住自己那个 118 MB 的父数组
   batches = [np.ascontiguousarray(x[va_rows][s:s+c]) for s, c in ...]
   # ✅ 提到外面 → 2000 个视图共享同一个父数组
   x_va = x[va_rows]
   batches = [np.ascontiguousarray(x_va[s:s+c]) for s, c in ...]
   ```

   实测（300k×201 的 x、150k 行的 fancy index、只做 100 个 batch）：
   **修复前 RSS 11.26 GB / 100 个父数组；修复后 0.14 GB / 1 个父数组**。
   2000 个 batch 就是 236 GB。
   **教训：小切片的内存占用不等于小 —— 要看 `.base` 钉住了什么。**
6. **长任务的输出不要接 `| tail`。** `tail` 缓冲到进程结束才输出，进程被杀就**一行日志都没有**。
   上面那次 OOM 因此完全无法定位，只能靠事后重建。后台跑 + 读输出文件，或前台不接管道。
7. **树是阶跃函数：训练与推理的特征计算必须逐位相同，「数学等价」不够。**
   `v3_hybrid` 首次一致性验收 `max|Δ|=1.138e-03`（门限 1e-6）。查下来：

   | 环节 | 差异 |
   |---|---|
   | 输入（`cross_sectional_deviation` 的 reduceat 顺序累加 vs `single_time_deviation` 的 `.mean()` 成对求和） | **4.77e-07**（float32 一个 ulp，60% 元素不逐位相同） |
   | 岭回归输出（线性） | 9.5e-07 —— 无所谓 |
   | **LGBM 输出（树）** | **2.85e-03** —— 放大 6000 倍，0.52%（39/7500）的行 |

   输入在分裂阈值附近差一个 ulp 就翻到另一枝，输出直接跳一个叶子值。
   修法：推理端的 LGBM 分支改用与训练端**同一个**归约函数 → `max|Δ|` 降到 **4.42e-08**。
   岭回归分支保持 `single_time_deviation`（线性无所谓，且要与 v1_ridge 生产路径逐位一致）。
8. **测「推理成本」要测端到端，不能只测模型本身。**
   `lgbm_inference_cost` 只计时 `booster.predict`，外推 160 轮约 0.22 ms/次；
   而 runner 实测 **1.292 ms/次**，差 6 倍 —— 差在两次 pandas `.loc` 取列、
   两次 `apply_robust_transform`、截面去均值、岭回归矩阵乘。
   **15 行小批上，特征管线开销比模型本身大。**
9. **`load_rows` / `load_time_sample` 的读取开销与 `sample_modulo` 无关。**
   `iter_batches` 把每个分区的每一批完整读出来（12 万行 × 328 列）再按 mask 丢弃，
   所以「调小采样率省内存」只对留下来的 numpy 数组成立，对读取阶段无效 ——
   实测 modulo 40 只留 33 万行（0.40 GB），但加载后 RSS 已到 **4.10 GB**（pyarrow 内存池不及时归还）。
10. **自己写树推理时，「差不多的语义」= 错的语义。** LightGBM 文本模型有四处细节
    必须照 C++ 源码抄，任何一处想当然都会在分裂边界上翻枝：

    | 细节 | 正确语义 |
    |---|---|
    | 数值分裂方向 | `x <= threshold` 走**左**（不是 `<`） |
    | 缺失值 | `decision_type` 的 bit2-3 是 missing_type；本模型全是 `None` → NaN 先当 **0.0** 再比 |
    | 分类分裂 | `int(fval)` **截断取整**，负数直接走右；再查 `cat_threshold` 位集合，**命中走左** |
    | shrinkage | `leaf_value` **已经乘过** learning_rate，`boost_from_average` 也已烘进 Tree=0 → 直接求和，别再乘一遍 |

    还有一条容易漏：`threshold` 是以 `%.17g` 写出的 double，**必须按 float64 解析并比较**；
    float32 输入交给 LightGBM 时它内部 `static_cast<double>`，所以两侧都在 double 上比才逐位一致。
11. **每个策略目录都有一个叫 `main` / `train` / `features` 的模块 —— 靠 `sys.path` 顺序
    import 它们迟早会拿到错的那个。** 08-09 一天之内踩了三次：

    | 现场 | 症状 |
    |---|---|
    | `check_consistency.py` import v3_hybrid 的 `train` | 它模块级写着 `from train import …`（本意是 v1_ridge 的），**结果 import 了自己** → 循环导入报错。当脚本跑时模块名是 `__main__`，所以以前一直没暴露 |
    | `unittest discover` 跑整个 tests/ | 先跑的用例已经把 v1_ridge 的 `main` 塞进 `sys.modules`，后面的用例拿到它 → `Model.__init__() got an unexpected keyword argument` |
    | v3_hybrid 的 `features` | 与 v1_ridge 的**恰好逐字节相同**，所以拿错了也不报错 —— 最危险的那种 |

    修法：跨策略加载一律用 `importlib.util.spec_from_file_location` **按文件路径**加载并给唯一模块名；
    策略内部只在函数体里 import 另一个策略的模块，别放模块级。
12. **优化 numpy 查表遍历时，表的大小比指令数重要得多。**
    为了省掉每层一次 `np.add`，我把「每 asset 一份阈值表」预展开成 43 MB ——
    **反而从 0.885 ms 慢到 1.392 ms**，因为 gather 的目标放不进 L2。
    保持 4 张表合计 2.9 MB、多做一次加法，才是快的那条。
    **7200 元素的小数组上，numpy 是访存受限的，不是指令受限的。**

## 时间线

- 8/23 公榜截止 + 标签回补（主办方重发包 → 对 docs/examples/timeseries_api 跑 git diff）
- 8/31 策略文件提交截止（私榜策略共 10 次）
- 9 月 实盘评估；10 月 答辩（交付物最好逐行自己重写，验收标准：复现 0.00119）
