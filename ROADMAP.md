# ROADMAP.md — 改进优先级

> 2026-08-08 大改。原版源自 HANDOFF_20260724 §4/§5/§8，其中 P0 已完成、P1 已复测、
> P2-1 已被实验否决、P2-2 已有答案。旧条目一律保留在下方「已结案」里并标注证据文件，
> 不删除 —— 免得下周重走一遍已经排除掉的路。

---

# 🗺️ 到 8/31 的路线（2026-08-10 定，每周复核）

**日程**（主办方 Timeline 原文）：8/23 公榜停止更新 + 发布标签回补；8/31 关闭策略提交。
**剩余预算**：公榜 8/11–8/22 共 **12 天 × 5 = 60 次**；私榜策略文件 **10 次**（到 8/31）。

⚠️ **8/23 之后没有公榜可打。** 这一条决定了整条路线的形状。

## A 段（8/11 – 8/22）：只做第③类结构改动

判据成本决定顺序 —— ③类本地免费，②类本地量反、1 次额度只换一个配置。
**②类旋钮（alpha / 特征数 / 树深 / 学习率 / 轮数 / history 列数 / `sample_modulo`）
全部推迟到 8/23 之后。**

🚩 **2026-08-11 用两次公榜额度把这条禁令坐实了**（不再是先验，是实证）：

| ②类旋钮 | 本地说 | 公榜说 |
|---|---|---|
| **轮数** 480→160 | 本地 peak 0.00192421 ≥ 0.00191184，**说 160 更好** | **−10.71%**，480 明显更好 |
| **history 列数** 40→80 | 本地梯子 **+2.96%**（还有干净的倒 U 内部极值） | **−0.0032%，完全打平** |

⚠️ 第二行还纠正了一个**方法论错误**：本地梯子有「内部极值」只说明**那次本地测量自身干净**
（共用基准 ⟹ 不吃「基准弱」的坑），**不构成它能迁移的理由** —— 内部效度与迁移效度是两回事。
⟹ **8/23 之前对②类不花任何额度、也不信任何本地量级。**

| # | 项目 | 依据 | 成本 |
|---|---|---|---|
| **A1** | ~~每资产历史特征接进**岭回归**~~ **→ 08-11 否决** | ❌ 那 +2.5% 是**固定 `prediction_scale=0.5` 下的绝对分**（`walk_forward_history.py:37,124`），换成尺度无关的 peak 后是 **−1.97%、2/5 折**（`ΔA +2.21%` < `ΔB/2`）。同一批拟合、同一批折，两把尺子给出相反符号。**与 08-10「市场模型重建 13% 余量」是同一个机制，第二次咬人。** 证据 `outputs/experiments/history_peak.md` | 已结案 |
| **A1′** | **每资产历史特征接进 LGBM 截面块**（新立项，**已过判据**） | ✅ 原实验从没测过这一块。peak **+8.38%、5/5 折全正**、去掉最好一折 **+7.73%**、`2ΔA(12.3%) > ΔB(3.74%)`（加信号不是加方差）。因果性单独验过，重建路径与 `AssetHistory.transform` **四块逐位相同**。⚠️ 480 轮下是 +17.64%，**别用**：本地 baseline 在 480 轮上被打坏（0.001775→0.001625，正是②类本地量反），点估计取 **+8.38%**。证据 `outputs/experiments/history_peak_lgbm_r160.md` | 待接线 + 1 次确认 |
| **A0** | ~~`responder_*` 换训练目标~~ **→ 08-11 结案，不支持** | ❌ 47 列全扫 + 两层折 + 完整 alpha 阶梯，**四条判据全不过**：`inner_selected` **−2.80%、1/5 折**、整条阶梯全负；`multi` −3.66%。选择偏置当场抓住 —— `responder_04` 筛选折 +15.80%，外层被选中那折 **−7.55%**；5 折里 2 折内层自己选回 `target`。证据 `outputs/experiments/responder_targets{,_stage1}.md`。详见下方专条 | 已结案 |
| **A2** | ~~截面 rank 变换~~ **→ 08-11 结案，不采用** | ❌ 会上线的那个臂（history 臂）绝对 peak：deviation **0.00188096** vs rank **0.00186730**，**rank 低 0.73%**（逐折 +4.00 / −4.28 / +0.04）。⚠️ 脚本 `verdict()` 报的 +11.58% > raw 的 +9.79% 是假象 ——rank 同时作用在两个臂上、把**基准**削弱了 2.32%，比值才虚高（正是 `history_gap` 记的 r=−0.986 那条）。猜测原因：只有 ≤15 个资产 ⟹ rank 只有 ≤15 个取值，分辨率太粗。证据 `outputs/experiments/a2_rank.md` | 已结案 |
| **A3′** | ~~history 改喂「滞后截面偏差」~~ **→ 08-11 结案，不采用** | ❌ 同折 **−3.73%、0/3**，`2ΔA>ΔB` 不成立（history 臂 peak 0.00188096 → 0.00164937）。解释：`difference = x_t − x_{t−1}` 在**原始特征**上含该资产自身的绝对变化（含市场共同移动），换成 dev 后只剩相对变化，那部分信息丢了。⟹ **A1′ 的 raw 口径是对的，别改。** 证据 `outputs/experiments/a3p_dev.md` | 已结案 |
| **A4** | ~~条件性组合器~~ **→ 08-11 结案，不采用** | ❌ 按 `ê` 截面离散度分 4 箱、各箱一个权重：折均 **−0.08%、1/3 折**（+1.72 / −1.11 / −0.84）。⭐ 基准取的是**训练段内解出的最优全局 c**（第①类免费），不是 c=1 —— 否则会把①类收益算成 A4 的。副产物：**全局最优 c 逐折在 0.33 / 0.89 / 0.90 之间摆**，相对权重本身就不稳，与 0c「配比只值 +0.02%」一致。防泄漏照搬 A0 教训（系数与分箱边界只用内层留出段，且用同一批内层模型预测外层）。证据 `outputs/experiments/conditional_blend.md` | 已结案 |

🚩 **A 段③类到此全部测完**（A1′ ✅ 唯一赢家；A0 / A2 / A3′ / A4 全 ❌）。
8/12–8/22 的主线转到下面的 **A5**，公榜额度基本闲置 ——
③类做完了、②类不迁移，**不要为了用掉额度去交②类变体**。

### A5：`responder_*` 多任务（8/12 A/B/C 门禁结案）

依据仓库外 `~/Documents/Quant_trade_notes/2026-08-11_responder与Modal可行性计划.md`，
8/12 已把昂贵 NN 之前的三道本地门禁一次跑完；完整证据见
`outputs/experiments/responder_{analysis,predictability,residual_increment}.{json,md}`。

| 阶段 | 实测结果 | 裁决 |
|---|---|---|
| **A 结构分析** | 全量 **13,227,692 行 / 888,315 个 time_id**；47 列聚为 **24 个族群**，PCA **10 个成分解释 90%** | ✅ 只冻结结构，不作提分结论 |
| **B 样本外可预测性** | 严格 5 折、两层选择；**8/24 个族群通过**，通过族群均 5/5 折为正，且不是单资产制造 | ✅ feature 确实能预测部分 responder |
| **C 低成本残差门禁** | 把阶段 B 的 8 个冻结族群预测加进同口径 target Ridge：逐折 **−32.55 / −27.63 / −3.90 / −12.82 / −30.67%**，折均 **−20.64%、0/5 折** | ❌ 不含 target 残差增量，停止 |
| **D 多任务 NN / Modal** | 未启动 | ⛔ 阶段 C 不过，不安装 PyTorch、不上传 17GB 数据、不花 GPU 额度 |

⭐ **结论要分两层读**：responder 并非不可预测；它们是**能预测但不补 target 残差**。
因此 A0（换/变换标签）与 A5（先验门禁）合起来，已经足以关闭当前数据上的 responder 路线。

⚠️ 阶段 C 本轮是经济型的 **Ridge 残差门禁**，不是“当前 3 种子 × 480 轮 history-LGBM”
的完整五折重训。之所以不继续，是因为方向 0/5 且平均 −20.64%，已远低于进入昂贵确认档的门槛；
不得把省下的确认档写成“已经跑过”。若 8/23 后扩展训练数据改变了关系，再从阶段 B/C 重开，
不能直接跳到 NN。

工程副产物：`src.metric.scale_invariant_score()` 现在是全项目统一的 `A/B/peak/a*` 接口；
所有新脚本只在训练段拟合特征选择/变换，外层验证段不参与选择，完整测试在
`tests/test_metric.py` 与 `tests/test_responder_research.py`。

**8/12 后 A 段全部结案**：A1′ 是唯一上线赢家；A0/A2/A3′/A4/A5 全部关闭。
8/13–8/22 不为用额度而开新公榜点，主线改为交付风险与 8/23 数据更新准备。
2 种子全量 runner 已实测 **536.86 秒（8.95 分钟）**，相对 3 种子的 567.80 秒只省
**30.94 秒 / 5.45%**，收益太小；故默认仍是 3 种子，2 种子只保留为明确超时退路。
3 种子 staging 的私榜 zip 演练也已通过内容审计；打包工具支持 `--model-dir`，冻结前可先从
staging 出包，不必提前覆盖生产目录。

### A0 专条：`responder_*` —— 唯一可能是「新信号来源」的一条

**事实**：`data/train` 每分区 375 列 = 5 索引 + 323 `feature_*` + **47 `responder_*`**。
主办方原文：「基于**未来不可见区间**构造的一组匿名目标，覆盖多个预测窗口和多个市场响应维度」。
公榜测试与私榜都不提供。实测（p008，modulo 10，加权）：

| | 与 target 加权相关 | std | 值域 |
|---|---:|---:|---|
| `responder_03` | **+0.8187** | 0.27 | **[0, 1]** |
| `responder_28` | +0.7033 | 1.08 | ±3 |
| `responder_04` | +0.4148 | 0.29 | [0, 1] |

**47 个合起来线性解释 target 82.7% 的方差**（同期、样本内，只作上界）。

**8/10 晚 3 折粗筛（不作数，但记着）**：`responder_04` 均值 **+9.40%**、2/3 折，
但**整个增益由单折（+34.4%）造出**，去掉最好一折 **−1.55% 翻负**；其余 5 个全负。
⭐ **选哪个不看它与 target 的相关性** —— `responder_03` 相关 0.82 却 −9.6%，
`responder_04` 相关 0.41 却是唯一为正。

⚠️⚠️ **2026-08-11 更正：上面这段「alpha 不公平」的推理是错的，已作废。**
原文写「岭回归里目标缩放 c 倍、等效正则要按 c² 缩放才不变 ⟹ 给 `responder_04` 的正则
强了约 14 倍」。**不成立。** 正确的等变律是

    R(y/s, α) = R(y, α) / s        （**不是** R(y, α·s²)/s）

推导：`J̃(b) = Σw(y/s − Xb)² + α‖b‖²`，令 `b = u/s` 得
`J̃ = (1/s²)·[Σw(y − Xu)² + α‖u‖²]` —— **数据项与惩罚项同步缩放 `s²`**，
提取公因子后 α 原封不动。即岭回归对目标缩放**严格等变**：目标缩放 s 倍，
系数就缩放 1/s 倍，方向完全不变。而 `peak = A²/B` 对预测缩放**不变**
⟹ **目标的量纲对 peak 没有任何影响**，一个固定的 α 对 std 0.29 和 std 1.09 的目标一样紧。

实测：合成数据 **1.4e-15**、真实数据 **4.19e-15**（机器精度）；
反倒是按 `alpha_eff = α·var(y)/var(target)` 调 α 会**引入** 1.05% 的 peak 失真、
系数偏 **65%** —— 那条规则不是在修公平性，**是在制造不公平**。
⟹ **不许再按目标方差调 alpha。** 所有候选走同一条 alpha 阶梯即可。
（验证代码在 `experiments/responder_targets.py:check_solver`，`--check-solver` 跑。）

⟹ 顺带结论：**8/10 那次粗筛的「缺陷 1」根本不存在**，它真正的问题只剩
「只测了 6 个」「只有 3 折」，外加那次的脚本与产物在仓库里没有任何留存。

⚠️ **真正要小心的是中心化，不是缩放**：`peak` 对预测缩放不变，但**对平移不变不了**。
[0,1] 型 responder 的常数偏移（均值 ≈ 0.5）若进了预测，`B` 直接爆炸、A/B/peak 全错。
做法：X 和 y 都按加权均值中心化 ⟹ 加权最小二乘的截距恒为 0。

**正式实验 `experiments/responder_targets.py` 的设计要点**：

- ~~`alpha_eff = fold_alpha × var(y_train)/var(target_train)`~~ **作废，见上面的更正**；
  所有候选走**同一条 alpha 阶梯，α 不随目标方差调整**
- **两层折**防选择偏置（47 选 1，直接在外层挑最好的必然选出噪声）：候选只在内层选
- 两阶段：阶段 1 单折扫 47 个剪枝 → **短名单写成脚本常量、预注册**；阶段 2 外层多折判决
- 判据加严：配对 Δ>0 + **去掉最好一折仍为正** + 整条 alpha 阶梯为正 + 点估计 **≥ +5%**
- ⚠️ 两个已踩的坑：[0,1] 的 responder **必须中心化再拟合**（否则常数偏移进 B，A/B/peak 全错）；
  **`np.nan_to_num` 默认把 inf 映射成 1.8e308**，要用 `np.where(np.isfinite(x), x, 0.0)`

### 08-11 实测结果（结案）

**阶段 1**（47 列全扫，单折，冻结选列 + 共用 Gram，仅剪枝）：

| 训练目标 | 单折 peak 相对 target | 阶梯上胜出档数 |
|---|---:|---:|
| `responder_04` | **+15.80%** | 5/7 |
| `responder_28` | +11.34% | 6/7 |
| `responder_05` | +7.68% | 4/7 |
| `responder_29` | +5.57% | 4/7 |
| `responder_03`（相关最高 0.82） | **−15.47%** | 0/7 |

⭐ **「不看相关性」这条被硬证实**：相关 0.82 的 `responder_03` 是 −15.47%，
而相关只有 0.41 的 `responder_04` 排第一。

**阶段 2**（短名单 5 个 + target 基准，外层 5 折，候选只在内层选）：
**四条判据全不过。**

| 臂 | Δpeak | 正折 | 去掉最好一折 | 整条阶梯为正 |
|---|---:|---:|---:|---|
| `inner_selected` | **−2.80%** | 1/5 | −6.30% | ❌（7 档全负） |
| `multi`（OOF 线性组合） | −3.66% | 1/5 | −12.86% | ❌ |

⭐ **两层折确实拦住了选择偏置**：`responder_04` 在筛选折上 +15.80%，
轮到外层被内层选中那折是 **−7.55%**；5 折里有 **2 折内层自己选回了 `target`**。
这正是「直接在外层挑最好的必然选出噪声」的实证。

⚠️ `multi` 臂第一版是**实现缺陷**不是结论：γ 在内层模型的预测尺度上标定，
却拿去乘全训练段重训模型的预测，尺度失配 ⟹ 5 折从 −41% 摆到 +41%。
改成「用同一批内层模型预测外层」后 fold 0 由 −41.07% 变 −0.37%，修好后仍不过判据。
（按外层验证段统计量归一化会用到验证信息 = 泄漏，不能那样修。）

**⟹ A0 结案，不支持。** 47 列这条「新信号来源」的路走不通，
`ê_lgbm` 那边也不必再复测（ridge 上连方向都没有）。

### 补测：target 投影去噪（08-11，最后一个没测过的形态，仍不支持）

把 `target` **投影到 47 个 responder 张成的空间**上当训练标签
（`ŷ = R·β`，β 在训练段内加权最小二乘解）—— 与前面「**换成** `responder_k`」不同，
它**保留 target 作锚点**，只削掉 responder 解释不了的那 17.3%。合法（推理端不需要 responder）。

**结果：更差。** 加进这个选项后 `inner_selected` 从 −2.80% 掉到 **−6.58%**，
1/5 折为正，去掉最好一折 −11.02%。第 4 折内层**确实选中了 `projection`**，
外层给出 **−17.67%** —— 直接证伪，不是「没测出来」。

⟹ **responder 作为「训练标签的任何线性变换」这条路彻底关闭**：
换目标（47 列全扫）、线性组合（`multi`）、投影去噪（`projection`）三种形态全部否决。

⭐ **但别把这读成「responder 全废」。** 还没试过的是**多任务表示学习** ——
共享编码器 + 多个头同时预测 `target` 与若干 responder，让 responder 当**辅助监督信号
塑造表示**，而不是替换/变换标签。上面否决的三种都是「换标签」，多任务是「换损失结构」，
不是一回事。LGBM 做不到，**属于 C 段的 NN 原型（8/23 后，硬性时间盒）**。
另外 8/23 标签回补的扩展训练数据也会带 responder，届时数据量更大。

## B 段（8/23 当天）：标签一到手，先修尺子，不许先训模型

按顺序，**不许跳步**：

1. **`git diff` 主办方重发的包**（`docs/` `examples/` `timeseries_api/`）——
   伤疤清单第 3 条记着原始发布包已丢失、`README.md` 与 `linear_window/train.py`
   被改过且无法核验，**8/23 是唯一能核实的机会**
2. **用 `src/metric.py` 在公榜测试期上复算我们已交过的每一份 CSV**，与公榜实测对拍。
   这是重建本地尺子的第一块砖 —— **对不上就说明理解有误，先查清楚再往下走**
3. **诊断 alpha 反转**（第 1 项）：在真实测试期上跑 alpha 2e6 vs 5e5。
   这个答案决定私榜该用多强的收缩
4. **定交付 scale**（第 6 项的预注册判据，别临时改口径）

## C 段（8/23 – 8/31）：本地尺子能信之后

1. **②类旋钮一次性网格搜** —— alpha / 特征数（`ab_featsweep` 说 300~323 可能更好）/
   树深 / 轮数 / 学习率 / `sample_modulo`。憋了一个月的东西全靠这几天
2. **NN 原型**（已定放这里）：推理成本反而不是问题（几个矩阵乘比 1440 棵树便宜），
   价值在于与 GBDT 误差结构不同、做集成有料。⚠️ 只剩 8 天且**没有公榜可验证**，
   **必须设硬性时间盒，到点不成就砍**，不许挤掉重训和打包
3. **用扩展数据重训**最终模型
4. **打包私榜 zip**（`scripts/make_submission.py`，由本人执行）：端到端计时 +
   `predict_total` 实测（1440 棵树）+ 0 超时 + 主路径与 numpy 兜底对拍。
   10 次机会，**至少留 3 次余量**

## 对 0.0045 的诚实判断

**2026-08-11 重算。** 到 0.0045 现在只需 IC **+17.6%**（现在 **≥0.05703**，目标 0.06708）——
今早还是 +30.8%。IC 增幅史：

    +25.4%（严格求解器+scale）→ +10.7%（LGBM 截面块）→ +4.1%（replace）
    → +2.0%（480 轮）→ +0.8%（phase_balanced）→ **≥+11.1%（每资产历史特征）**

⭐ **原文那句判断被再次证实**：「唯一一次两位数跳跃来自新增一个信号来源，不是调参」——
这次的 ≥+11.1% 又是**新增一个信号来源**（项目第一次引入**时间维度**，
此前所有特征都是同期截面的），而不是任何一个旋钮。
⟹ **优先找新信号轴，不要优先拧旋钮。** 剩下没被碰过的信号轴：
history 的窗长/列数（从没搜过）、截面 rank（A2）、非线性组合（A4）。

**已用掉的与还剩的**：A 段原计划「四项全中乐观 +5~10% IC」，而**单单 A1′ 就给了 ≥+11.1%**，
超出整个 A 段的乐观上限。②类网格搜历史上值 +10~25%（alpha 那次 23.8%）且还没做
（要等 8/23 回补后本地网格搜）。**0.0045 从「需要意料之外的赢家」变成「够得着」。**
每周复核一次，别让它变成执念。

---

## 现在的主线（按价值排）

### 0. ✅ 当前最好：`v3_hybrid` **replace**（岭回归的 `m̂` + LGBM 截面分量全量替换）

**2026-08-10 更新：轮数 160→480，ridge 换成 `phase_balanced`。**

| | |
|---|---:|
| 已交最好成绩 | **0.0026330806**（@ scale 1.16） |
| 真实峰值 | **0.00263415**（@ 最优 scale 1.1371，IC 0.05132） |
| vs 480 轮 + 生产 ridge（0.00258931） | **+1.7%** |
| vs 160 轮（0.00248770） | **+5.9%** |
| vs 严格岭回归（0.00187236） | **+40.7%** |
| 公榜第一 | 0.00520002（IC 0.07211）—— 我们到它的 **50.7%**（IC 到 71.2%） |

**当前最好模型不是一个现成目录**，是两个候选产物的组合：
`outputs/candidates/v3_hybrid_r480/`（LGBM 截面块，3 种子 × 480 轮）
+ `outputs/candidates/v1_ridge_phasebal/baseline_model.json`（产 `m̂`）。
拼装命令与出 CSV 的命令见 NOTES「当前最好模型是怎么拼出来的」。

⚠️ **生产 `model/` 仍是 160 轮 / `blend_weight` 0.5 / `periodic`** —— 先测后转正，等你决定。
转正前要先确认推理耗时（1440 棵树，见第 4 项第 5 点）。

⚠️ **交付 scale 未定**，见第 6 项。当前模型的最优 scale 是 **1.1371**。

### 0b. ❌ 已结案（2026-08-10）：重建市场模型 —— **那个 13% 余量不存在**

> 本条 08-09 立为「最高优先级」，08-10 被 `experiments/market_model.py` 推翻。
> 原文保留在下面并标注，免得下周再走一遍。证据 `outputs/experiments/market_model.md`。

原来的四条理由里，**前三条仍成立**（200 列恒为 0；`m̂` 占预测方差 63%；
它是照「预测 y」训的），**第四条是假的**：

`mt_predictability` 那个比较里，候选是最小二乘（**自带最优缩放**，实测 a\*=0.877），
基准却是在 **`prediction_scale=0.5`** 下算的 —— 预测被人为砍半，R² 当然低。
换成尺度无关的 `peak = A²/B`（各臂在自己最优 scale 下的 R²_m）之后：

| 臂 | peak_m | vs 基准 |
|---|---:|---:|
| **基准**（生产 `m̂`，无权截面均值） | **+0.002474** | — |
| 直接回归 m_t（加权 X̄ = 原候选） | +0.002416 | **−2.4%** |
| 直接回归 m_t（无权 X̄，可交付） | +0.002524 | +2.0%，但 6/10 折、去掉最好一折翻负、6 档 α 只 1 档为正 |

**基准的真实水平比原来报的高 58%**（0.001565 → 0.002474），13% 全在这里。
机制自检：同一份预测按固定 scale 报出来是 +0.001766，与 `mt_predictability` 逐位相同。

**而且数据量这个杠杆是反的**：训练窗 +52% 之后直接回归纹丝不动，
Δ 从 +2.0% 变成 **−4.9%**。行级岭回归能从 15 倍的行里榨出市场信号，
「先取截面均值再回归」把那部分信息在进模型之前就扔了 —— **外推到全分辨率只会更差**。

> ⚠️ 08-10 下午更正：这里原来还写着「基准涨 7.3%」，**那个数已被 `ridge_data_ladder` 推翻**
> （两次运行的验证段不同，而 `peak_m` 逐折能差 39 倍）。配对之后是 **−3.7%**。
> 但本段的结论**不受影响** —— `direct_u` vs `implied_u` 是同一次运行内的配对比较。

四条留下来的产物：

1. ⭐ **无权口径不吃亏**（与加权差 0.4%，可交付版本反而更好）——
   以后不必为「推理端拿不到 weight」设计绕路
2. **对 m_t 做特征预选有害**：top-50 −27%、top-200 −2.7%，323 列全上最好
3. ⭐ **`peak = A²/B` 应作为以后比模型的默认指标**。固定 scale 下的 R²/分数会把
   「幅度碰巧对」算进模型质量 —— 这次它造出一个 13% 的幻觉，还被写进 ROADMAP 当最高优先级
4. ⭐ **跨运行比 `peak` 的绝对值是无效比较**，除非验证段逐位相同 ——
   与「本地绝对分 ≈ 公榜分」被撤回同一个病，**有效的永远是配对 Δ**

**还没排除的**：非线性市场模型。但 `lgbm_mt` 两轮已否定过树做 m_t，先验很低。

⚠️ **相位错配（第 2 项）没有因此被解决** —— 它原计划搭这趟车，现在得单独走。

### 0d. ❌ 已结案（2026-08-10）：岭回归缺数据 —— **加数据买不到东西**

生产岭回归只用了 13.2M 行的 **8.7%**（`train_rows = 1,146,659`），看着像个现成的杠杆。
`experiments/ridge_data_ladder.py` 用**逐位相同的验证段**（折按最大档划一次、
小档取同一训练区间的后缀）+ 每行正则强度不变（`fold_alpha ∝ 窗口`）测了一条阶梯：

| 训练窗（原始 time_id） | 训练行数 | `peak_m` | vs 生产档 |
|---:|---:|---:|---:|
| 300,000 | 443,359 | +0.002083 | **−20.1%** |
| **394,800（生产等效）** | 585,463 | **+0.002607** | — |
| 500,000 | 743,127 | +0.002618 | +0.4% |
| 600,000 | 893,100 | +0.002512 | −3.7% |
| 700,000 | 1,043,053 | +0.002557 | −1.9% |

四条判据全不过。⭐ **这次能说「没效果」而不是「测不出来」，因为阴性对照响了**：
数据砍掉 24% 时 `peak_m` 掉 20.1% —— 尺子对数据量有分辨力，
它只是在生产窗口以上再也读不到东西。（`mt_lagged` 那次缺的正是这个对照。）

⟹ **不要去接全量训练的正规方程解法**，ROADMAP 第 2 项里那条「(a) 训练覆盖全部相位
（全量或更密采样）」的**「全量」那一半可以划掉，「全相位」那一半仍然要测** ——
那是「换哪些行」不是「加多少行」，另一根轴。

<details><summary>原文（08-09 写的，已推翻）</summary>

`replace` 是 `f = m̂ + ê_lgbm`，岭回归**唯一的作用就是产 `m̂`**。而：

1. **它 400 列里有 200 列贡献恒为 0** —— 截面去均值列按构造零均值，
   `(Σᵢdevᵢ)·coef = 0`，实测 `max|Δ| = 4.5e-08`（纯浮点误差）
2. **`m̂` 占预测方差 63%**，是账面上最大的一块
3. **它是照「预测 y」训的，不是照「预测 m」训的** —— 特征选择、alpha、设计矩阵
   全是为 400 列预测 y 定的
4. `mt_predictability` 早就量过：专门回归 m_t 的样本外 **R²_m = +0.00177，
   比现在隐含的 +0.00157 高 13%**

做法：把「预测 m_t」当成一个独立的建模问题重做（设计矩阵 = 特征的截面均值，
目标 = m_t），复用 `experiments/mt_predictability.py` 的机制。
这是**第③类（结构性）**改动 → 本地滚动配对 Δ 能信，先本地筛、公榜只做确认。
**相位错配（第 2 项）也应该在这个新模型上一起解决**（`--sampling` 口子已开）。

⚠️ 期望值：R²_m +13% 不等于总分 +13%，要看 `A_m` 占多少 —— 而那正是下面这一项要量的。

</details>

### 0c. ❌ 已结案（2026-08-10 傍晚）：分量配比值 **+0.02%**，等于零

花了 2 次额度（`m̂@1.3` = 0.0014991067、配比 mix2 = 0.0025767425）。
mix2 **比榜上还低**，因为我给的系数建立在估错的 `B_m` 上。

⭐ **但这两次买回来一个更值钱的东西**：三个公榜点 + `A_m + A_el = A(480)` 这条精确关系
把方程组定死，`A_m`、`B_m` 从此是**真值**不是估计：

| | 存档 Gram 估的 | **真值** | |
|---|---:|---:|---:|
| `B_m` | 0.00112820 | **0.00131423** | 估低 **14.2%** |
| `A_m` | 0.00130991 | **0.00143083** | 估低 8.5% |
| `A_el` / `B_el` | — | 0.00091888 / 0.00081805 | |

用真值重算：2 分量真最优峰值 **0.00258991**，只比单一 scale 的 0.00258931 高 **+0.02%**。
市场块占分 **60.1%**（不是先验说的 85%）。

⟹ **一个 OOF 线性 stacker 学出来的就是这组系数，所以那条路的上限也是 +0.02%。**
还没试过的只剩**条件性/非线性**组合器（按截面离散度或 regime 调两块权重）—— 见路线 A4。

⚠️⚠️ **无权 B 代理在「分量」上失效**（08-09 在完整预测上验过 1.6%，这里偏 14.2%）。
`m̂` 在每个 time_id 内是常数，加权/无权二阶矩之比与完整预测不是同一族。
**规矩：无权 B 代理只在结构相近的完整预测之间用。**

<details><summary>原文（08-10 上午写的，已推翻）</summary>

### 0c. 🔥 一次额度解锁最优配比 —— **轮数结算后价值变大，现在是头号活项**

`f = c_m·m̂ + c_r·ê_ridge + c_l·ê_lgbm`，二阶矩已全部已知
（`⟨m̂,ê⟩ ≡ 0`，B_m=0.00112820 / B_er=0.00032887 / **B_el(480)=0.00100408**，
`corr(ê_r,ê_l)=0.618`）。**唯一缺 `A_m`。**

交一次「只有 `m̂`」的 CSV（**纯算术，不用跑模型** —— 就是 replace CSV 的逐 time_id 截面均值）
⟹ `A_m` 精确 ⟹ `A_er = A(0) − A_m`、`A_el = A(480) − A_m` 全出来
⟹ 最优配比与最优峰值有闭式解 `peak_opt = aᵀM⁻¹a`。

**为什么值**：① 现在的 2 自由度族隐含约束 `c_r + c_l = c_m`，放开是**免费**的第①类改动；
② `replace` 把 `ê_ridge` 整个丢了，而它与 `ê_lgbm` 只有 0.618 相关；
③ 拿到 `A_m` 才知道市场块和截面块各占多少分，**决定剩下约 60 次额度往哪投**。

**2026-08-10：轮数从 160 提到 480 之后这一项的价值变大了。** `B_m` 不随轮数变、
`B_el` 涨了 49.8%，两块的最优 scale 分家更开 ⟹ 光是 **2 分量解耦**（`m̂` + `ê_lgbm`，
连 `ê_ridge` 都不用）就值 **+0.1%~+9%**，取决于 `A_m` 落在哪。
（我 08-09 写的「只值 +0.20%」是 160 轮上的数，已作废。逐档见 NOTES。）

⚠️ **先交 2 分量、别直接上 3 分量**：2 分量的系数在整个 `A_m` 先验区间里都很温和
（`c_m` 1.06~1.42、`c_l` 1.07~1.12）；3 分量的 `c_r` 从 +0.225 走到 −1.065 **中途变号**，
且 `B_er` 两个来源差 1.13%（`B(0)−B_m = 0.00032515` vs Gram 反解 0.00032887）。
判据：`A_m` 落定后 `|c_r| > 0.3` 且对 `B_er` ±1.5% 不变号，才值得为 3 分量再花一次额度。

</details>

### 1. 🔒 【阻塞至 8/23，但它定了所有实验的规矩】本地与公榜为什么在 alpha 上量反了

**症状**：公榜峰值对峰值 alpha 2e6 比 5e5 高 23.8%；三把本地尺子都说反话。

| 尺子 | 2e6 / 5e5 峰值比 |
|---|---:|
| 公榜 | **1.238** |
| 相位隔离（训练相位0+5，验证相位1+6） | 0.935 |
| modulo 10（只评训练相位） | 0.870 |

**两个假设都试过、都不成立**：

| 假设 | 怎么测的 | 结果 |
|---|---|---|
| 本地只在训练过的相位上评估 | `--holdout-phase 1`，训练相位 0+5、验证相位 1+6 | ❌ 比值 0.935，方向没变 |
| 本地外推距离太短 | 验证段 49,345 → 164,490 → 246,740（比公榜还长 15%） | ❌ 比值 0.908→0.914→0.929，斜率外推需再加 3.3 倍训练数据才够 |

**剩下的候选**：公榜测试期本身的分布/regime 与全部训练期数据不同，
而更强的收缩对分布漂移更稳健。**本地测不了** —— 没有那段时期的标签。

**解决时点：2026-08-23 标签回补。** 拿到公榜测试期的标签后即可在本地复现公榜评分，
一次性诊断清楚。距今 15 天。

**在那之前的工作规矩（重要）**：任何影响「拟合紧密度」的参数
—— alpha、特征数、模型复杂度、树深、轮数、学习率 —— **本地结论一律不可信，
改用公榜两点法比峰值**。预算够：到 8/23 约 75 次（5/天），每模型 2 次 → 约 35 组配置。
不受影响的是纯后处理旋钮（scale、clip）和结构性诊断。

### 2. 训练采样与相位错配（效应真实，但不是上面那个问题的原因）

测试集是连续 time_id（**全部 10 个相位**），而生产 `sample_modulo=5`
**只在相位 0 和 5 上训练**。`phase_diagnostic` 实测：

- 相位 5 的行比相位 0 难预测 **2.1 倍**
- 训练里混进相位 5，模型在相位 0 上也掉 **27%**（多给一倍数据反而变差）

说明不同相位的「特征→target」关系不一样。两条可能的修法：
**(a)** 训练覆盖全部相位（全量或更密采样；13.2M 行的设计矩阵放不下，
要改成分块累积 XᵀX 的正规方程解法）；**(b)** 把相位当特征 / 按相位分组建模。

这件事同时污染测量口径 —— 现在所有本地分数都依赖于「取哪些相位」，
所以它排在其他建模改动之前。

### 2026-08-09 进展：口子开好了，可以开测

- `strategies/v1_ridge/train.py` 加了 **`--sampling {periodic,phase_balanced}`**
  （`src/io.py:time_sample_mask` 早就实现了，只是没接上）。实测同预算下
  `periodic` 只覆盖相位 0/5（各 20%），`phase_balanced` 十个相位各 4%
- **默认仍是 `periodic`，且默认路径下产物与生产模型 `c23a8cfb` 逐字节相同** ——
  `sampling` 这个键只在非默认时才写进 json，免得搞坏「重训能复现 sha」那条验收
- **LGBM 那半早就是 `phase_balanced` 了**，所以错配只剩在 ridge 上 ——
  而 ridge 承载 85% 的分

**怎么判**：它是「参数三分类」里的第③类（结构性改动），本地 ΔA 约 1:1 迁移
→ 本地配对 Δ 原则上能信。⚠️ **量它的时候验证段必须取全部相位**
（`ridge_phase_sampling.py` 做对了这一点，而 `walk_forward_rolling` 默认是抽样的）。

### 2026-08-10 结论：**本地永远测不出来，改走公榜**

折数从 3 加到 5（`--validation-partitions 4 5 6 7 8`，这已是 `--train-partitions 4`
下的上限）：

| | periodic | phase_balanced | Δ |
|---|---:|---:|---:|
| peak 均值 | 0.00095686 | 0.00097451 | **+1.84%**，4/5 折，p=0.188 |
| 去掉最好一折 | | | **−0.39%，翻负** |
| 配对 t | | | **0.44** |

⭐ **关键是检出下限**：逐折 Δ 的标准差 9.06e-05 ≈ 均值 peak 的 9.5%，
5 折下要 t≥2 得有 **+8.5%** 的效应；点估计只有 +1.8% ⟹ **需要约 105 折**才看得见。
分区只有 9 个，做不到。旁证：3 折→5 折，点估计从 −0.01% 跳到 +1.84%，估计量本身不稳。

⟹ **改成一次公榜额度**（第③类 + 重训，一点 + 无权 B 代理 → 0.3% 精度）。

### ✅ 2026-08-10 当晚结案：公榜说 `phase_balanced` **赢 +1.97%**，已上位

候选 = **只换 ridge**（`--sampling phase_balanced` 重训，其余逐项相同），
LGBM 三个文件与 `v3_hybrid_r480` 逐位相同：

| | A | B | 最优 scale | 峰值 | IC |
|---|---:|---:|---:|---:|---:|
| r480 + 生产 ridge | 0.00234971 | 0.00213228 | 1.1020 | 0.00258931 | 0.05089 |
| **r480 + phase_balanced** | 0.00231661 | 0.00203735 | 1.1371 | **0.00263415** | **0.05132** |

同 scale 1.16 原始分 **+1.97%**（不依赖任何近似）；峰值 +1.73%，B 拨 ±1% 不翻。
`ê` 逐位相同 ⟹ 市场块可直接剥出：**`peak_m` +2.86%**。

⭐⭐ **由此得到一条新规矩（写进「参数怎么调」）**：本地 5 折的点估计是 **+1.84%**，
但 t=0.44、去掉最好一折翻负 —— **按判据是「测不出来」，照判据办事就不会去交这一次**。
公榜实测 +2.86%，**方向对、量级对（1.6 倍）**。加上 `ab_decomposition` 那次的 1.07，
这是第二个独立证据。

> **第③类（结构性）改动：看本地配对 Δ 的点估计的符号与量级即可上公榜确认，
> 不要求本地显著。** 分区只有 9 个 ⟹ 本地检出下限卡在 +8% 量级，
> 而真实效应普遍 +2~3%，**等显著等不到，只会漏掉真的**。
> ⚠️ 只对第③类成立 —— 第②类本地连符号都是反的。

**还没做**：LGBM 那半早就是 `phase_balanced`，所以这条轴到此为止；
但 ridge 的 `sample_modulo` 本身（5 → 更密）没单独测过，属于第②类，留到 8/23 之后。

### 3. ✅ 修 train.py 的线程可复现性 —— **已结案（2026-08-08）**

`tol 1e-4/max_iter 100` → `1e-8/2000`，正式模型已换成严格求解器那份
（sha `e2bec9a9…` → `c23a8cfb…`），7 道门禁全过，配对 Δ +1.87e-06 在噪声地板内。

**分数复现无条件成立**（三档线程数下相对漂移 1.97e-06，6 位有效数字一致）→ 答辩那条已安全。
**逐位复现仍需钉线程数**（`OPENBLAS_NUM_THREADS=4 OMP_NUM_THREADS=4`），
这是 LSQR 迭代解法的固有性质，收紧 tol 只能压小、压不到 0。详见 NOTES.md。

### 4. LightGBM（2026-08-08 立项）—— 现在是**唯一没被排除**的方向

**先做三件前置检查，任何一件不过就别往下投入：**

1. ~~**评测环境到底有没有 lightgbm？**~~ → ✅ **已问主办方，有**（2026-08-09）。
   残余风险只剩模型文本是 `version=v4`（LightGBM 4.x 格式），
   评测端若是 3.3.x 会读错 —— 那是「import 成功但结果不对」，`try: import` 兜不住。
   `main.py` 因此保留纯 numpy 兜底 + 开机对拍自检，见 NOTES.md。
2. **推理预算**：岭回归是 0.42 ms/次 × 21.4 万次 = 89 秒，余量很大。
   LightGBM 三个种子 × 上千棵树要实测；4 核 / 12GB / 无 GPU / 顺序推理，超时该 time_id 置 0。
3. **本地依赖**：主 venv 是 Python 3.13 + numpy 2.5 + pandas 3.0，而增补包 README 钉的是
   numpy 1.24.3 / pandas 2.0.3 / pyarrow 11.0.0（这些没有 3.13 的 wheel）。
   **别为它降级主 venv**——v1_ridge 的数值复现绑在当前这套栈上。真跑不起来就单开 `.venv-lgbm`。

**做法**：~~先用主办方增补包跑通烟测~~ —— 2026-08-08 更新：

- 前置检查 1（评测环境有没有 lightgbm）**不再阻塞近期工作**：公榜交 CSV，
  不依赖评测环境；只有 8/31 私榜 zip 才需要，而 `docs` 明说 zip「包含所有相关依赖」，
  且 `model_to_string` 是纯文本 → 纯 numpy 写树遍历可兜底。仍应问主办方，但并行做即可。
- 前置检查 3（本地依赖）**已排除**：`lightgbm 4.3.0` 在主 venv
  （py3.13 / numpy 2.5.1 / pandas 3.0.3）上 `Dataset` / `train` / 早停 /
  `predict(num_iteration=k)` / `model_to_string` 全部正常，**不需要 `.venv-lgbm`**。
- ⚠️ **主办方基线的加载路径在本机必然 OOM**：`load_parquet_files` 先读全 9 分区再 `pd.concat`，
  实测峰值 ≈33.1 GiB > 本机 30 GiB；`--max-train-rows` 作用在 CV 循环内部、救不了。
  将来做截面块要先加一个**载入期**的分区/行上限。详见 NOTES.md。

**择时块 m_t 已经打完两轮（`experiments/lgbm_mt.py`）→ ❌ 结案。**

第二轮把尺子修严（3 种子平均 / pooled Δ / 判据写进代码 / 第三基准 `ridge_fixed`
零选择方差 / 收缩轴外推到 lr 0.005 + 轮数 3000）后：
**6 组候选对两个基准全部 INCONCLUSIVE，最大 |t| 只有 0.71，
14 个配对比较里「去掉最好一折」后的 t 无一为正。**
第一轮那个看着有希望的 `ultra_shrunk` 从 t=1.11 掉到 t=0.21 —— 那是噪声。

**保留的限制**：本地检出下限是公榜的 **+9%**。INCONCLUSIVE 是「测不出来」不是「没效果」，
要分辨这个量级只能靠公榜两点法。详见 NOTES.md。

**截面块 `e = y − m`（本节原定主攻）→ ✅ PASS，项目里第一次**（`lgbm_xs`，2026-08-08）

3 组候选对**两个基准全部 PASS**：`xs_loose` t=+6.38、5/5 折为正、去掉最好一折 t=+5.35、
机制判据带折扣通过。对比择时块（最大 |t| 0.71、去掉最好一折全翻负）是完全不同量级的信号。

⚠️ **两条限定，别当成已经赢了**：

- **ê 必须投影成截面加权零均值**，否则 `Δscore = 0.28 × ΔR²_e` 的恒等式不成立
  （投影只赚不亏，已解析证明；现测值是投影版的下界）
- **折扣后排序翻转**：本地最好的 `xs_loose`（+8.2%）折后不如 `xs_shrunk`（+11.1%），
  因为前者 ΔB 涨 28.1% 靠容量硬拟合。**要带走的是 `xs_shrunk`**

**装车验证已做完（`lgbm_blend`）→ ✅ 四个臂全部 PASS**

用比赛真正的评分口径、在完整预测上量：`replace_xs_loose` +43.0%（t=+7.36、5/5 折、
去掉最好一折 t=+6.25），**四个臂折后全部收敛到 +10.0%~+11.7%**。
ê_mean 实测占 ê 的 25~26%，投影买回约 7% 的 R²_e —— **投影是必需步骤**。

❌ 上一节写的「折扣后该带走 `xs_shrunk`」**装车后被推翻**，`xs_loose` 两个口径都赢。

**下一步该做的**（按阻塞程度排）：

1. ~~🚨 实测推理成本~~ → ✅ **已清，余量很大**（`lgbm_inference_cost`）。
   900 棵 × 3 种子 = 1.285 ms/次 → 21.4 万次共 **4.6 分钟**（岭回归 1.45 分钟且从未超时）；
   模型纯文本 18.5 MiB。成本随树数近似线性，可以用轮数换时间
2. ~~**接进 `strategies/` 做成可提交的策略**~~ → ✅ **已做**（`strategies/v3_hybrid`，
   含投影步骤 + 纯 numpy 树推理兜底 + 开机对拍自检）。
   端到端实测：主路径 4.93 分钟 / 兜底 5.44 分钟 / 0 超时；
   主路径的 CSV 与 08-08 已交那份**逐字节相同**，兜底那份 `max|Δ|/std = 6.4e-15`
3. ~~**公榜两点法验证**~~ → ✅ **已做（2026-08-09）**：峰值 **0.00229575**，
   较严格岭回归 **+22.6%**。见第 0 项与 NOTES.md「公榜裁决」
4. ~~**`replace`（`blend_weight` 1.0）应该换上去**~~ → ✅ **已确认（2026-08-09）**：
   **实测峰值 0.00248770**（预测 0.00245479，偏低 1.34%），比 blend50 **+8.4%**、
   比严格岭回归 **+32.9%**。只花了**一次**额度（`A(1)` 是精确值）。
   `hybrid_meta.json` 尚未转正，等你决定
5. **轮数：加不减** —— `num_iteration` 80 的峰值 0.00237519，**比 160 轮低 4.5%**
   （免训练测的：模型文件本来就有 160 棵树）。逐折 `best_iteration` 44~302、折均才 160，
   偏保守 → **下次重训往 320 走**。⚠️ 这与 ridge 上「公榜偏好更强收缩」方向相反，
   **「公榜总偏好更强收缩」不成立**，每个旋钮单独量

   **2026-08-10：✅ 已结算，轮数轴关闭。** 480 轮重训 + 嵌套性机械验证
   （`outputs/candidates/v3_hybrid_r480/`，`experiments/lgbm_nested_check.py`：
   前 160 棵树与生产模型 `max|Δ| = 1.7e-18`，meta 逐位一致）之后，
   r320 / r480 各交一次（都钉 scale 1.16，同 scale 原始分可直接比大小）：

   | 轮数 | 峰值 | vs 160 | 最优 scale |
   |---:|---:|---:|---:|
   | 80 | 0.00237519 | −4.52% | 1.2261 |
   | 160 | 0.00248770 | — | 1.1761 |
   | 320 | 0.00256656 | +3.17% | 1.1329 |
   | **480** | **0.00258931** | **+4.08%** | 1.1020 |

   **⛔ 不再为轮数花额度**：按 log2 折算每翻一倍的增益 +4.74% → +3.17% → +1.52%，
   逐次减半，剩余总余量约 **+1.8%**，代价是推理树数再翻倍。
   ⚠️ **带走 480 还是 320 取决于推理耗时**。
   ✅ **2026-08-11 已实测**（4 线程，21.4 万次 predict，官方切片语义）：

   | 配置 | `predict_total` | 增量 |
   |---|---:|---:|
   | 160 轮 / 无 history（现生产） | 4.93 分钟 | — |
   | **480 轮 / 无 history** | **6.64 分钟** | +1.71（轮数） |
   | **480 轮 + history（A1′ 候选）** | **7.95 分钟** | +1.31（history） |

   ⚠️ 原来那个「线性外推 ≈ 4 分钟」**低估了 66%**（实测 6.64）。
   三档都是 0 超时、0 个非有限值。公榜交 CSV 无风险（本地生成）；
   **8/31 私榜 zip 跑在主办方 4 核机上，7.95 分钟是目前的实测底数** ——
   主办方从未公布超时阈值。

   🚩 **2026-08-11 风险升级：降轮数这条退路被公榜证伪。**
   同一批模型文件交 160 轮（480 轮的前 160 棵树，`lgbm_nested_check` 验过 1.7e-18），
   同 scale 1.16 → **0.0029039065，比 480 轮低 10.71%**。
   而**本地说反话**（160 轮 history peak 0.00192421 ≥ 480 轮 0.00191184）—— ②类量反再次证实。
   🚩 **320 轮当天也交了，同样不行：0.0031385676，比 480 轮低 3.50%。**
   而**旧模型（无 history）上 320 只低 0.88%** ⟹ **history 让模型对轮数更敏感**。
   ⟹ 上面那句「320 只比 480 低 0.9%，是便宜的退路」**对现模型正式作废**。

   完整阶梯：160 **−10.71%** / 320 **−3.50%** / 480 基准（单调，无内部极值）。
   ⟹ **私榜必须带 480 轮，7.95 分钟甩不掉。**
   「减 history 列数」这条也没了 —— c80 打平、c40 已是最省的那个。
   **压超时只剩一个未测杠杆：减种子数（3→2）**，线性省 1/3 树遍历但会掉分，
   8/31 打包前必须实测 `predict_total` 再定。

   **副产物（改变了 0c 的价值）**：`B_el(480) = 0.00100408`，比 `B_el(160) = 0.00067024`
   涨 **49.8%**，而总 `A` 只涨 11.1% → `m̂` 与 `ê` 的最优 scale 分家分得更开，
   「放开单一 scale」这个**第①类免费改动**现在值 +0.1%~+9%（取决于 `A_m`）。
   详见 NOTES「`A_m` 的价值随轮数变大」。

   **❌ 顺带排除（0 额度）**：把四个轮数的预测线性组合 = 给 boosting 各段树重新配权。
   理论峰值 0.00261132（+0.85%），但 Gram 条件数 3038、系数在 ±1.5 间横跳；
   按 `A` 的真实精度 ±0.3% 扰动 2000 次，实际期望只剩 **+0.18%**、5% 分位 −1.5%。

**期望值**：当初写「折后 +10~12% 落到约 0.0021」，**实测 0.00229575，预测偏保守**
（因为折扣系数在这类改动上过严）。但到 0.0052 还差 2.3 倍，
截面块这条线基本吃完了（它总共只占 28% 方差 / 15% 的分），**不是通往 0.004 的那条路**。

**主攻方向应该是截面块**：它占 28% 方差却只出 15% 的分（R²_截面 0.00071，
只有 R²_择时 的一半），是被浪费的那一块，而非线性 / 交互正是树的强项。
择时块的线性部分已近饱和（专门回归 m_t 的样本外 R² 只比模型隐含的高 13%），
要在那边突破得靠**滞后输入**（见第 3 项），不是换模型。

**调参方式必须改**（第 1 项的直接后果）：树深、轮数、学习率、`num_leaves` 全是
「拟合紧密度」参数，本地网格搜索会像 alpha 那样量反。改成：
**预注册 3~5 组超参（照增补包的做法，不做网格搜索）→ 每组用公榜两点法拿精确峰值 → 比峰值选**。
每组 2 次额度，到 8/23 完全够。本地只用来确认「跑得通、不超时」，不用来选参。

### 5. 等权平均集成

两个预测子各自 IC=ρ₁、ρ₂，彼此相关 c，等权平均后
`IC_blend = (ρ₁+ρ₂) / √(2(1+c))`。

**别去拟合混合权重**——拟合单个标量的教训见 `ab_scale_auto`（每折 a\* 从 0.34 到 1.78，
配对 A/B 测不出收益）。0.5/0.5 是先验，先验不会过拟合。

> ⚠️ 这一节的编号被 `hybrid_meta.json` / `main.py` / `train.py` / `lgbm_blend.py`
> 以「ROADMAP §5」引用（含已提交的模型产物），**别给它换号**。新条目往后加。

### 6. ✅ 交付 scale —— **2026-08-11 提前结案，不必再花额度**

**结论：交付用 scale 1.16，或最优 1.1958，两者只差 0.090%。**

新模型（480 轮 + history）在公榜上交了 **两个点**（scale 1.16 与 0.90），
联立 `S = 2aA − a²B` 解出真值（回代残差 0）：
`A = 0.00272226`、`B = 0.00227652`、**峰值 0.00325527**、**最优 scale 1.1958**、IC 0.05705。

用 `Score(a)/峰值 = 1 − (1 − a/a*)²`：**scale 1.16 处已达峰值的 99.910%**。
⟹ 下面那套「本地最优 × 实测比值 / 调和平均 1.112」的预注册判据**不必再执行** ——
它是为「只有本地尺子、公榜最优未知」准备的，而现在两点法已经把 `a*` 直接量出来了。
**8/23 之后也不用重跑**：`a*` 是这个模型的属性，与标签回补无关。

⚠️ 唯一还要做的是**把 `hybrid_meta.json` 的 `prediction_scale` 从 0.856 换掉**
（0.856 对新模型只有峰值的 `1−(1−0.856/1.1958)² = 91.9%`，白丢 8%）。转正时一并改。

下面的历史推导保留作对照，**不再是决策依据**。

---

证据 `experiments/scale_transfer.py` → `outputs/experiments/scale_transfer.md`。

**先纠正一处口径错误**：08-09 的计划书拿「岭回归本地最优 0.759」对公榜 1.1350 得出比值 1.50，
但 **0.759 是 alpha=5e5 那个配置的，不是生产模型（alpha=2e6）的**。生产 alpha 下
五把尺子给 0.865~0.973（均值 **0.912**）。小 alpha 抬高 B、压低 a*，
那个数把「换模型」和「换尺子」混在了一起。

hybrid 同理：`blend50_xs_loose` 的 0.856 出自「内层选 alpha」的基准。
改走同一次实验里的**臂间比** 1.120（公榜上的同一个比是 1.075，差 4% → 互相印证）：

| 模型 | 本地最优（生产口径） | 公榜最优 | 比值 |
|---|---:|---:|---:|
| 严格岭回归 | 0.912 | 1.1350 | **1.244** |
| v3_hybrid | **1.022** | 1.2196 | **1.194** |

**口径对齐后两端只差 1.2 倍，不是 1.42 倍。** 代入 `Score(a)/峰值 = 1 − (1 − a/a*)²`
（损失是**相对误差**的平方，关于比值对称）：

| 取值 | 真最优=本地 | 真最优=公榜 | 最坏 |
|---|---:|---:|---:|
| 1.022（本地最优） | 100.0% | 97.4% | 97.4% |
| **1.112（调和平均，minimax）** | 99.2% | 99.2% | **99.2%** |
| 1.220（公榜最优） | 96.2% | 100.0% | 96.2% |
| **0.856（`hybrid_meta.json` 现值）** | 97.4% | 91.1% | **91.1%** ← 真正的问题 |

**所以这件事的正确结论不是「本地还是公榜」，而是「别用 0.856」** ——
那是训练脚本里的本地占位值，最坏要掉 8.9%；而 [1.02, 1.22] 里随便取都只掉 1~3%。
限幅从 1.435 起生效，全部候选都安全。

**预注册判据（现在写下，8/23 执行）**：标签回补后在公榜测试期直接算 hybrid 的真实
`A`、`B`、`a*` 与本表对账；同时算「训练期最后几折的 a*」，看 1.19 是不是**时期效应**
（越靠近测试期 a* 越高）。是 → 用「本地最优 × 实测比值」（私榜评的是更靠后的时期）；
不是 → 用本地最优 1.022。**8/23 出岔子就退回调和平均 1.112**，不用公榜最优。

8/31 私榜截止，8/23 给出 8 天余量。

### ⚠️ 关于「上 0.004」这个目标，先把账算清楚

Score = IC²。**现在公榜峰值 0.00229575 → IC = 0.04791**；目标 0.004 → 需要 **IC = 0.06325**，
还要 **1.32 倍**。公榜第一 0.00520002 → IC 0.07211。

这张表是 08-08 写下的**预测**，08-09 拿到了实测：

| LightGBM 的 IC | 与岭回归相关 c | 集成后 IC | 集成后 Score |
|---:|---:|---:|---:|
| 0.0432（与岭回归同强度） | 0.6 | 0.0483 | **0.00233** |
| 0.0432 | 0.3 | 0.0536 | 0.00287 |
| 0.0663 | 0.5 | **0.0632** | **0.00400** |

**第一行预测 0.00233，实测 0.00229575 —— 打中了。** 这条集成公式在本项目上是准的，
以后可以拿它做事前估值（注意：它给的是**上界性质**的估计，要求两个预测子各自 IC 已知）。

**结论不变：把一个和岭回归同强度的 LightGBM 混进来，只能到 0.0023 左右，到不了 0.004。**
要 0.004，LightGBM 自己就得有 IC≈0.066（已经是公榜第一 0.0721 的 92%）。

所以路线应该是：集成拿那稳稳的 +25%，**但真正的突破口在别处**——
按今天的分数拆解，85% 的分来自择时块，而那块的线性部分已经榨干。
最可能的量级增益依次是：**相位错配（第 1 项）→ 滞后特征进择时块（第 3 项）→ 非线性做截面块（第 4 项）**。

## ⭐ 参数怎么调：分三类，每类判法完全不同（2026-08-09 定）

**先问「这个参数属于哪一类」，再决定花不花公榜额度。**

| 类别 | 例子 | 怎么判 | 成本 |
|---|---|---|---|
| **① 纯后处理**<br>（预测的解析函数） | `prediction_scale`、`blend_weight`、**分量配比 `(c_m,c_r,c_l)`**、集成权重 | **解析求解**：`A` 对它是线性的 → 已有的公榜点就能把整条曲线钉死；`B` 用历史 CSV 的二阶/交叉矩算（**按 −1.5% 修正后精度约 0.3%**） | **0 次** |
| **② 拟合紧密度** | `alpha`、特征数、轮数、树深、学习率、`num_leaves` | 本地量反（公榜 2e6/5e5 峰值比 1.238，五把本地尺子 0.87~0.94），**只能公榜判**。同一线性族里 `A` 能钉死则 1 次，否则两点法 2 次 | 1~2 次/组 |
| **③ 结构性改动** | 加一个新分量、换训练分布（相位）、换基底 | 本地 ΔA 约 **1:1** 迁移（实测比值 1.07）→ **本地滚动配对 Δ 这次能信**，先本地筛，公榜只做最终确认 | 0~1 次/组 |

五条配套规矩：

1. **别再对第①类花额度。** 08-09 的实证：`blend_weight` 整条曲线是免费算出来的
   （`A(w)` 线性 + 两个已有公榜点），还顺带翻了 `blend50 优于 replace` 那个案。
2. **动第②类之前先问「`A` 能不能钉死」。** 只改后处理的同族模型能；换了模型不能。
3. **第③类别再无脑打 2.2 折。** 那个系数只对第②类成立，用在结构性改动上会把真收益折没
   —— `blend50` 就是这么被判错的（折后预期 +11.1%，实测 +22.6%）。
4. ⭐ **第③类看本地点估计的符号与量级，不要求本地显著（2026-08-10 新增）。**
   相位那次本地 5 折 +1.84%、t=0.44、去掉最好一折翻负 —— 按判据是「测不出来」，
   **照判据办事就不会去交，而公榜实测 +2.86%**。分区只有 9 个 ⟹ 本地检出下限卡在
   +8% 量级，真实效应普遍 +2~3%，**等显著只会漏掉真的**。⚠️ 只对第③类成立。
5. ⚠️ **无权 B 代理只在结构相近的「完整预测」之间用（2026-08-10 新增）。**
   它在完整预测上验过 1.6%（修正后 0.3%），但拿去估**分量** `m̂` 的 `B` 时偏了 **14.2%**
   —— `m̂` 在每个 time_id 内是常数，加权/无权二阶矩之比与完整预测不是同一族。
   这与「跨运行比 `peak` 绝对值无效」「本地绝对分 ≈ 公榜分已撤回」是同一类错误：
   **把在 A 场景标定过的近似搬到 B 场景。**

## 用尺子的规矩（P0 的产出，比 P0 原定目标更重要）

- **结论一律看每折的配对 Δ，不看绝对分。** 绝对分 SE=2.11e-04，
  换一套 fold 边界均值就漂 1.44e-04。
- **噪声地板不是常数，取决于你在测什么改动**——两臂共享的计算越多，Δ 越安静：

  | 两臂共享什么 | 噪声地板 |
  |---|---:|
  | 同一次拟合，只换后处理旋钮（scale / clip / intercept） | 9.9e-06 |
  | 只共享 fold 切分，各自拟合（feature_count / alpha / 基底） | 2.16e-05 |
  | 还多一个自己在抖的派生参数 | 2.2e-04 |

- **相关的旋钮必须联合扫**。单独扫会得出错误结论——`feat323` 就是这么被误判的。
- 效应没明显超过地板 = 没测出来，别写进结论。
- 🚩 **判据一律用 `peak`，绝不用任何固定 scale 下的绝对分。**（2026-08-11 加，
  这已经是同一个机制**第二次**咬人：先是「市场模型重建 13% 余量」，再是 A1 的 +2.5%。）
  机制：在 `Score(a) = 2aA − a²B` 上，固定 `a` 时分数对 B 的相对惩罚是
  `a²B/(2aA − a²B)`；生产的 A、B 代入 `a=0.5` 得 **≈0.22**，而 peak 的对应量是 **0.5**
  ⟹ **固定低 scale 对「方差膨胀」的惩罚不到 peak 的一半**。
  任何「加列 / 加分量」的改动都会抬 B，在固定低 scale 下就会被系统性高估。
  A1 实测：同一批拟合、同一批折、同一批系数，旧口径 **+0.71%**、peak **−1.97%**，
  **符号相反**（`outputs/experiments/history_peak.md`）。
- **看改动是靠什么起作用的，再决定信几分**（`ab_decomposition`）：
  把分数拆成 `Score(a) = 2aA − a²B`，`A` = 信号对齐、`B` = 预测方差。
  峰值涨 ⟺ `2·ΔA > ΔB`。**折 ΔA、不折 ΔB** 再判断。
  这条已经拦下过一个改动（截面标准差归一化：本地 +1.6%，折扣后 −5.6%）。

  ⚠️ **2026-08-09 修订：折多少取决于改动的性质，2.2 不是普适常数。**

  | 改动性质 | 公榜 ΔA | 本地 ΔA | ΔA 比值 |
  |---|---:|---:|---:|
  | 拧紧拧松同一个模型（alpha / 特征数 / 树深 / 轮数 / 学习率） | +12.3% | +27.4% | **2.22** |
  | 加一个结构不同的分量（LGBM 截面块 blend50） | +14.1% | +15.1% | **1.07** |

  第二条是 08-09 v3_hybrid 公榜两点法测出来的。**按旧的 2.2 折，blend50 该只有 +11.1%，
  实测 +22.6%** —— 折扣过严。直接后果：`lgbm_blend` 当初判 `blend50` 优于 `replace`
  的那个结论要重新算，见第 4 项。ΔB 那一半在换模型轴上只有一个点、方向还与正则轴相反。

## 已结案

### P0 · 验证框架重建 → ✅ 完成

1. `rolling_time_folds` 按 time_id 滚动切 10 折 —— 已实现（`src/validation.py`）
2. 配对 Δ —— 已实现（`walk_forward_rolling.py` 的具名配置注册表 + fit 缓存共享）
3. 噪声地板 —— 已实现（`--fold-offset half`）
4. embargo ≥ H —— 已做，H≈6（`mt_diagnostics` 实测自相关线性归零到 lag 5~6，
   与 MA(H=6) 窗口重叠理论吻合，与指数长尾明显不符）

**原定「标准误压到 2e-5 以下」没达到**：实测最好 ~1e-5（共享拟合）、2.2e-5（各自拟合），
而 6e-6 仍不可分辨。但真正的产出是上面那条「地板取决于测什么改动」。

前置诊断：73% 共同分量已在 9 分区复验 → 实测 0.626~0.732、均值 68.4%±3.4%
（p008=0.732 是最高的那个）。证据 `mt_diagnostics.md`。

### P1 · 三个便宜的改动 → ✅ 全部复测

1. `intercept = 0` —— 测不出差异（+1.8e-05，5/10 同号，地板 9.9e-06）。保持原值。
2. 闭式最优缩放 a\* —— **「每折重估 a\*」这个流程没用**（−7.6e-05，符号随 fold 边界翻转）。
   但**「scale 该取多少」这个问题极其重要**：本地最优 0.759，公榜最优 1.132，
   而原来用的是 0.5/0.6424。两件事当初被我混为一谈，导致过一次错误的框架性判断。
3. `--feature-count 323` —— 在 alpha=2e6 下 +5.9e-05，但那是**过度正则的假象**；
   alpha 调到 5e5 后 323 反而略差于 200。见 `ab_shrinkgrid`。

### P2-1 · 拆 [截面均值 ‖ deviation] 分别加罚 → ❌ 否决

已实现（`train.py` 的 `--design-basis mean_dev --market-alpha-ratio`，
系数换算回 `[raw ‖ dev]` 存盘，推理端零改动）。结果与假设完全相反：

| 择时分量的正则倍数 r | mean(Δ) | 同号 |
|---:|---:|---:|
| 1（只换基底） | +3.7e-05 | 6/10 |
| 3 | −1.6e-04 | 8/10 |
| 10 | −3.9e-04 | **10/10（p=0.002）** |
| 30 | −6.3e-04 | 9/10 |

**压制择时分量会大幅掉分。** 这个假设建立在一处推理漏洞上：
「分区级加权均值符号随机」是关于**十万个 time_id 的长期漂移**的陈述，
推不出「每个 time_id 的 m_t 不可预测」。两者是不同尺度的命题。

### 截面标准差归一化 → ❌ 不采用（2026-08-08，`ab_xsstd`）

`[raw ‖ (raw−截面均值)/截面sd]`。选它是因为固定容量、按 A/B 规则本该迁移性好。
实测机制不对：B 涨得比 A 还多（生产 alpha 处 ΔA +6.5% / ΔB +11.6%），
本地峰值 +1.6% 只是靠 A 在平方项上占便宜，折扣后预期公榜 **−5.6%**。

代码保留（`train.py --cross-sectional-scaling std`，默认 `none`；
`main.py` 缺该字段视作 `none`，旧模型不受影响），两条路径一致性已验证。

### 滞后的特征截面均值接进择时块 → ❌ 否决（2026-08-08，`mt_lagged`）

原本的理由很硬：m_t 的 ac1=0.836、lag 5~6 才归零，时序结构明摆着。
在**全时间分辨率**（888,315 个 time_id，不是 1/10 抽样 —— 那个口径下的「lag 1」
其实是真实 lag 10，早出了相关性范围，必然假阴性）上测四个臂：

| 臂 | 峰值 R²_m | ΔR²_m | 逐折 Δ 为正 | 去掉最好的一折 |
|---|---:|---:|---:|---:|
| `now`（仅当期） | +0.00085 | — | — | — |
| `lag1` | +0.00091 | +0.00005 | 4/10 | **−0.000069** |
| `lag3` | +0.00090 | +0.00004 | 3/10 | **−0.000056** |
| `roll6` | +0.00088 | +0.00002 | 5/10 | **−0.000002** |

七档 alpha（1e5~1e11）整条曲线都没上移，四个臂在 α=1e8 同时见顶且几乎重合。
表面增量全部在去掉单个最好折之后翻负。

**教训**：m_t 的自相关 ≠ 滞后特征能预测它。那个自相关的衰减形状贴合 MA(6) 窗口重叠理论
而非指数长尾（`mt_diagnostics`），说明它主要来自 **target 的构造方式**（前瞻窗口重叠），
是标签的性质，不是可利用的信息。

**后果**：择时块（85% 的分）的线性路到头了。`experiments/history_features.py`
的行级滞后特征仍未测过，但本次结果把它的先验拉低了不少。

### P2-2 · 特征能否预测 m_t → ✅ 能，而且已经是主要收入来源

`Score = share_m·R²_m + share_e·R²_e`，恒等式残差 5.89e-16：
**择时贡献 0.001138（85%）/ 截面贡献 0.000200（15%）**。证据 `mt_predictability.md`。

## 之后

- 截面标准化补全（除截面标准差 / 换 rank）—— 便宜，属于截面块
- 47 个 responder 当辅助目标做多任务
- MLP（本地 4060 训练，导 npz 用纯 numpy 写前向，消除 torch 依赖）

## 提醒

- **公榜交 CSV（`row_id,target`），私榜交 zip**。公榜每天最多 5 次成功评分（到 8/23），
  私榜策略文件共 10 次（到 8/31）。`scripts/make_submission.py` 是私榜用的。
- 刷公榜不是目标。**但「本地绝对分≈公榜分」这条已撤回**（那次吻合很可能是巧合，
  公榜测试期紧邻本地最好的一折；见 NOTES.md）。**有效的是配对 Δ，不是绝对分。**
  选配置一律看滚动配对 A/B，公榜额度只用于关键确认，每次都记进 `experiments/ledger.csv`。
- ~~**私榜交付的参数用本地最优，不要用公榜最优**~~ → **2026-08-09 加限定**：
  这条对「拟合紧密度」参数仍然成立，但对 **scale** 这个纯后处理旋钮要另算 ——
  本地与公榜的最优 scale 差 1.42~1.48 倍，而且**两个不同模型给出同一个比值**，
  说明那是时期效应而非过拟合。别照搬任一端，见第 6 项的预注册判据。
