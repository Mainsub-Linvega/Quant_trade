# ROADMAP.md — 改进优先级（源自 HANDOFF_20260724 §4/§5/§8）

## P0 · 验证框架重建（不做这个，后面全是在噪声里捞信号）

现状：3 个 fold，均值标准误 ≈ 5.1e-5，而要区分的差异是 6e-6 量级——尺子不够。

1. 按 time_id 滚动细切 8–12 个 fold（`src/validation.py::rolling_time_folds` 占位待实现），
   标准误压到 2e-5 以下
2. 一律看配对 delta（每 fold 的差 + 是否同号），不罗列绝对值
3. 先测噪声地板：同一配置，fold 边界平移半个分区跑两遍，漂移量 = 检出下限
4. 训练段与验证段之间加 embargo ≥ H 个 time_id

前置诊断（决定 embargo 大小 H）：
- m_t 自相关衰减形状：线性衰减到 0 → MA 型窗口重叠，H≈6；指数长尾 → 真实状态持续
- 73% 市场共同分量在 9 个分区上是否稳定（目前只测过 p008）

## P1 · 三个便宜的改动（一行级别，重构验收通过后做）

1. `intercept = 0.0`（保留 fit_intercept=True 估系数，只是不发布）——
   截距贡献 ±5e-5 的分数波动且与模型质量无关，等于全部测量噪声的一半
2. 闭式最优缩放 a* = Σwyf / Σwf² 替换拍脑袋的 0.5（内层 fold 拟合、外层报分）
3. `--feature-count 323` 跑一次对比（200 是单变量相关选的，在 73% 共同分量下
   大概率全是市场 beta 类特征；alpha=2e6 本来就压得住冗余列）

## P2 · 结构性实验（73% 发现的直接后果）

- 显式拆分设计矩阵为 [截面均值 ‖ deviation]：数学上与现在等价，但可对择时分量
  和截面分量施加不同 alpha——73/27 分配下可能是真提升
- 测特征能否预测 m_t：特征截面均值回归 m_t 时序，看 IC——73% 蛋糕的直接入口

## P3 · 之后

- 截面标准化补全（除截面标准差 / 换 rank）
- 接入时序状态（experiments/history_features.py 已写好未接入；
  注意其 walk_forward_history 结论 p≈0.15，不足以下结论，需 P0 框架复验）
- LightGBM
- 47 个 responder 当辅助目标做多任务
- MLP（本地 4060 训练，导 npz 用纯 numpy 写前向，消除 torch 依赖）

## 提醒

- 公榜额度不稀缺（到 8/23 约 150 次），私榜策略文件只有 10 次
- 刷公榜不是目标；定期花一次公榜额度做本地-公榜校准，记入 experiments/ledger.csv
